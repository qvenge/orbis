-- 0006_refresh_replay.sql
-- След предыдущего refresh для детекта реплея (OAuth 2.1 §7.5, Р7). Без него ротация
-- затирает единственный отпечаток токена в строке, и предъявленный повторно старый
-- refresh не с чем связать — отказать можно, а погасить цепочку нечем.
-- Уникального индекса намеренно нет: после отзыва значения могут повторяться.
ALTER TABLE "agent_grants" ADD COLUMN "prev_refresh_hash" text;
