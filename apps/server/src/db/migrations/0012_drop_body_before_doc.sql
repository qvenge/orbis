-- Снятие страховки обратимости после удачного переноса корпуса.
--
-- ИМЯ ФАЙЛА ЗАНИМАЕТ НОМЕР, КОТОРЫЙ ВОЗЬМЁТ СЛЕДУЮЩАЯ СГЕНЕРИРОВАННАЯ МИГРАЦИЯ. Накат от этого
-- не ломается (мигратор идёт по журналу, а этой записи в журнале нет), но если drizzle-kit
-- создаст свой файл с тем же номером, рядом окажутся два файла с одним номером и разной
-- судьбой. Тогда переименуйте ЭТОТ файл в следующий свободный номер — инструкция ниже от
-- этого не меняется, она про содержимое, а не про имя.
--
-- УЖЕ ПЕРЕИМЕНОВАН планом ADE-среза 1: 0010 занял entity_versions, 0011 — его RLS.
--
-- НЕ ЗАРЕГИСТРИРОВАНА в meta/_journal.json намеренно: когда снимать — решает владелец, а не
-- автонакат. До снятия текст, который человек из заметки УДАЛИЛ, продолжает лежать в базе;
-- это осознанный размен «обратимость против права быть забытым», и закрывать его должен
-- человек, а не миграция, доехавшая с деплоем.
--
-- Порядок снятия:
--   1. убедиться, что `audit-bodies` даёт нули по ТРЁМ кранам: «пара разошлась»,
--      «сконвертировано без страховки обратимости», «без документа». Остальные три —
--      первопроходные (неустойчивость, потеря текста/ссылки, пропало слово) — к этому моменту
--      молчат ПО ПОСТРОЕНИЮ: корпус уже сконвертирован, и его тело и есть канон. Требовать
--      «нули по всем кранам» значит повторять ту же путаницу, ради которой аудит вынесен
--      на шаг ДО выкатки (см. отчёт §14.2);
--   2. снять колонку из schema.ts и ВЫКАТИТЬ КОД — раньше наката, а не после. После починки
--      раунда 7 колонку пишет ГОРЯЧИЙ путь (создание и первая правка), и drizzle перечисляет
--      колонки поимённо: снять её в базе раньше, чем уйдёт код без неё, — значит уронить
--      создание записи у каждого пользователя. Это ровно тот отказ, ради которого накат
--      миграции 0009 стоит ДО выкатки; здесь порядок обратный по той же причине;
--   3. дописать в meta/_journal.json запись { idx, version: "7", when, tag:
--      "0012_drop_body_before_doc", breakpoints: true };
--   4. `bun scripts/ops.ts migrate`.
--
-- Снимок схемы после снятия сгенерируется штатным `drizzle-kit generate`.
ALTER TABLE "entities" DROP COLUMN IF EXISTS "body_before_doc";
