// apps/server/src/agent-loop/constants.ts
// Пороги круга исполнителя (§9.3).

/**
 * Порог брошенного прогона (С6, решение плана 1): 30 минут без единого шага.
 *
 * Щедрость намеренна — между `orbis_run_step` агент гоняет тесты (минуты), думает и
 * читает. Цена ошибки несимметрична: ложное срабатывание уводит тикет в `waiting` и
 * стоит внимания человека, пропуск стоит только ожидания. Значение — серверная
 * константа, тесты инжектируют собственный порог и `clock`.
 */
export const RUN_STALE_AFTER_MS = 30 * 60_000;
