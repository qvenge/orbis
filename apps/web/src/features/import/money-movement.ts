// apps/web/src/features/import/money-movement.ts
//
// Адреса полей, в которые ложится строка выписки, — ОДНИМ списком (§А1-1, §А8).
//
// Импорт — единственный экран, который переводит ЧУЖИЕ имена (колонки банка) в наши поля,
// и до реформы эти поля жили в нём словами: «Колонка даты», «Колонка суммы», «расход».
// Слово в коде переименование label владельцем (§А10-2 — бесплатная операция) не
// переживает: имя колонки в импорте разошлось бы с именем того же поля на карточке записи.
// Здесь лежат только АДРЕСА; сами подписи берутся из реестра по ним.
//
// Отдельным файлом, а не константами внутри `ImportFlow.tsx`, потому что читателей двое:
// форма маппинга (подписи колонок) и таблица сверки (пикер категории).

/** Дата операции — куда ложится колонка даты выписки. */
export const OCCURRED_ON = 'orbis/occurred_on';
/** Контрагент — куда ложится колонка описания. */
export const COUNTERPARTY = 'orbis/counterparty';
/** Сумма (всегда положительная: знак несёт направление). */
export const AMOUNT = 'orbis/amount';
/** Направление: варианты `income`/`expense` — из реестра, не из кода. */
export const DIRECTION = 'orbis/direction';
/** Тождество операции в выписке банка — по нему ловится повтор. */
export const BANK_TXN_ID = 'orbis/bank_txn_id';
