/**
 * Коробка тела записи — ОДНА строка классов на первый кадр и на редактор.
 *
 * Отдельный листовой файл, а не константа в одном из них: EditorShell лежит в эагерном чанке,
 * BodyEditor — в ленивом, и статический импорт между ними схлопнул бы чанк редактора обратно в
 * первый кадр (та же ловушка, о которой предупреждает scripts/check-lazy-chunks.ts).
 *
 * Значение унаследовано от прежнего просмотра тела в DetailScreen (Задача 15 его убрала): при
 * подмене первого кадра редактором текст не должен прыгать — ни на пиксель отступа, ни на
 * размер шрифта. Обе стороны подмены берут строку ОТСЮДА, поэтому разъехаться им нечем.
 */
export const BODY_BOX_CLASS = 'min-h-24 w-full rounded-lg px-2 py-1.5 text-sm leading-relaxed';

/**
 * Приглашение к вводу для ПУСТОГО тела. Близнец в `DetailScreen` умер вместе с прежним
 * просмотром тела (Задача 15) — эта копия осталась единственной.
 *
 * Именно ПУСТОГО: у тела из одного `{{query:…}}` (сидированный All Tasks) текста нет, но
 * смотреть есть на что, и «Заметки…» печатались бы над живым списком задач.
 */
export const BODY_PLACEHOLDER = 'Заметки…';
