// Correction-правила памяти на клиенте (01-arch §7.5): ОДНА строка запроса на всех
// потребителей — быстрый ввод читает правила из этого кэша (useFastPath), карточка
// предложения его же и освежает после [Запомнить] (MemoryRuleCard).
//
// ПОЧЕМУ ОТДЕЛЬНЫЙ МОДУЛЬ, А НЕ КОНСТАНТА В ХУКЕ: правила читаются из ТЁПЛОГО кэша,
// иначе карточка «⚡ без AI» ждала бы сеть перед каждым вводом (§2.5). Плата за это —
// кэш обязан кто-то освежать точечно; вторая строка запроса рядом означала бы второй
// кэш, и правило, только что подтверждённое пользователем, молча не работало бы.

/** Область денежных правил (§А8/В3): `orbis/rule_scope` — ссылка на КОНТРАКТ, не на аспект. */
export const RULES_SCOPE = 'orbis/money-movement';

/**
 * Активные правила владельца: аспект `orbis/memory`, род записи «правило». ОБЛАСТИ В
 * ЗАПРОСЕ НЕТ, и это не забывчивость.
 *
 * Отбирать надо «область money-movement ЛИБО области нет вовсе» (глобальное правило
 * применяется везде — так область описывает реестр, и так же её понимает сервер:
 * `memory/select.ts`). Грамматика §А5-3 такую дизъюнкцию не выражает — скобок в v1 нет,
 * — поэтому запрос берёт ВСЕ правила, а область отсекает предикат `ruleAppliesTo` при
 * сборке контекста быстрого ввода. Строгий `orbis/rule_scope=…` в запросе давал бы
 * ДРУГОЙ ответ, чем сервер, на одних и тех же данных: глобальное правило работало бы в
 * импорте и не работало в быстром вводе.
 *
 * Имена свойств — namespaced key реестра (§А5-3а): голых `kind`/`scope` в реестре нет вовсе,
 * их место заняли `orbis/memory_kind` и `orbis/rule_scope` (В7 инвентаря).
 */
export const MEMORY_RULES_QUERY = {
  query: 'aspect=orbis/memory, orbis/memory_kind=rule',
} as const;

/** Правила меняются редко (эскалация §7.8) — держим их в кэше 5 минут, а не 30 секунд. */
export const MEMORY_RULES_STALE_TIME = 5 * 60_000;
