# Концепция реформы: свойства, аспекты, контракты, роли — что мы делаем и зачем

| Поле | Значение |
|---|---|
| Дата | 2026-08-25 |
| Назначение | Самодостаточная формулировка концепции для независимой проверки НАПРАВЛЕНИЯ: агент в новой сессии читает этот документ, затем нормативы (§5), и отвечает прежде всего на вопрос «верна ли идея в самой сути и в масштабе» (§6); техническая проверка согласованности — вторым блоком (§7) |
| Статус | Решения владельцем ПРИНЯТЫ (не черновик концепции — черновиком остаётся её проверка); спека ещё не написана — находки проверки лягут в неё |
| Источник | Диалоги владельца 2026-08-22…25; стартовый вопрос — «в Notion можно точечно настраивать свойства тип-значение; можем ли мы так?» |

---

## §1. Зачем: мотивация в три шага

**Шаг 1 — исходная боль.** Механизм аспектов негибок: 13 встроенных аспектов зашиты в код, пользователь не может ни завести своё свойство, ни изменить встроенное, ни добавить вариант статуса. Владелец хочет «максимально гибко настраивать систему под себя и не навязывать своё видение — только предложить, с возможностью менять/удалять/добавлять».

**Шаг 2 — почему это вопрос ядра, а не UI.** Разведка (568 мест в пяти зонах кода) показала: жёсткость сидит не в отсутствии экранов настройки, а в модели данных. Значение существует только внутри аспекта (`aspects['orbis/task'].status`) — у факта нет собственного тождества; смысл («это задача», «это завершено», «это ребро — привязка к конверту») вычисляется кодом из формы данных, а не записан в данных; функции (Agenda, Budget, чекбокс) привязаны к конкретным id аспектов и значениям enum, поэтому пользовательский аспект мёртв для всех функций.

**Шаг 3 — критерий North Star.** «Помогает ли это Orbis брать на себя больше — так, чтобы человек мог спокойно это отдать?» (05-mission §4). Главный автор настроек — не человек, а AI («правая рука»): структура приходит ПОСЛЕ данных (ретро-миграция §3.10 уже признаёт это), AI постоянно предлагает новые типы, свойства, правила. Декларацию — в отличие от кода — можно провалидировать, показать диффом, принять пачкой, откатить undo. Поэтому цель реформы: **перевести из кода в данные всё, что отвечает на вопросы «что существует, что это значит и что должно выполняться», оставив кодом только «чем это исполняется»**. Каждая ступень декларативности расширяет зону безопасной автономии AI.

Рулинги, задающие рамку: пользователей и прод-данных нет (базу можно пересевать; обратная совместимость бесплатна); цена работ не ограничение («надо переделать ядро — переделаем»); гибкость — возможность, не обязанность (анти-цель 5: умолчания работают сами, настройка — словами через AI).

## §2. Сдвиг модели: свойство — факт, аспект — интерпретация («путь С»)

Было: сущность = карта «аспект → объект полей»; JSON Schema аспекта — истина; поле не существует вне аспекта. Станет:

```
сущность = core (title, body, tags) + props {property-id → значение} + aspects [id]
```

- **Свойство** — самостоятельный факт с тождеством: реестр `id` (неизменяем; встроенные — читаемые строки `orbis/task_status`, пользовательские — uuid) + `key` (ASCII-слаг, изменяем без следа в данных) + `label`/`description` (per-locale, свободно) + `type` (закрытый словарь kind'ов). Одно свойство — одно значение на сущности. Суффикс `_ref` упразднён (тип `ref` сам говорит).
- **Аспект** — интерпретация: «что эта сущность есть» — набор ссылок на свойства с обязательностью + ai_instructions + tag_mappings. JSON Schema — генерируемая производная (для валидатора и тулов LLM). Слово «аспект» сохранено (рулинг В1).
- **`meta` удаляется**: извлечённый AI факт — свободное свойство (существующее или `proposed` с капом), а не write-only мешок.
- **Сохранённые запросы — AST по ссылкам (id), текст грамматики — проекция** (парсер принимает key и label, сериализатор печатает key машинам / label людям). Фильтр канонически — булево дерево and/or/not (текст — плоский сахар). Прецедент внутри системы: сущности в body уже адресуются `[[entity:uuid]]`, не заголовком.
- Прецеденты рынка, подтверждающие семантику: Tana (supertags + глобальные поля, одно значение на узел), Anytype, Obsidian; Notion как контрпример (свойство заперто в базе — у Orbis баз нет, сущность мультиаспектна).

## §3. Понятия поверх сдвига

| Понятие | Отвечает на вопрос | Где живёт |
|---|---|---|
| Свойство | «что это за значение?» | таблица `property_definitions` |
| Аспект | «что эта сущность есть?» | `aspect_definitions` (состав меняется); пользовательские дельты поверх встроенных |
| Контракт | «что код умеет с этим делать?» — слоты + классы значений + именованные наборы (`closed = [done, cancelled]`) | таблица `contract_definitions`; встроенный контракт = API модуля, неизменяем |
| Привязка | «чем аспект реализует контракт?» — `bind` (слот→свойство) + `value_map` (вариант→класс) | jsonb-поле `implements` в аспекте; главный пользовательский жест |
| Роль связи | «что значит это ребро?» — с generic-ограничениями (`target_max_incoming`, `acyclic`, контракты концов, `created_by`, именование сторон) | таблица `relation_role_definitions`; на ребре — колонка `role`; `relation_type` УДАЛЯЕТСЯ (роль — единственная истина) |
| Правило | «что должно выполняться?» — каталог ~10 шаблонов (`requires_when`, `on_enter_class`, `default`, `unique_among`, `nearest_ancestor`, агрегаты, материализация…) | jsonb-поля в строке владельца (свойства/аспекта/роли) — НЕ таблица (правило умирает с владельцем) |
| Подписка | «что эта поверхность показывает/считает?» — consumer-сторона контракта | таблица `subscription_definitions`; Budget/Agenda/строки/excludeBlocked — сиды модулей |
| Действие | «что можно сделать?» — макрос: параметры (типизированы контрактами), precondition (CAS), шаги — ТОЛЬКО вызовы существующих тулов исполнителя с выражениями, класс подтверждения, `offered_by` (где предлагается) | таблица `action_definitions`; исполнение — атомарный batch с журналом и undo (механика в проде: batch_execute, отложенные действия D42, Т6 «одно действие, один batch, один undo»). НЕ программа: без циклов и ветвлений (условия — только в precondition/`offered_by.when`); длинное и умное — рутинам |
| Модуль | «что включено у пользователя?» — словарь + код-остаток + поверхности, вкл/выкл целиком | манифест в коде + `user_settings` |

Ключевая симметрия: у сущности «форма» (props) и «смысл» (аспекты); у ребра «форма» (source/target) и «смысл» (роль); у контракта две стороны — привязка (реализация) и подписка (потребление), обе — данные, друг о друге не знают, ссылаются на стабильную середину. Замыкание: свойство — факт; правило — «что должно выполняться»; действие — «что можно сделать»; подписка — «где показывается и предлагается». AI заводит любое из них так же безопасно, как заметку: декларация, дифф, пачка решений, undo.

## §4. Принципы (по ним проверять каждое решение)

1. **Одна истина, остальное — проекции.** Значение — по id; схема, wire, текст запроса, промпт, подписи — генерируются. Переименование ничего не переписывает в данных; слияние — единственная операция реестра, трогающая данные.
2. **Три слоя**: A — данные (правятся через исполнителя: журнал, дифф, undo); B — ядро-интерпретаторы и машинерия (код навсегда); C — именованный остаток кода с записью «почему» (метрика сжатия — 568 мест инвентаря). Sandbox-плагинов нет; негорячий остаток — внутреннему исполнителю (AI-рутины).
3. **Дисциплина против inner-platform**: шаблон/kind попадает в каталог при ≥2 повторах формы; выражения тотальные; ноль кейсов — ноль механизма.
4. **Модули не знают друг о друге**: общее для двоих — контракт ядра; присутствие на чужой поверхности — только подпиской; механизма «модуль требует модуль» нет.
5. **Не навязывать = всё объяснимо и всё заменяемо**: у ограничений — честные зависимости («статус использует Agenda, чекбокс…») вместо серых кнопок; удаление встроенного = скрытие/выключение модуля; смена типа = форк.
6. **Fail-closed и наблюдаемость**: невалидное — незаписываемо (а не «записано и молча мёртво»); каждое изменение реестра — действие исполнителя.
7. **Словарь требует садовника**: разрастание свойств — постоянная функция (namespace'ы, слияние, AI-предложения, отчёт о сиротах) — обратная сторона «одного графа».

## §5. Карта нормативных документов (что читать проверяющему)

1. **`specs/2026-08-23-properties-model-notes.md`** — ГЛАВНЫЙ: Р1–Р20 (модель) + §7 Ч1–Ч11 (модули, дельты, контракты, три слоя, подписки, Budget-разбор, транзитивный обход, закрывающий пакет по связям, действия-декларации) + решённые В1–В4 в §5.
2. **`specs/2026-08-23-property-ref-type-notes.md`** — тип `ref` (цель — запрос), с шапкой-поправками.
3. **`reviews/2026-08-23-aspect-coupling-inventory.md`** §1–§7 — инвентарь 568 мест, механизмы M1–M14, словарь контрактов R1–R32, решения В1–В12 (все закрыты; В11 — namespaced key снимает коллизию reserved-слов, В12 — булево дерево). Приложения Z1–Z5 — по мере надобности, там точные file:line.
4. Контекст (не норматив): `docs/prd/05-mission.md` (North Star, анти-цели), `docs/prd/01-architecture.md` (текущая архитектура, §10 правила аспектов, §12 ограничения), `reviews/2026-08-25-work-map-roles-alignment.md` — параллельная сверка с картой работ (если уже существует).

## §6. Главное задание: проверка направления

Прежде любых деталей ответь на три вопроса — с позицией, а не обзором. Мандат: **атаковать идею**, а не полировать; вежливое согласие — провал ревью.

**1. Верна ли ставка.** Центральный тезис реформы: путь к North Star («правая рука, которая ведёт дела») лежит через перевод структуры системы в данные, потому что главный автор структуры — AI, а декларацию — в отличие от кода — можно провалидировать, показать диффом, принять пачкой и откатить. Проверь тезис против миссии и анти-целей (05-mission), против модели монетизации D36 (констелляция приложений над одним графом) и против того, что ты знаешь о рынке и о возможностях моделей. Отдельно: не рационализация ли это красивой архитектуры задним числом?

**2. Стилмен отвергнутых направлений.** Сыграй в полную силу за каждое:
- (а) остаться аспект-центричными и наращивать гибкость точечно (дельты без смены хранения);
- (б) вообще без схем: LLM понимает сырой текст, структура — атавизм до-LLM эпохи; факты живут прозой, «понимание» — в модели, не в реестре;
- (в) AI пишет КОД (модули/плагины), а не декларации: кодогенерация зрела, песочницы существуют, зачем свой интерпретируемый мета-язык с его inner-platform-риском;
- (г) взять готовую мета-платформу/движок вместо собственного механизма.
Для каждого: при каких условиях оно бьёт выбранный путь; наблюдаемы ли эти условия сейчас; что из него стоит украсть, не меняя направления.

**3. Что фальсифицирует направление.** Сформулируй 3–5 проверяемых предсказаний, при провале которых реформу надо останавливать или резать (примеры класса: «AI по словам владельца надёжно пишет привязки/подписки/действия», «декларативные агрегаты держат производительность Budget», «словарь при живом использовании не зарастает несливаемыми дублями», «генерируемый промпт не деградирует качество модели против рукописного»). Для каждого — как проверить ПРОБОЙ до спеки или в первом срезе, и какой результат считается провалом.

Вердикт §6 — отдельным разделом в начале отчёта: «направление верно / верно с оговорками (какими) / есть более сильная альтернатива (какая)».

## §7. Второе задание: техническая проверка согласованности

После вердикта по направлению — дыры, противоречия и нестыковки, не пересматривая принятые решения (Р/Ч/В — норматив; находка «решение Х плохое» допустима только как доказанное противоречие другому решению или принципу §4). Классы:

1. **Внутренние противоречия**: Р↔Ч↔В-решения разных дней (ранние Р писались до Ч7–Ч11; шапки-поправки могли покрыть не всё).
2. **Неразобранные взаимодействия**: дельты × локали × подписки; `proposed`-свойства × генерация тулов; слияние свойств × AST запросов × undo; выключение модуля × его роли на рёбрах × его контракты в `value_map` × его действия в `offered_by`; дельта × обновление системного определения × drift; действия × политика подтверждений × отложенная пачка D42.
3. **Недоопределённости**: для каждого понятия §3 — «кто создаёт / правит / удаляет / что со ссылками при удалении»; жизненный цикл `proposed`; порядок детей в иерархии; конкурентность правок реестра; судьба `rel_uniq(source,target,relation_type)` при удалении колонки.
4. **Производительность и конкурентность**: агрегаты-декларации на каждом чтении Budget; пересчёт `nearest_ancestor` на большом поддереве; кеш эффективного реестра и инвалидация; RLS новых таблиц.
5. **Безопасность и полномочия**: может ли act-рутина править реестр и что её ограничивает; `created_by: system`; подписка/действие как вектор «тихо перенастроить, что видит и делает владелец» (анти-цель 3).
6. **AI-поверхность**: бюджет токенов промпта при сотнях свойств; как модель узнаёт классы контрактов в `attach_*`; как строит булево дерево в `entity_query`; как выбирает действие из `action_definitions`.
7. **Согласованность с PRD**: какие параграфы 00–03 становятся ложью и не названы в Р20; инварианты §13 PRD 01.
8. **Пропущенное**: чего в документах нет вовсе, а спека без этого не пишется (известные кандидаты: UX реестра для человека; ошибки валидации привязок; версирование деклараций; экспорт при per-locale). Найди своё.

Формат отчёта: (1) вердикт по направлению (§6); (2) таблица находок «№ | документ:место | класс | суть | предложение» по убыванию важности; (3) итог «готовы ли заметки быть входом спеки и что обязательно закрыть до неё».
