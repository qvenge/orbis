# PRD v3 Documentation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Написать 4 документа PRD v3 в `docs/prd/` строго по дизайн-спеке `docs/superpowers/specs/2026-06-12-orbis-prd-v3-design.md`.

**Architecture:** 4 документа по слоям (product / architecture / core-os / budget) с правилом «каждый факт живёт ровно в одном документе». Порядок написания: 01 → 00 → 02 → 03 (фундамент раньше потребителей). Финальный таск — кросс-ревью комплекта.

**Tech Stack:** Markdown. Язык документов — русский (термины-идентификаторы: английский, как в спеке). Никакого кода реализации — это продуктово-архитектурная документация.

**ВАЖНО — без коммитов:** владелец проекта просил пока ничего не коммитить. Ни один шаг плана не делает `git commit`. Коммит — только по явной просьбе владельца после ревью.

**Источник истины:** дизайн-спека `docs/superpowers/specs/2026-06-12-orbis-prd-v3-design.md` (далее — СПЕКА). При любом расхождении между этим планом и СПЕКОЙ побеждает СПЕКА. Старые документы в `docs/*.md` — только архивный референс для грамматики query-языка и схем полей; новые документы НЕ ссылаются на них.

## Карта единственных источников (что где живёт)

| Факт | Живёт ТОЛЬКО в | Остальные |
|---|---|---|
| Схемы аспектов (поля, типы) | 01-architecture §3 | ссылаются |
| Схемы таблиц БД | 01-architecture §4 | ссылаются |
| Sync-протокол, матрица конфликтов | 01-architecture §5 | ссылаются |
| Грамматика query-движка | 01-architecture §6 | ссылаются |
| Модель AI-контекста, fast-path, undo-журнал | 01-architecture §7 | ссылаются |
| Правила проектирования аспектов (3 шт.) | 01-architecture §10 | ссылаются |
| Боли, метрики успеха, роадмап, Future Directions | 00-product | ссылаются |
| Глоссарий терминов | 00-product (приложение) | ссылаются |
| Экраны и UX ядра (Chat/Browser/Agenda), навигация | 02-core-os | ссылаются |
| Экраны Budget, флоу импорта, rollover | 03-budget | ссылаются |
| Envelope-модель данных (категория→конверт→транзакция) | 03-budget §2 (ссылаясь на схемы из 01) | — |

---

### Task 1: `docs/prd/01-architecture.md`

**Files:**
- Create: `docs/prd/01-architecture.md`
- Read (входные данные): `docs/superpowers/specs/2026-06-12-orbis-prd-v3-design.md` (§5, §6, §7, §9, §11), `docs/orbis-data-model-v3.md` (только §3.2 «Extended Query Syntax» — как референс грамматики), `docs/orbis-db-schema.md` (только §2 — как референс типов столбцов)

- [ ] **Step 1: Написать шапку и §1 «Обзор и принципы»**

Содержание: таблица метаданных (версия 3.0, дата, статус Draft, родитель: 00-product.md — прямая ссылка); таблица стека «слой → технология» без развёрнутых обоснований (Runtime: Bun + TypeScript; API: tRPC; ORM: Drizzle; БД: PostgreSQL (Supabase) + RLS; Auth: Supabase Auth; Frontend: Vite + React PWA; State: Zustand; Local DB: IndexedDB; AI: LLM-agnostic слой, Claude по умолчанию; Агенты: MCP); абзац-граница «PRD фиксирует контракты и поведение, не реализацию — детали кода живут в implementation-планах фаз»; три сквозных принципа с формулировками из СПЕКИ: (1) «данные существуют до views», (2) «единый query-движок» (шесть потребителей перечислить: body-блоки, smart lists, фильтры views, прогресс целей (future), авто-чекины (future), AI-тул entity_query), (3) правила слоёв (meta — только AI-извлечённое, body — текст и живые запросы, aspects — всё типизированное).

- [ ] **Step 2: Написать §2 «Модель данных: сущность»**

Содержание из СПЕКИ §5.1: 4 слоя с ASCII-диаграммой (Core: id UUIDv7 client-generated, title, emoji, timestamps, archived / Body: markdown + `[[entity:uuid|text]]` + `{{query:...}}`, body_refs[] / Tags+Meta / Aspects); таблица «что является сущностью vs что нет» (сущности: задачи, расходы, заметки, проекты, конверты, категории, memory-факты, smart lists, recurring-шаблоны и инстансы, чекины; не сущности: user_settings, aspect_definitions, история чатов, метеринг); правило идентичности («сущность — то, на что пользователь ссылается как на вещь с собственной судьбой»).

- [ ] **Step 3: Написать §3 «Аспекты MVP» — 7 полных схем**

Для каждого аспекта — таблица полей (имя, тип, required, описание) строго по СПЕКЕ §5.2: orbis/schedule (start_at*, end_at, duration_min, all_day, recurrence, location, timezone), orbis/task (status*: inbox|planned|in_progress|waiting|done|cancelled, priority, due_date, completed_at, effort_min, waiting_for), orbis/financial (amount*, currency, direction*: income|expense, category_ref* — UUID категории-сущности, planned: bool, recurring, payment_method, counterparty), orbis/note (content_type, pinned), orbis/budget (category_ref*, limit*, currency, period_start*, period_end*, carryover; явный абзац: «spent не хранится — вычисляется из транзакций, привязанных relation parent к конверту; единственный источник истины»), orbis/category (icon, color, aliases[] с примером рус+англ), orbis/memory (kind: fact|rule, scope: опциональный aspect-id). Плюс tag_mappings для каждого. Плюс таблица реестра: id, default-статус (все active). Плюс подраздел «Ретроактивная миграция» — механизм подключения будущих аспектов: поиск сущностей по tag_mappings → маппинг meta-ключей на поля аспекта (имена meta-ключей обязаны совпадать с именами полей аспектов — правило для AI-инструкций) → валидация по схеме → attach; невалидные — флаг на ручной разбор с указанием, где пользователь их увидит (Browser, фильтр «требуют разбора»).

- [ ] **Step 4: Написать §4 «Таблицы БД» — 8 таблиц**

По СПЕКЕ §5.3: entities (столбцы как в v2.2 — перечислить полностью), relations (+ updated_at, + deleted_at tombstone; типы: parent, blocks, related_to, derived_from; constraint no-self-relation; unique (source,target,type)), aspect_definitions (+ description, + icon), user_settings (+ plan text default 'dev'; перечислить остальные поля: timezone, defaultCurrency, weekStartDay, tagColors, installedViews, pinnedEntities, statusStripMetrics — удалить за ненадобностью? НЕТ: status strip отложен → поля statusStripMetrics в v3 НЕТ; viewPreferences), chat_threads (id, user_id, entity_id NULL=глобальный, title, archived, created_at, updated_at), chat_messages (id, thread_id, role, content, metadata JSONB для actions/cards, created_at; append-only — без updated_at), ai_usage (user_id, date, input_tokens, output_tokens, request_count, model), sync_log (как v2.2). Индексы: перенести GIN-набор из старой схемы (tags, aspects, meta, body_refs, full-text title+body) + b-tree sync-индексы + индекс relations по (target_id, relation_type) и (source_id, relation_type). RLS: одна фраза-принцип + политика-шаблон (user_id = auth.uid()) — без копирования всех политик. Абзац «реестр тегов — вычислимый, таблицы нет».

- [ ] **Step 5: Написать §5 «Sync-протокол»**

По СПЕКЕ §5.4: модель (IndexedDB источник истины на клиенте, PostgreSQL — в облаке, push/pull по курсорам); полная матрица конфликтов по полям (title/emoji/archived: LWW; tags: LWW всего массива — с обоснованием отказа от union; meta: key-level LWW; aspects: aspect-level LWW; body: LWW + конфликтная копия — archived-снапшот с пометкой, описать как сущность с тегом #conflict-copy и relation related_to к оригиналу); tombstones для relations (deleted_at, sync по updated_at); детерминированные ID порождаемых сущностей uuid5(template_id + date) с перечислением применений (recurring-инстансы, чекины); sync чатов (threads — LWW, messages — append-only merge по id, конфликтов нет).

- [ ] **Step 6: Написать §6 «Query-движок»**

Грамматика — полная таблица синтаксиса (перенести из старого data-model §3.2 как референс, адаптировав: tags=a|b, excludeTags, aspect=, поле=значение|значение, отрицание !, date-relative due=today|overdue|next_7d, числовые > и диапазоны .., parent_of=this/child_of=uuid, excludeBlocked, sortBy multi-field, search=, limit=, display=, title=). Два бэкенда: SQL (сервер) и JS поверх IndexedDB (клиент) — требование эквивалентности результатов, тест-сьют на парность (фраза о приёмке: один набор фикстур, оба бэкенда дают одинаковый результат). Шесть потребителей (таблица). Поведение при невалидном запросе: ошибка парсинга отображается, пустой результат ≠ ошибка.

- [ ] **Step 7: Написать §7 «AI-слой»**

По СПЕКЕ §6: пятислойная модель контекста (диаграмма + таблица с бюджетом токенов на слой: system ~800, память ≤50 фактов ~1-2K, рабочий контекст ~500, история треда ~4K rolling+summary, тулы ~1.5K); принцип «диалоги эфемерны, знания живут в графе» + три пути переноса (memory-факт по подтверждению, дозапись в body якоря, note-сущность с relation); треды (глобальный с entity_id=NULL — мессенджер-модель, тред сущности — вкладка detail-экрана; тулы не скоупятся); память (orbis/memory, видимость/редактируемость, инжекция активных, кап); fast-path — отдельный подраздел: клиентский детерминированный парсер, таблица паттернов MVP («<текст> <число>» → expense с резолвом категории по aliases, «+<число> <текст>» → income, «<текст> <число> <валюта>»), правило передачи в LLM при неуверенности (неизвестная категория, множественные суммы, вопросительная форма), применение correction-правил из памяти, работа офлайн; динамические тулы из реестра (core-набор: entity_create/update/search, relation_create/delete, user_query + attach_<aspect> на активный аспект; tiered loading); роутинг моделей (дешёвая: нормализация/категоризация; основная: создание/cross-aspect; имена моделей НЕ фиксировать — конфиг); corrections & undo: журнал действий (таблица action_log не нужна — обратные операции хранятся в chat_messages.metadata.actions; описать формат: type, entity_id, inverse: {op, payload}), inline-правка карточек, эскалация в правило после 2 одинаковых исправлений; деградация (таблица: офлайн/AI-down → что работает).

- [ ] **Step 8: Написать §8 «Entitlements & metering» и §9 «API и внешние агенты»**

§8 по СПЕКЕ §7: резолвер (один модуль, строковые ключи фич — таблица стартовых ключей: ai.requests_per_day, ai.tokens_per_day, aspects.custom_max, import.csv, history.depth_days, agents.requests_per_day; сейчас всё разрешено), user_settings.plan, ai_usage, rate limits через резолвер, явный список «НЕ делаем» (платёжка, состояния подписки, тарифы, paywall). §9 по СПЕКЕ §6.8, три подраздела: (а) Внутренний API — tRPC-роутеры списком с назначением (entity, relation, aspect, user, sync, ai, chat — треды/сообщения; отдельного роутера категорий нет — категории суть сущности), принципы (Zod-схемы в shared, protectedProcedure, оптимистичные мутации, идемпотентность создания по client-generated id), точные сигнатуры процедур НЕ фиксируются — контракт живёт в коде (end-to-end типы); (б) Реестр тулов — единый публичный контракт: таблица тулов с input-схемами (entity_query, entity_get — c body/relations/backlinks/тредом, entity_create, entity_update, relation_create, relation_delete, user_query + динамические attach_<aspect> из реестра) — это ЕДИНСТВЕННОЕ место спецификации API как контракта, два потребителя: внутренний чат-LLM и MCP; (в) MCP-сервер — тонкий адаптер над тем же tool-executor'ом; аутентификация PAT-токеном (отдельно от Supabase JWT; MVP: один токен полного доступа, генерация CLI/env, без UI), действия агентов пишутся в общий журнал действий (Undo работает), сквозной сценарий «найди задачу X и выполни» по шагам (entity_query → entity_get → entity_update).

- [ ] **Step 9: Написать §10 «Правила проектирования аспектов», §11 «Проверка масштабируемости», §12 «Известные ограничения»**

§10: три правила из СПЕКИ §5.6, каждое с одним абзацем обоснования и примером нарушения из v2.2 (check_ins[] в аспекте привычки). §11 — приложение по СПЕКЕ §9: три домена (фитнес: дерево программа→дни→derived_from→факт; привычки: чекины-дети + детерминированные ID + авто-чекин auto_source: {query}; цели: progress_source: {query, aggregate: sum|count|latest}) с выводом «ядро не меняется — домен = записи в реестре + view». §12 по СПЕКЕ §11: JSONB-аналитика, LWW для одного пользователя, full-text без embeddings, max внутрь массивов вне MVP-механизма целей.

- [ ] **Step 10: Верификация Task 1**

Чек-лист: (а) все 7 аспектов имеют полные таблицы полей и совпадают со СПЕКОЙ §5.2 дословно по именам полей; (б) все 8 таблиц описаны, statusStripMetrics отсутствует, plan присутствует; (в) матрица конфликтов покрывает: title, emoji, archived, tags, meta, aspects, body, relations, threads, messages; (г) query-грамматика включает все строки таблицы синтаксиса; (д) поиск по файлу: нет «TBD», «TODO», «позже»; (е) нет ссылок на docs/orbis-*.md; (ж) термины category_ref/planned/deleted_at написаны единообразно; (з) реестр тулов в §9 согласован с тулами AI-слоя §7 (одни и те же имена и набор) и покрывает сценарий «найди задачу и выполни».

---

### Task 2: `docs/prd/00-product.md`

**Files:**
- Create: `docs/prd/00-product.md`
- Read: СПЕКА (§2, §4, §10), `docs/prd/01-architecture.md` (для корректных ссылок)

- [ ] **Step 1: Написать §1 «Vision» и §2 «Боли и ответы»**

§1: формулировка «персональная AI-операционная система для жизни»; главный тезис дословно из СПЕКИ: «Orbis продаёт не трекинг, а связность — ответы на вопросы, требующие данных из нескольких доменов сразу»; платформенная аналогия — короткая таблица (граф=ФС, чат=терминал, browser=файл-менеджер, views=приложения) без marketplace-строк. §2: пять пар боль→ответ из СПЕКИ §4 с примером «купить кроссовки» (одна сущность, три аспекта).

- [ ] **Step 2: Написать §3 «Пользователь и цель итерации», §4 «Скоуп MVP»**

§3: личный MVP (dogfooding, один пользователь), multi-user-ready схема, freemium-задел (отсылка к 01 §8 Entitlements). §4: таблица «в скоупе» (Core OS: Chat+Browser+Agenda-lite; Budget view; 7 аспектов; offline-first; CSV-импорт; **MCP-сервер для внешних агентов — минимальный, один PAT**) / «вне скоупа» (полный календарь, fitness/nutrition/habits/goals views, voice, marketplace, native, push-уведомления, multi-currency, биллинг, UI управления PAT-токенами и скоупы агентов) — каждая строка «вне скоупа» с пометкой, какой механизм её поддержит в будущем (ссылки на 01 §11).

- [ ] **Step 3: Написать §5 «Принципы ввода» и §6 «UX-принципы»**

§5: иерархия ввода: fast-path (мгновенно, офлайн, без LLM) → LLM-чат (сложное, cross-aspect) → quick-add в views → CSV-импорт; пользователь не выбирает режим. §6, три подраздела: (а) Навигация — нижняя панель Chat/Browser/Agenda/Budget, views — экраны (не модалки), detail — push, back-стек предсказуем, deep links на сущности/треды, эволюция Budget→«Views» при 3+ views; (б) Corrections & Undo — продуктовое обещание: «каждое действие AI обратимо и правится в одно касание; система учится на исправлениях» (механика — ссылка на 01 §7); (в) Notification policy — в MVP НЕТ push-уведомлений и проактивных сообщений; вся сигнализация — бейджи на вкладках и секции в экранах (просроченное в Agenda, перерасход в Budget); proactive AI — Future с явной политикой (digest, quiet hours) при появлении.

- [ ] **Step 4: Написать §7 «День пользователя»**

Сценарий из СПЕКИ-обсуждения, 6 моментов: утро (Daily Planning), день (fast-path «обед 340» + карточка с inline-правкой), вечер (вопрос «что по бюджету?»), неделя (CSV-импорт с ревью), месяц (rollover одним экраном), полгода (включение fitness → ретроактивная миграция из tags+meta — «wow-момент»).

- [ ] **Step 5: Написать §8 «Метрики», §9 «Роадмап», §10 «Future Directions», §11 «Риски», приложение «Глоссарий»**

§8: пять метрик дословно из СПЕКИ §4. §9: три фазы — Фаза 1 «Ядро» (схема БД, sync, query-движок, Chat с fast-path и тредами, Browser, Agenda-lite, онбординг-сидирование), Фаза 2 «Budget + агенты» (категории, конверты, 5 экранов, CSV-импорт, rollover, planned; MCP-сервер с PAT — тонкий адаптер над готовым реестром тулов), Фаза 3 «Полировка» (надёжность sync + конфликтные копии в UI, query-builder, память/undo-UX, перф); без календарных оценок. §10: список из СПЕКИ §10 одним блоком. §11: 5–6 рисков с митигациями (точность AI-категоризации → fast-path+правила+метрика исправлений; смерть ручного ввода → импорт CSV; сложность offline-first → фиксированная матрица конфликтов + парный тест-сьют движка; перегрузка чата → треды; scope creep → правило «новый домен = реестр+view, без изменений ядра»). Глоссарий: entity, aspect, view, конверт, категория, query-движок, fast-path, memory-факт, тред, pinned-сущность, конфликтная копия, ретроактивная миграция.

- [ ] **Step 6: Верификация Task 2**

Чек-лист: (а) все 5 метрик совпадают со СПЕКОЙ; (б) нет дублирования схем/таблиц из 01 (только ссылки); (в) Future Directions покрывает весь список СПЕКИ §10; (г) notification policy не противоречит отсутствию push в скоупе; (д) нет «TBD/TODO»; (е) каждый термин глоссария реально используется в комплекте.

---

### Task 3: `docs/prd/02-core-os.md`

**Files:**
- Create: `docs/prd/02-core-os.md`
- Read: СПЕКА (§6, §8.1–8.4, §8.6), `docs/prd/01-architecture.md`, `docs/prd/00-product.md`

- [ ] **Step 1: Написать §1 «Навигация» (детализация)**

Карта экранов: 4 вкладки; стек поверх вкладки (detail → detail → ...); правила back (закрывает верхний push; на корне вкладки — переключение на предыдущую вкладку запрещено, системный exit); deep links: orbis://entity/<id>, orbis://thread/<id>, orbis://budget/category/<id>; восстановление состояния после перезапуска PWA (вкладка + стек).

- [ ] **Step 2: Написать §2 «Chat»**

Глобальный тред: мессенджер-UI, rolling-история, summary-сжатие (поведенчески; механика в 01 §7). Тред сущности: вкладка на detail, заголовок = title сущности, контекст = якорная сущность. Карточки действий — таблица типов MVP: entity_card (создано/обновлено: title, аспект-иконки, inline-правка полей, Undo), entity_list, query_result (агрегация: сумма/остаток), import_review (ссылка на 03), error_card. Suggestion chips: 2–4, генерируются AI. Fast-path UX: карточка появляется мгновенно, пометка «⚡ без AI»; тап «не то» → передать строку в LLM-путь. Очередь офлайн: видимый блок «ждут сети: N», по тапу — список, можно отменить. UI памяти: экран в настройках = отфильтрованный Browser по orbis/memory + объяснение «что AI помнит».

- [ ] **Step 3: Написать §3 «Entity Browser»**

Сайдбар: pinned-сущности с бейджами (число результатов первого query-блока), drag-перестановка, «закрепить» из detail. Преднастроенные: Daily Planning / Upcoming / All Tasks — привести полные body с query-блоками (взять тексты запросов из старого entity-browser-дока §4, адаптировав синтаксис к грамматике 01 §6; excludeBlocked в MVP-грамматике — да). Query-builder: рендер блока виджетом (заголовок, список, кнопка «настроить»), форма-редактор полей грамматики, сериализация обратно в строку, ошибки парсинга — красная плашка с текстом, не пустота. Detail-экран: порядок секций (title+emoji → теги → body → карточки аспектов → subtasks → relations/блокировки → backlinks → тред); native-рендеринг task (чекбокс с цветом приоритета, статус-бейдж, lock при блокировке, счётчик детей) и schedule (дата/время, recurrence-иконка); generic-карточка для остальных аспектов; «+аспект». Quick-capture бар: контекстное поведение (в pinned-контексте → task со статусом из контекста; в проекте → child). Фильтры: теги/аспекты/статус/приоритет/даты, «Сохранить как smart list» → создаёт сущность с query-блоком + пин. Служебные сущности: чекины и conflict-copy исключены из списков по умолчанию (фильтр-переключатель «показать служебные»).

- [ ] **Step 4: Написать §4 «Agenda-lite»**

Один экран: секция «Просроченное» (красная, сверху), затем дни (Сегодня → +7); строка = время start_at + title + аспект-иконки; all_day — сверху дня; recurring-инстансы показываются, шаблоны — нет; тап → detail; пустое состояние («день свободен»); без сетки/драга — явная отсылка «полный календарь — Future (00 §10)».

- [ ] **Step 5: Написать §5 «AI-сценарии ядра», §6 «Edge cases», §7 «Онбординг»**

§5: 6–8 примеров с ожидаемым поведением: «купи продукты» (inbox), «закончить API к пятнице» (planned+due), «обсудим план питания» из detail-треда (скоуп), «запомни: не ем свинину» (memory-факт), «отмени последнее» (undo из журнала), «что я сделал на неделе?» (query). §6 таблица: завершение родителя с активными детьми (AI спрашивает), циклическая блокировка (отказ с путём цикла), блокированная задача с due сегодня (скрыта в Today, видна в Upcoming), правка инстанса recurring (detach изменённых полей), конфликтная копия body (появляется рядом, бейдж), сущность без аспектов (валидна — заметка/идея). §7: сидирование из СПЕКИ §8.6 (12 категорий с alias'ами — полный список: Еда, Транспорт, Жильё, Здоровье, Подписки, Развлечения, Одежда, Образование, Путешествия, Подарки, Зарплата, Фриланс; 3 pinned-сущности; настройки: timezone Europe/Moscow, currency RUB, weekStartDay monday, plan 'dev').

- [ ] **Step 6: Верификация Task 3**

Чек-лист: (а) body преднастроенных сущностей синтаксически валидны против грамматики 01 §6; (б) типы карточек чата согласованы с форматом ответа в 01 §7; (в) поведение чекинов/conflict-copy согласовано с 01 (§3 memory/служебные, §5 конфликтные копии); (г) категории онбординга совпадут с теми, что будут в 03 (единый список — источник здесь, 03 ссылается); (д) нет «TBD/TODO»; (е) нет дублирования грамматики/схем из 01.

---

### Task 4: `docs/prd/03-budget.md`

**Files:**
- Create: `docs/prd/03-budget.md`
- Read: СПЕКА (§8.5), `docs/prd/01-architecture.md` (§3 financial/budget/category), `docs/prd/02-core-os.md` (§7 список категорий)

- [ ] **Step 1: Написать §1 «Обзор и философия» и §2 «Модель»**

§1: envelope-бюджетирование с AI-ассистом (AI предлагает, пользователь подтверждает); что Budget даёт сверх Browser (таблица: прогресс конвертов, charts, carryover, прогноз recurring+planned). §2: дерево «категория (вне времени) → parent → конверты по периодам; транзакции: financial.category_ref + relation parent к конверту» с ASCII-диаграммой; правило spent (только из relation-привязанных транзакций; авто-привязка при создании: категория+период → конверт); carryover-логика (профицит/дефицит → effective_limit следующего месяца — формулы); planned-расходы (не уменьшают spent; отдельная секция прогноза; завершение task → AI предлагает превратить в факт); recurring (template+derived_from, детерминированные ID — ссылка на 01 §5); произвольные периоды (отпуск: конверт с кастомным period_start/end); иерархия категорий (родитель агрегирует детей в отображении).

- [ ] **Step 2: Написать §3 «Экраны» — все 6**

Overview: баланс месяца (income−expense), сетка конвертов (карточка: имя+иконка категории, прогресс-бар с порогами 60/85/100%, остаток, дневной темп, carryover-бейдж), Coming up (recurring следующие 14 дней), Planned (запланированные покупки с суммами), Unbudgeted (категории без конверта). Категория: конверт текущего периода, carryover, список транзакций, мини-тренд 3–6 мес., body категории (правила) и кнопка в тред категории. Транзакции: список (дата, title, сумма ±цвет, бейдж категории, recurring-иконка), фильтры (категория/направление/период/суммы/planned), поиск, свайпы (рекатегоризация, в recurring). Импорт CSV — полный флоу: выбор файла → парсинг (AI: колонки/кодировка/формат дат) → экран ревью: таблица строк со статусами (новая / дубликат ручной записи — критерий: сумма±дата±1д+counterparty-похожесть / уже импортирована) и предложенными категориями, массовое подтверждение, правка категории инлайн → создание пачкой → итог-карточка; дедуп-ключ хранится в meta.import_id. Rollover: триггер — первое открытие в новом месяце; экран: таблица «категория / факт прошлого / carryover / предложение AI / поле правки», подтверждение одним действием, конверты создаются пачкой. Quick-add: сумма-клавиатура, 4–5 последних категорий пилюлями, опциональный title, переключатель income.

- [ ] **Step 3: Написать §4 «AI-сценарии», §5 «Edge cases», §6 «Бейджи»**

§4: «обед 340» (fast-path → карточка с остатком конверта), «получил 150к» (income + предложение распределить), «могу позволить кроссовки за 8000?» (остаток discretionary − planned − recurring до конца месяца), «переведи 3000 с транспорта на еду» (правка двух конвертов), «настрой бюджеты на июль» (rollover из чата), «сколько трачу на фитнес вообще?» (cross-aspect: financial по тегу fitness). §5 таблица: транзакция без конверта (Unbudgeted + предложение), возврат (income в ту же категорию), рекатегоризация (перенос relation, оба конверта пересчитываются), первый месяц (AI спрашивает доход/оценки вместо истории), повторный импорт того же CSV (полный дедуп по import_id), мульти-валюта (отказ в MVP: транзакция в чужой валюте сохраняется, в агрегаты не входит, бейдж предупреждения), удаление категории с историей (запрет; архивация с переносом транзакций). §6: бейдж вкладки Budget = число конвертов в красной зоне (>85%).

- [ ] **Step 4: Верификация Task 4**

Чек-лист: (а) поля аспектов соответствуют 01 §3 (никаких новых полей без правки 01); (б) список категорий — ссылка на 02 §7, не копия; (в) дедуп-критерий импорта однозначен; (г) spent нигде не описан как хранимое поле; (д) planned не участвует в spent, но участвует в прогнозах — проверить все упоминания; (е) нет «TBD/TODO».

---

### Task 5: Кросс-ревью комплекта

**Files:**
- Modify: все 4 документа (точечные правки по результатам)

- [ ] **Step 1: Проверка «один факт — один документ»**

Grep-проходы по `docs/prd/`: имена полей аспектов (category_ref, planned, carryover, deleted_at) — полные определения только в 01; query-синтаксис — грамматика только в 01; список категорий — только в 02 §7; метрики — только в 00. Любой дубль → заменить ссылкой.

- [ ] **Step 2: Проверка решений СПЕКИ §2**

По таблице решений СПЕКИ — каждый пункт имеет отражение в документах (чек-лист из 15 строк, включая «Внешние агенты/MCP»; отметить документ и раздел для каждого).

- [ ] **Step 3: Проверка ссылок и терминологии**

Все внутренние ссылки вида «см. 01 §N» указывают на существующие разделы; термины из глоссария 00 используются единообразно (не «конверт/энвелоп» вперемешку); ни один документ не ссылается на `docs/orbis-*.md`.

- [ ] **Step 4: Финальный отчёт владельцу**

Сводка: 4 файла, объёмы, список решений с местами фиксации, найденные и исправленные несоответствия. БЕЗ коммита — предложить владельцу проверить и решить про коммит.
