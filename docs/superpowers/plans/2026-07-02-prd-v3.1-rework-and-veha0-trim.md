# Переработка PRD v3 → v3.1 + подрезка плана Вехи 0 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Привести комплект `docs/prd/` к архитектуре v3.1 (онлайн-первый разрез + агентная петля), создать единый decision log, зафиксировать implementation-архитектуру (модули, потоки, sequence, ER) и подрезать план Вехи 0.

**Architecture:** Это план правки документов, не кода. «Тест» каждой задачи — grep-проверки вырезанных терминов и чек-листы согласованности из спеки §6; «реализация» — правки по карте из спеки §5. Порядок правки PRD: 01 → 00 → 02 → 03 → 04 (фундамент раньше потребителей), затем UX-ревью владельца, приёмка, implementation-архитектура и подрезка плана Вехи 0.

**Tech Stack:** Markdown, Mermaid (диаграммы), grep/git для верификации.

**Источник истины всех правок:** `docs/superpowers/specs/2026-07-02-prd-v3.1-online-first-agent-loop-design.md`. Исполнитель каждой задачи ОБЯЗАН прочитать спеку целиком до начала правок. Решения обозначаются D1–D11 (спека §2), состав слайсов — спека §4, карта правок — спека §5, приёмка — спека §6, входные данные подрезки Вехи 0 — спека §7.

## Global Constraints

- Язык документов — русский; идентификаторы кода, имена таблиц/полей — оригинал.
- **Grep-лист вырезанных терминов** (спека §6): `PowerSync`, `synced_at`, `server_revision`, `sync_log`, `tombstone`, `конфликтная копия`, `эквивалентност`, `import_ids`, `tiered`, `SQLite`. После переработки они допустимы в `docs/prd/` только: (а) в `04-decision-log.md` в полях «что заменяет/обоснование»; (б) в явных Future-сносках (например, 00-product §10). Нигде больше.
- Нейминг: `owner_id` (владение строкой) / `actor_user_id` (кто совершил действие); двусмысленный `user_id` запрещён (D11).
- Терминология: «retry-буфер» (не «очередь офлайн»); «одна грамматика — шесть потребителей» (не «два бэкенда»); «фаза 0», «слайс 1/2/3».
- Каждый факт живёт ровно в одном документе; кросс-ссылки вида «см. 01-architecture §M» валидны (спека §6 п.2).
- Ни одного TBD/TODO в итоговых документах.
- Слоты нумерации секций по возможности переиспользуются (замена содержимого вместо удаления секции), чтобы не ломать кросс-ссылки; где нумерация всё же меняется — grep по старым номерам и починка ссылок входит в задачу.
- Коммит после каждой задачи; сообщения — в шаге «Commit» задачи.
- Предусловие «закоммитить незакоммиченные правки docs/prd/» (спека §5) — **уже выполнено**: коммит `c8cc624`.

---

### Task 1: Переработка `docs/prd/01-architecture.md`

**Files:**
- Modify: `docs/prd/01-architecture.md`

**Interfaces (нумерация секций — контракт для задач 2–5, 8):**
- Consumes: спека §2 (D1–D7, D11, carried), §3, §5 (блок «01-architecture»).
- Produces: §4.8 = таблица `entity_origins` (слот `sync_log` переиспользован); §5 = «Персистентность и конкурентность» (§5.1 источник истины и кэш, §5.2 конкурентность, §5.3 retry-буфер, §5.4 детерминированные ID; секций §5.5/§5.6 больше нет); §6.2 = «Один бэкенд и golden-тесты»; §7.7 = «Модель и транспорт чата»; §7.10 = «Политика подтверждений AI-действий» (новая); §9.4 = «Экспорт данных» (новая). На эти номера ссылаются задачи 2–4 и Task 8.

- [ ] **Step 1: Прочитать спеку целиком и текущий 01-architecture.md**

- [ ] **Step 2: Применить правки §1**

- §1.1 стек-таблица: удалить строки `Local DB` и `Sync`; строка `State` → `Zustand (UI state) + TanStack Query (server state, оптимистичные обновления)`; остальные строки без изменений.
- §1.3 принцип 2 заменить на:

  > **Принцип 2: одна грамматика — шесть потребителей.** Одна грамматика запросов (§6), один движок с одним SQL-бэкендом (Postgres) и шесть потребителей — от `{{query:...}}`-блоков в body до AI-тула `entity_query`; любая новая фильтрующая возможность добавляется в грамматику один раз и автоматически доступна всем потребителям. Полный список потребителей — §6.3.

- §1.3 принцип 3: из перечня служебных meta-ключей убрать и `raw_input`, и `import_ids` (D4): `raw_input` живёт в контексте сообщения чата, provenance импорта — таблица `entity_origins` (§4.8). Если исключений не остаётся — переформулировать пункт без исключений. Затем `grep -n 'raw_input\|import_ids' docs/prd/01-architecture.md` и починить все остальные вхождения (в т.ч. §2.4 сквозной пример, §4.1).

- [ ] **Step 3: Применить правки §4**

- §4.1 `entities`: убрать столбец `server_revision` и любые упоминания `synced_at`; `user_id` → `owner_id`.
- §4.2 `relations`: убрать tombstone-механику (`deleted_at`, `server_revision`); удаление — обычный `DELETE`; unique-индекс — полный `UNIQUE (source_id, target_id, relation_type)` без partial-условия.
- Все таблицы §4: `user_id` → `owner_id` (включая `aspect_definitions`: встроенные записи — `owner_id IS NULL`).
- §4.8: содержимое `sync_log` целиком заменить таблицей **`entity_origins`** (слот и число таблиц сохраняются):

  | Столбец | Тип | Описание |
  |---|---|---|
  | `id` | uuid PK | |
  | `owner_id` | uuid NOT NULL | |
  | `entity_id` | uuid NOT NULL FK → entities | Сущность, созданная импортом |
  | `namespace` | text NOT NULL | Пространство источника (например, `csv:<источник>`) |
  | `external_id` | text NOT NULL | Идентификатор записи в источнике |
  | `created_at` | timestamptz NOT NULL | |

  Инварианты: `UNIQUE (owner_id, namespace, external_id)`; Undo импорта **физически удаляет** строки origins — повторный импорт возможен; содержательный дедуп-критерий — 03-budget §3.4.1.
- §4.9 индексы: убрать строки про `server_revision`, `synced_at` и SQLite-индексы; добавить `entity_origins` UNIQUE-индекс; для `chat_threads`/`chat_messages` заменить sync-индексы на `(owner_id)` / `(thread_id, created_at)` (история + RLS-проверка).
- §4.10 RLS: `user_id` → `owner_id`; добавить фразу-контракт (D11): «Политики генерируются из одного шаблона — будущий переход owner→membership правится в одном месте».
- §4.12 «Клиент vs сервер» переписать (3–5 предложений): клиент тонкий, клиентской БД нет; server-state-кэш (TanStack Query) — деталь реализации, не хранилище; единственные клиентские персистентные данные — retry-буфер неотправленных create-мутаций fast-path (§5.3).

- [ ] **Step 4: Заменить §5 «Sync-протокол» на «Персистентность и конкурентность»**

Удалить §5.1–§5.6 целиком, написать компактную секцию — контракты поведения, не реализация (D1–D3):

- **§5.1 Источник истины и кэш.** PostgreSQL — единственный источник истины; чтение через server-state-кэш с инвалидацией после мутаций; мгновенность UX — оптимистичные обновления с откатом при ошибке.
- **§5.2 Конкурентность.** LWW на уровне API; для правок `body` — optimistic-check по `updated_at`: правка с устаревшей версией отклоняется структурированной ошибкой, UI предлагает перезагрузить и повторить. Компромисс «две вкладки» — §12.
- **§5.3 Retry-буфер ввода.** Буферизуются только create-операции fast-path: client-generated UUIDv7; серверная идемпотентность по client-UUID обязательна даже онлайн (повторы браузера, упавшие вкладки); буфер различает transport failure (ретраить) и business rejection (не ретраить, показать ошибку); удаление из очереди — только после подтверждения сервера; корректность не зависит от service workers / Background Sync. Офлайн-правки недоступны.
- **§5.4 Детерминированные ID порождаемых сущностей.** Перенести содержимое бывшего §5.5 с двумя правками: uuidv5-инстансы порождает **только сервер**; формулы, включающие идентификатор пользователя (глобальный тред, entity-треды), пометить «workspace-scoped при введении workspace'ов» (D11).

Затем `grep -n '§5\.' docs/prd/*.md` — починить все ссылки на старые §5.1–§5.6 (в 01 и других документах, где встретятся).

- [ ] **Step 5: Применить правки §6, §7**

- §6.1/§6.4: убрать упоминания второго бэкенда/SQLite, если есть.
- §6.2 → «Один бэкенд и golden-тесты»: единственный SQL-бэкенд — Postgres; корректность компиляции грамматика→SQL проверяется юнит-тестами на golden-фикстурах (запрос → ожидаемый SQL + ожидаемый результат на эталонном датасете). Требование эквивалентности бэкендов удалено вместе со вторым бэкендом.
- §7.1: убрать tiered loading тулов, если упоминается — в MVP тулы загружаются всегда.
- §7.7 «Роутинг моделей» → «Модель и транспорт чата» (D7): в MVP одна модель (Claude по умолчанию), роутинг моделей — Future; транспорт — обычная tRPC-мутация, ответ целиком; ни `useChat`, ни SSE; шов для стриминга остаётся в `LLMProvider`.
- §7.8: добавить абзац retention журнала — числа 90/180/30 дней взять с расшифровкой из `docs/implementation_old/00-decision-ledger.md` (carried-решение).
- §7.9 «Деградация» переписать: офлайн — работает fast-path-ввод через retry-буфер (§5.3) и чтение из кэша; LLM-путь офлайн честно недоступен (без очереди); сбой провайдера/лимит — явная ошибка с возможностью повторить, не очередь.
- Новая **§7.10 «Политика подтверждений AI-действий»** (D6): уровни `execute / preview / explicit-confirmation / forbidden`; уровень определяет политика Orbis после валидации tool-call, не модель; входы классификации: явность намерения, обратимость, масштаб (single/bounded/bulk), внешние эффекты, чувствительность; одобрение исполняет сохранённый неизменяемый payload с ревалидацией состояния — не повторный вызов модели; правила едины для внутреннего AI и MCP-агентов.

- [ ] **Step 6: Применить правки §8–§9**

- §8: проверить, что entitlements-резолвер описан как принимающий субъекта параметром (D11); если нет — добавить одной фразой.
- §9.1: добавить контракты — **min-compatible-version** (сервер объявляет минимальную совместимую версию клиента; устаревший клиент получает требование обновиться) и **«один путь мутаций»** (PWA никогда не ходит в Supabase Data API напрямую — все мутации только через tRPC → executor).
- §9.2: добавить 7-стадийный конвейер executor'а как контракт исполнения любых мутаций: validate envelope → validate aspects → load state → validate all before first write → apply in transaction → inverse ops + cards → audit.
- §9.3 MCP: статус — MVP, слайс 1; PAT-контракт: hash-only хранение, constant-time сравнение, показ токена один раз, выдача через CLI/env (экран настроек — Future); добавить эталонный сценарий **«что нового»** рядом с существующим «найди задачу и выполни»: `entity_query` с фильтром `updated_at > cursor` находит изменённые задачи → `entity_get(include: ["thread"])` читает новые инструкции в тредах → курсор «момент прошлого опроса» агент хранит у себя; нового механизма на сервере нет.
- Новая **§9.4 «Экспорт данных»** (D8): JSON-дамп (сущности, relations, треды, настройки) — одна ручка + кнопка в настройках (02-core-os §1.6); при любом будущем CSV-экспорте — formula-injection escaping.

- [ ] **Step 7: Применить правки §12–§13**

- §12: пункт 2 (last-applied-wins / устройства / конфликтная копия) заменить тремя: «офлайн-правки недоступны — офлайн покрывает только буферизованный ввод и кэш чтения»; «две вкладки/устройства, правящие один body, — optimistic-check с отказом, не merge»; «полный offline-first отложен осознанно (см. 04-decision-log, D1)».
- §13: №1 (clock skew sync) заменить двумя проверками: (а) optimistic-check — два клиента читают сущность, оба правят body; поздняя правка со stale `updated_at` отклоняется структурированной ошибкой; после перезагрузки повтор успешен; (б) идемпотентность досылки — повторная отправка create с тем же client-UUID не создаёт дубль и возвращает исходный результат. №2 переформулировать без офлайн/sync: два клиента одновременно впервые пишут в тред одной сущности → существует одна строка `chat_threads` с детерминированным ID, оба сообщения в ней (конфликт гасится на уровне БД). №5 Decimal: убрать «клиентском (SQLite)» — точность проверяется в Postgres и в persisted JSON.

- [ ] **Step 8: Верификация**

```bash
grep -niE 'powersync|synced_at|server_revision|sync_log|tombstone|sqlite|import_ids|tiered' docs/prd/01-architecture.md
grep -nE 'онфликтн.{0,4} копи|квивалентност' docs/prd/01-architecture.md
grep -n 'TBD\|TODO' docs/prd/01-architecture.md
grep -n 'user_id' docs/prd/01-architecture.md
```
Ожидаемо: первые три — пусто; четвёртый — только `actor_user_id` (если встречается в журнале) и `auth.uid()`-подобные выражения, ни одного голого `user_id`-столбца.

- [ ] **Step 9: Commit**

```bash
git add docs/prd/01-architecture.md
git commit -m "docs(prd): 01-architecture — онлайн-первый разрез v3.1 (D1–D8, D11)"
```

---

### Task 2: Переработка `docs/prd/00-product.md`

**Files:**
- Modify: `docs/prd/00-product.md`

**Interfaces:**
- Consumes: спека §1 (два якорных сценария), §2, §4 (роадмап), §5 (блок «00-product»); номера секций 01 из Task 1 (§5.3 retry-буфер, §7.10 политика подтверждений, §9.4 экспорт).
- Produces: §9 роадмап «фаза 0 + слайсы 1–3» — на его состав ссылаются пометки слайсов в 02/03 (задачи 3–4) и приёмка (Task 7 п.6).

- [ ] **Step 1: Прочитать спеку §1, §2, §4, §5 и текущий 00-product.md**

- [ ] **Step 2: Применить правки §1–§4**

- §1 Vision: добавить второй якорный сценарий — **рабочее пространство человек + агенты**: внешние агенты (Claude Code через MCP) работают внутри Orbis над задачами проектов; их действия видимы, атрибутированы, обратимы и управляемы с телефона; первый проект в системе — разработка самого Orbis. Оба сценария — ядро, не Future.
- §2: добавить шестую боль: «работа агентов непрозрачна и живёт вне моих инструментов» → ответ Orbis: агенты работают в том же графе, что и владелец, — каждое действие в журнале, атрибутировано (`actor_user_id`), обратимо (Undo) и подчинено политике подтверждений (01-architecture §7.10).
- §3: цель итерации → максимально быстрый продакшен, в котором ежедневно работают двое: владелец (телефон + ноут) и внешние AI-агенты; критерий — работа над Orbis ведётся в Orbis.
- §4 «В скоупе»: строку `Offline-first` заменить на «**Ввод не теряется без сети**: retry-буфер fast-path-создания (01-architecture §5.3); полный offline-first — Future»; строку MCP дополнить «слайс 1, включая паттерн „что нового"»; добавить строку «**Экспорт данных** — JSON-дамп одной кнопкой (01-architecture §9.4): система, претендующая на роль единственного хранилища жизни, обязана иметь выход с первого дня». «Вне скоупа»: строка про PAT-UI и скоупы агентов остаётся; других строк про MCP быть не должно.

- [ ] **Step 3: Применить правки §5–§8**

- §5 принципы ввода: четыре пути остаются; пометить — fast-path и LLM-чат со слайса 1, quick-add в views и CSV-импорт — слайс 2.
- §6.2 Corrections & Undo: дополнить — правила распространяются на действия агентов; уровень подтверждения до исполнения определяет политика (01-architecture §7.10).
- §7 «День пользователя» переписать по спеке §5: М1 утро (Daily Planning); М2 «обед 340» — карточка мгновенно, в слайсе 1 без остатка конверта (категория и сумма; остаток — со слайса 2); М3 cross-aspect задача; М4 ноут; М5 «что по бюджету?» — в слайсе 1 отвечает чат агрегацией, Budget Overview — слайс 2; М6 исправление + Undo; **М7 агентный момент**: поставил агенту задачу из треда с телефона → вечером посмотрел статусы и заметки агента.
- §8 метрики: «покрытие ≥95% транзакций» измеряется со слайса 2 (требует CSV-импорта); добавить приёмочную метрику слайса 1: «работа над Orbis ведётся в Orbis» (агент создал проект, перенёс документацию, слайс 2 ведётся через задачи в Orbis).

- [ ] **Step 4: Применить правки §9–§11 и глоссарий**

- §9 роадмап заменить на «фаза 0 + три слайса» — состав дословно из спеки §4 (фаза 0: SPIKE-01 RLS, SPIKE-05 деплой; слайс 1 «Агентная петля + ввод» с приёмкой; слайс 2 «Финансовый контур + время»; слайс 3 «Полировка и глубина»).
- §10 Future: убрать MCP (теперь MVP); добавить: полный offline-first (при возврате отправная точка — готовый sync-движок класса PowerSync, не рукописный протокол — это Future-сноска, единственное допустимое упоминание термина), Yjs/CRDT-совместное редактирование, стриминг ответов, workspace'ы + шаринг + роли (D11).
- §11 риски: sync-риск заменить на «offline отложен осознанно; ввод покрыт retry-буфером»; добавить риск «write-агент подрывает доверие к данным» → митигация: политика подтверждений + журнал с атрибуцией + Undo.
- Глоссарий: добавить «retry-буфер», «optimistic-check», «PAT», «entity_origins», «паттерн „что нового"»; убрать термины вырезанного sync-протокола (tombstone, конфликтная копия, server_revision), если есть.

- [ ] **Step 5: Верификация**

```bash
grep -niE 'synced_at|server_revision|sync_log|tombstone|sqlite|import_ids|tiered' docs/prd/00-product.md
grep -niE 'powersync' docs/prd/00-product.md
grep -nE 'онфликтн.{0,4} копи|квивалентност' docs/prd/00-product.md ; grep -n 'TBD\|TODO' docs/prd/00-product.md
```
Ожидаемо: первый и третий — пусто; второй — не больше одного вхождения, и только внутри §10 Future.

- [ ] **Step 6: Commit**

```bash
git add docs/prd/00-product.md
git commit -m "docs(prd): 00-product — второй якорный сценарий, скоуп и роадмап v3.1"
```

---

### Task 3: Переработка `docs/prd/02-core-os.md`

**Files:**
- Modify: `docs/prd/02-core-os.md`

**Interfaces:**
- Consumes: спека §4–§5 (блок «02-core-os»); 01 §5.3/§7.10/§9.3/§9.4 (Task 1); 00 §9 слайсы (Task 2).
- Produces: обновлённые ASCII-мокапы с пометками слайсов — предмет UX-ревью владельца (Task 6); §2.6 = «Retry-буфер» (слот сохранён).

- [ ] **Step 1: Прочитать спеку §4–§5 и текущий 02-core-os.md**

- [ ] **Step 2: Применить правки §1**

- §1.1 карта экранов: пометить слайсы — Agenda «слайс 2», Budget «слайс 2 (fast-path-ввод расходов — слайс 1)»; экраны слайса 1: Chat (глобальный тред + треды сущностей), Browser-lite, минимальный detail, настройки.
- §1.5 бейджи: бейдж Chat → размер retry-буфера («ждут отправки: N»); бейджи Budget/Agenda — пометка «со слайса 2».
- §1.6 настройки: добавить пункт «Экспорт данных» (JSON-дамп одной кнопкой, контракт — 01-architecture §9.4); PAT: выдача/отзыв через CLI/env, экран управления — Future.

- [ ] **Step 3: Применить правки §2**

- §2.2 тред сущности: пометка «слайс 1» (пересмотр под агентную петлю, D9): заметки агента по задаче и обсуждение задачи с ИИ живут в треде сущности; владелец отвечает агенту из треда с телефона. Зафиксировать архитектурную гарантию: чат-компонент переиспользуемый (лента + ввод + карточки) — тред сущности на detail-экране это тот же компонент.
- §2.3 карточки: убедиться, что есть карточка подтверждения для уровней preview/explicit-confirmation (01-architecture §7.10) — превью действия + кнопки «подтвердить/отменить»; если нет — добавить в перечень типов.
- §2.4 suggestion chips: пометка «слайс 3».
- §2.5 fast-path: добавить состояние карточки «ждёт отправки» (создание попало в retry-буфер).
- §2.6 «Очередь офлайн» → **«Retry-буфер»**, переписать: над полем ввода индикатор «ждут отправки: N» (N = create-операции fast-path в буфере); тап → список ожидающих, каждую можно отменить до отправки; удаление из буфера — только после подтверждения сервера; business rejection → запись покидает буфер с явной ошибкой; **LLM-запросы офлайн в очередь не встают — LLM-путь честно недоступен** с объяснением (упрощение против v3); поведенческий контракт — 01-architecture §5.3, деградация — §7.9.
- §2.7 UI памяти: пометка «слайс 2».

- [ ] **Step 4: Применить правки §3–§8**

- §3: пометки слайсов — слайс 1 = Browser-lite: список, сайдбар pinned, три сидированных smart lists, минимальный detail-экран (title/tags/body/аспекты/подзадачи + тред сущности); полный detail (блокировки, backlinks) — слайс 2; §3.4 query-builder-форма — слайс 3; §3.7 quick-capture бар — слайс 1 (главный не-чатовый путь ввода).
- §3.9 служебные сущности: убрать конфликтные копии.
- §4 Agenda: пометка «слайс 2» у заголовка; содержание без изменений; §8 приёмочные проверки Agenda — та же пометка.
- §5 AI-сценарии: добавить сценарий внешнего агента: агент через MCP создаёт проект и задачи, двигает статусы, пишет заметки в тред задачи; инструкцию владельца из треда находит паттерном «что нового» (01-architecture §9.3); все действия — через executor в журнал, с политикой подтверждений.
- §6 edge cases: пункт про конфликтную копию заменить поведением optimistic-check: правка body с устаревшей версией → структурированная ошибка → UI предлагает перезагрузить сущность и повторить.
- §7 сидирование: убедиться, что сидируются категории, smart lists, настройки и **глобальный тред** (слайс 1); добавить глобальный тред, если отсутствует.

- [ ] **Step 5: Обновить ASCII-мокапы**

Пройти все мокапы документа: экраны слайса 1 должны быть полны и самодостаточны (включая не-чатовые пути ввода: quick-capture бар, «+аспект» на detail-экране, inline-правки полей); элементы слайса 2/3 не удалять — помечать `[слайс 2]` / `[слайс 3]` рядом с элементом или экраном. Добавить мокап треда сущности на detail-экране (тот же чат-компонент).

- [ ] **Step 6: Верификация**

```bash
grep -niE 'powersync|synced_at|server_revision|sync_log|tombstone|sqlite|import_ids|tiered' docs/prd/02-core-os.md
grep -nE 'онфликтн.{0,4} копи|квивалентност' docs/prd/02-core-os.md
grep -n 'очередь офлайн\|Очередь офлайн\|ждут сети' docs/prd/02-core-os.md ; grep -n 'TBD\|TODO' docs/prd/02-core-os.md
```
Ожидаемо: всё пусто.

- [ ] **Step 7: Commit**

```bash
git add docs/prd/02-core-os.md
git commit -m "docs(prd): 02-core-os — retry-буфер, треды сущностей в слайс 1, пометки слайсов"
```

---

### Task 4: Переработка `docs/prd/03-budget.md`

**Files:**
- Modify: `docs/prd/03-budget.md`

**Interfaces:**
- Consumes: спека §4–§5 (блок «03-budget»); 01 §4.8 `entity_origins` (Task 1); 00 §9 слайсы (Task 2).
- Produces: §3.4.1 дедуп на `entity_origins` — согласован с 01 §4.8; пометки слайсов — согласованы с 00 §9 (проверяет Task 7 п.6).

- [ ] **Step 1: Прочитать спеку §4–§5 и текущий 03-budget.md**

- [ ] **Step 2: Применить пометки слайсов**

- §1: добавить явную рамку слайсов: fast-path-ввод расходов — слайс 1 («данные существуют до views»: история копится с первого дня без Budget-view); сам Budget view — слайс 2 и при открытии показывает накопленную историю.
- §2 модель и формулы — без содержательных изменений; §2.7 planned и §2.8 recurring — пометка «слайс 2».
- §3: Overview (§3.1), экран категории (§3.2), транзакции (§3.3), quick-add бар (§3.6) — «слайс 2»; CSV-импорт (§3.4) и rollover (§3.5, к границе первого месяца) — «слайс 2».
- §4 AI-сценарии: добавить пометку — в слайсе 1 «что по бюджету?» отвечает чат агрегацией через `entity_query` (без Overview).
- §6.1 бейдж Budget — «со слайса 2»; §7 приёмочные проверки — пометить, какие проверки выполнимы только со слайса 2.

- [ ] **Step 3: Переписать §3.4.1 дедуп-критерий**

Заменить механизм `meta.import_ids` на таблицу `entity_origins` (01-architecture §4.8):
- жёсткая уникальность source-id: `UNIQUE (owner_id, namespace, external_id)` на уровне БД — строка выписки не может быть импортирована дважды;
- содержательный дедуп-критерий (1)+(2)+(3) — сохраняется как есть, меняется только место хранения provenance;
- Undo импорта физически удаляет строки `entity_origins` → повторный импорт той же выписки возможен;
- у сущности может быть несколько origins (одна операция видна в двух выписках).

- [ ] **Step 4: Верификация**

```bash
grep -niE 'powersync|synced_at|server_revision|sync_log|tombstone|sqlite|import_ids|tiered' docs/prd/03-budget.md
grep -nE 'онфликтн.{0,4} копи|квивалентност' docs/prd/03-budget.md ; grep -n 'TBD\|TODO' docs/prd/03-budget.md
```
Ожидаемо: всё пусто (в т.ч. ни одного `import_ids`).

- [ ] **Step 5: Commit**

```bash
git add docs/prd/03-budget.md
git commit -m "docs(prd): 03-budget — дедуп на entity_origins, пометки слайсов"
```

---

### Task 5: Создание `docs/prd/04-decision-log.md` + финализация архива

**Files:**
- Create: `docs/prd/04-decision-log.md`
- Modify: `docs/implementation_old/00-decision-ledger.md` (только шапка)
- Create: `docs/implementation_old/README.md`

**Interfaces:**
- Consumes: спека §2 целиком (D1–D11 + carried-список).
- Produces: единственный действующий decision log; идентификаторы D1–D11 — на них ссылаются 00/01/02/03 (ссылки вида «см. 04-decision-log, D1»).

- [ ] **Step 1: Написать `docs/prd/04-decision-log.md`**

Структура:

```markdown
# Orbis PRD v3.1 — 04: Decision Log

Единственный действующий журнал архитектурных решений. Предыдущий журнал
(`docs/implementation_old/00-decision-ledger.md`) — superseded; его живые
решения перенесены сюда со статусом carried.

Формат записи: решение / статус / обоснование / что заменяет.

## Решения v3.1 (2026-07-02)

### D1. Онлайн-первый разрез
- **Решение:** …
- **Статус:** принято 2026-07-02
- **Обоснование:** …
- **Заменяет:** «полный offline-first» спеки v3; PROD-01 леджера

### D2 … D11 (по одной записи на решение)

## Carried-решения (из decision-ledger, статус carried)

| Решение | Суть (1–2 строки) | Где детализируется |
|---|---|---|
| Механика RLS через Bun API | … | SPIKE-01, implementation-план |
| … | | |
```

Содержимое каждой записи D1–D11 — из спеки §2, сжать до 3–6 строк **без потери контрактных деталей**. Обязательны к сохранению дословно: для D2 — три пункта контракта буфера (идемпотентность по client-UUID даже онлайн; transport failure ≠ business rejection; удаление только после подтверждения); для D5 — механика «что нового»; для D6 — четыре уровня и пять входов классификации + immutable payload с ревалидацией; для D11 — все пять пунктов workspace-ready чек-листа и принцип «Deferral means implementation must preserve the boundary, not add placeholder infrastructure», плюс отвергнутые альтернативы. Carried-список — все пункты из спеки §2 «Carried-решения леджера», по строке на решение.

- [ ] **Step 2: Обновить шапку леджера и пометить архив**

- В `docs/implementation_old/00-decision-ledger.md` в существующей superseded-шапке заменить упоминание будущего файла на действующую ссылку `docs/prd/04-decision-log.md`.
- Создать `docs/implementation_old/README.md` (5–8 строк): каталог — архив, действующие документы — `docs/prd/` (00–04); что из архива остаётся входом для implementation-планов: `01-application-architecture.md` (карта модулей, конвейер executor'а, механика RLS), `03-quality-security-delivery.md` (security-чеклист, правила миграций, перф-бюджеты, pgTAP).

- [ ] **Step 3: Верификация**

```bash
grep -c '^### D' docs/prd/04-decision-log.md        # ожидаемо: 11
grep -n 'TBD\|TODO' docs/prd/04-decision-log.md      # пусто
grep -n '04-decision-log' docs/implementation_old/00-decision-ledger.md  # ссылка на действующий файл
```

- [ ] **Step 4: Commit**

```bash
git add docs/prd/04-decision-log.md docs/implementation_old/00-decision-ledger.md docs/implementation_old/README.md
git commit -m "docs(prd): 04-decision-log — D1–D11 + carried-решения; архив финализирован"
```

---

### Task 6: UX-ревью владельца (checkpoint — выполняется в основной сессии, не сабагентом)

**Files:**
- Modify (по итогам ревью): `docs/prd/02-core-os.md`, `docs/prd/03-budget.md`

**Interfaces:**
- Consumes: обновлённые мокапы из задач 3–4.
- Produces: подтверждённый владельцем UX слайса 1; правки мокапов, если запрошены.

- [ ] **Step 1: Показать владельцу сводку изменённых мокапов** — экраны слайса 1 (Chat + тред сущности, Browser-lite, минимальный detail, quick-capture, настройки с экспортом) и пометки слайсов 2/3. Явно задать три вопроса: (1) экранов слайса 1 достаточно для ежедневной работы с телефона? (2) не-чатовые пути ввода (quick-capture, «+аспект», inline-правки) понятны? (3) подтянуть ли quick-add расходов из слайса 2 в слайс 1?
- [ ] **Step 2: Применить правки по фидбеку** в 02/03 (и в 00 §9, если владелец сдвинул состав слайсов).
- [ ] **Step 3: Commit (только если были правки)**

```bash
git add docs/prd/00-product.md docs/prd/02-core-os.md docs/prd/03-budget.md
git commit -m "docs(prd): правки мокапов и слайсов по UX-ревью владельца"
```

---

### Task 7: Приёмка переработки (спека §6)

**Files:**
- Modify (только починка находок): `docs/prd/*.md`

**Interfaces:**
- Consumes: все пять документов `docs/prd/` после задач 1–6.
- Produces: комплект PRD v3.1, проходящий все шесть пунктов приёмки спеки §6.

- [ ] **Step 1: Grep-проход по вырезанным терминам**

```bash
grep -rniE 'powersync|synced_at|server_revision|sync_log|tombstone|sqlite|import_ids|tiered' docs/prd/
grep -rnE 'онфликтн.{0,4} копи|квивалентност' docs/prd/
```
Ожидаемо: совпадения только в `04-decision-log.md` (поля «заменяет/обоснование») и один `PowerSync` в 00-product §10 Future. Любое иное совпадение — починить.

- [ ] **Step 2: Кросс-ревью ссылок и фактов**

```bash
grep -rnoE '0[0-4]-[a-z-]+(\.md)? ?§[0-9]+(\.[0-9]+)?' docs/prd/ | sort -u
```
Для каждой ссылки проверить, что целевая секция существует (сверить с `grep -n '^#' <файл>`). Затем выборочно проверить дублирование фактов: формула `spent` — только в 03; политика подтверждений — только в 01 §7.10 (остальные ссылаются); контракт retry-буфера — только в 01 §5.3; состав слайсов — только в 00 §9 (в 02/03 — пометки со ссылкой).

- [ ] **Step 3: Валидность сидируемых body**

Каждый `{{query:...}}`-блок в body преднастроенных сущностей (02 §3.3, §7) вручную разобрать против грамматики 01 §6.1: каждый ключ фильтра существует в грамматике, синтаксис значений соответствует. Несоответствия — починить (в body или в грамматике — по смыслу).

- [ ] **Step 4: Финальные проверки**

```bash
grep -rn 'TBD\|TODO' docs/prd/                      # пусто
ls docs/prd/                                        # ровно 5 файлов 00–04
grep -n 'SUPERSEDED' docs/implementation_old/00-decision-ledger.md  # шапка на месте
```
Плюс ручная сверка: слайсы 00 §9 согласованы с пометками в 02/03 (каждая функция с пометкой «слайс N» в 02/03 стоит в том же слайсе в 00 §9).

- [ ] **Step 5: Commit**

```bash
git add docs/prd/
git commit -m "docs(prd): приёмка переработки v3.1 — grep-pass и кросс-ревью"
```

---

### Task 8: Implementation-архитектура — `docs/implementation/00-architecture.md`

**Files:**
- Create: `docs/implementation/00-architecture.md`

**Interfaces:**
- Consumes: переработанный `docs/prd/01-architecture.md` (Task 1) — единственный источник состава таблиц и контрактов; `docs/implementation_old/01-application-architecture.md` — источник карты модулей и правил AI-пакета (адаптировать к v3.1: минус sync-модуль, плюс retry-буфер).
- Produces: карта модулей и имена пакетов (`apps/web`, `apps/server`, `packages/shared`) — на них ссылается подрезанный план Вехи 0 (Task 9); четыре sequence-диаграммы — референс для implementation-планов слайсов.

- [ ] **Step 1: Написать §1 «Карта модулей монорепо»**

Структура пакетов и правила направления зависимостей (сверить с картой файлов плана Вехи 0 и `implementation_old/01`):

```
apps/web        — PWA (React): экраны, переиспользуемый чат-компонент,
                  retry-буфер, TanStack Query + Zustand
apps/server     — Hono + @hono/trpc-server: tRPC-роутеры, executor (7 стадий),
                  fast-path-парсер, LLM-оркестрация (LLMProvider), MCP-сервер,
                  экспорт
packages/shared — Zod-схемы, AST-типы грамматики, константы, типы реестра
                  аспектов
```

Правила: `apps/*` → `packages/shared`; apps не импортируют друг из друга; типы Vercel AI SDK не выходят за пределы модуля LLMProvider; клиент не знает о Drizzle/Supabase — все мутации и чтения только через tRPC («один путь мутаций», PRD 01 §9.1).

- [ ] **Step 2: Написать §2 «Поток мутаций» и §3 «Поток чтения» (Mermaid flowchart)**

§2 — обязательные узлы: источники (Chat fast-path / Chat LLM tool-call / quick-capture / MCP-агент) → tRPC-процедура → [для LLM/MCP: политика подтверждений §7.10] → executor 7 стадий → журнал actions + Postgres (одна транзакция) → ответ → инвалидация server-state-кэша. Retry-буфер — на стороне web перед tRPC для fast-path-create.

§3 — обязательные узлы: потребители (Browser, `{{query:...}}`-блоки body, AI `entity_query`, MCP `entity_query`, Agenda, Budget) → парсер грамматики (shared) → SQL-компилятор (server) → Postgres → рендеринг/ответ. Шесть потребителей — сверить с PRD 01 §6.3.

- [ ] **Step 3: Написать §4 «Sequence-диаграммы ключевых флоу»**

Четыре диаграммы (Mermaid `sequenceDiagram`), каждая обязана кодировать контракт PRD:

1. **Fast-path + retry-буфер** (PRD 01 §5.3, §7.5): пользователь → парсер уверен → enqueue(clientId=UUIDv7) → оптимистичная карточка «ждёт отправки» → отправка; три ветки: transport failure (остаётся в очереди, ретрай с backoff), business rejection (удалить из очереди + ошибка в UI), успех (executor идемпотентен по client-UUID → журнал → подтверждение → удалить из очереди).
2. **Tool-call + политика подтверждений** (PRD 01 §7.10, D7): сообщение (tRPC-мутация) → LLMProvider → tool-call → валидация → классификация уровня; ветки execute / preview / explicit-confirmation (сохранённый immutable payload → одобрение → ревалидация состояния → исполнение без повторного вызова модели) / forbidden; ответ целиком, без стриминга.
3. **MCP-polling «что нового»** (PRD 01 §9.3): loop: `entity_query(updated_at > cursor)` [PAT] → изменённые задачи → `entity_get(include:["thread"])` → инструкции владельца → работа → `entity_update(status)` + заметка в тред → executor → журнал (актор — агент); cursor хранит агент.
4. **Optimistic-check body** (PRD 01 §5.2): две вкладки читают `updated_at=t0` → первая правит успешно (t1) → вторая получает 409 «устаревшая версия» → перезагрузка → повтор успешен.

- [ ] **Step 4: Написать §5 «ER-схема» (Mermaid erDiagram)**

Восемь таблиц — состав и ключевые поля скопировать из PRD 01 §4 (после Task 1): `entities`, `relations`, `aspect_definitions`, `user_settings`, `chat_threads`, `chat_messages`, `ai_usage`, `entity_origins`. Связи: `relations.source_id/target_id → entities`; `entity_origins.entity_id → entities`; `chat_threads.entity_id → entities` (nullable, глобальный тред); `chat_messages.thread_id → chat_threads`. Никаких полей `server_revision`/`synced_at`; владение — `owner_id`.

- [ ] **Step 5: Дописать §6 «Граница детализации»**

Три-четыре предложения: класс-уровневая детализация (сигнатуры, файлы) выполняется just-in-time в implementation-плане каждого слайса, не здесь; этот документ обновляется, когда меняются контракты PRD.

- [ ] **Step 6: Верификация**

- Каждая Mermaid-диаграмма рендерится без синтаксических ошибок (проверить в Mermaid-рендерере или внимательной вычиткой синтаксиса).
- Сверка с PRD: все таблицы §5 ER = PRD 01 §4 (по одной, без пропусков); участники sequence-диаграмм не противоречат карте модулей §1; в документе нет вырезанных терминов:

```bash
grep -niE 'powersync|synced_at|server_revision|sync_log|tombstone|sqlite' docs/implementation/00-architecture.md
```
Ожидаемо: пусто.

- [ ] **Step 7: Commit**

```bash
git add docs/implementation/00-architecture.md
git commit -m "docs(implementation): карта модулей, потоки, sequence-диаграммы, ER для v3.1"
```

---

### Task 9: Подрезка плана Вехи 0 (`docs/superpowers/plans/2026-06-16-phase1-veha0-foundation.md`)

**Files:**
- Modify: `docs/superpowers/plans/2026-06-16-phase1-veha0-foundation.md`

**Interfaces:**
- Consumes: спека §7 (полный перечень «выживает/подрезать/умирает»); `docs/implementation/00-architecture.md` (Task 8) — имена пакетов и модулей; PRD 01 §4/§5 (Task 1) — схема и контракты.
- Produces: исполняемый план Вехи 0 v3.1 — вход для superpowers:executing-plans / subagent-driven-development на этапе кода.

- [ ] **Step 1: Обновить шапку и карту файлов**

- В заголовок/шапку добавить строку: «Ревизия 2026-07-02 под v3.1 (онлайн-первый разрез) — см. спеку 2026-07-02 §7».
- Стек в шапке: убрать PowerSync/SQLite; добавить TanStack Query.
- «Карта файлов Вехи 0»: убрать файлы PowerSync/SQLite; добавить модуль retry-буфера (`apps/web/src/lib/retry-buffer/`) и файл схемы `entity_origins`.

- [ ] **Step 2: Подрезать T5 (Drizzle-схема)**

- Убрать таблицу `sync_log`, столбцы `synced_at`/`server_revision` где встречаются, `deleted_at` у `relations`; unique-индекс relations — полный `UNIQUE (source_id, target_id, relation_type)`.
- Добавить таблицу `entity_origins` (столбцы — PRD 01 §4.8) с `UNIQUE (owner_id, namespace, external_id)`.
- Нейминг столбцов: `owner_id` (не `user_id`).
- Сохранить/добавить сноску-граблю: drizzle-kit может не выражать partial unique индексы декларативно (unique-тред `WHERE entity_id IS NULL` в `chat_threads`) — SQL дописывается в миграцию вручную.

- [ ] **Step 3: Подрезать T6 (Zod-схемы)**

Убрать `syncedAt`/`deletedAt` из схем; Zod-схему для `entity_origins` не добавлять (server-only таблица, YAGNI — появится со слайсом импорта).

- [ ] **Step 4: Заменить T10 (PowerSync) на «Retry-буфер — скелет»**

Прежний T10 удалить целиком (общего кода нет). Новый T10 — модуль `apps/web/src/lib/retry-buffer/` в том же скелетном объёме, что и соседние задачи Вехи 0:

```ts
// Контракт — PRD 01 §5.3
interface QueuedCreate {
  clientId: string;      // UUIDv7, генерируется при постановке
  tool: string;          // имя тула реестра, например 'entity_create'
  payload: unknown;
  createdAt: string;     // ISO, для отображения в списке ожидающих
}

interface RetryBuffer {
  enqueue(op: Omit<QueuedCreate, 'clientId' | 'createdAt'>): QueuedCreate;
  flush(send: (op: QueuedCreate) => Promise<FlushOutcome>): Promise<void>;
  cancel(clientId: string): void;   // отмена до отправки (02-core-os §2.6)
  size(): number;                    // бейдж Chat «ждут отправки: N»
}

type FlushOutcome = 'confirmed' | 'transport_failure' | 'business_rejection';
// confirmed → удалить из очереди; transport_failure → оставить (ретрай);
// business_rejection → удалить + отдать ошибку наружу
```

Шаги нового T10 в стиле плана: failing-тест на enqueue→flush(confirmed)→удаление, тест на различение transport_failure (остаётся) vs business_rejection (удаляется с ошибкой), минимальная реализация с персистом очереди в localStorage (интерфейс хранения — отдельная функция, чтобы заменить на IndexedDB позже), прогон, коммит.

- [ ] **Step 5: Подрезать T11 (acceptance-харнесс)**

- Оставить: AST-типы грамматики, общие фикстуры, метод «PRD-контракт = исполняемый skipped-тест».
- Удалить: skipped-тесты эквивалентности бэкендов и матрицы конфликтов.
- Добавить skipped-тесты новых контрактов: fast-path-грамматика (PRD 01 §7.5), идемпотентность досылки по client-UUID (PRD 01 §5.3), optimistic-check `updated_at` (PRD 01 §5.2), дедуп CSV через `entity_origins` (PRD 03 §3.4.1), политика подтверждений — классификация уровней (PRD 01 §7.10).

- [ ] **Step 6: Переписать «Очерки слайсов» и пройтись по остальным задачам**

- «Очерки слайсов» заменить на фазу 0 + слайсы 1–3 из PRD 00 §9 (пост-Task 2); слайс «Sync» удалить целиком.
- T1–T4, T7–T9, T12 — оставить как есть, но вычитать на упоминания вырезанных терминов и `user_id` и починить.
- Сохранить сноску: версии в сниппетах (Zod ^3.24, Biome 2, tRPC v11, React 19) перепроверить при исполнении.

- [ ] **Step 7: Верификация**

```bash
grep -niE 'powersync|sqlite|synced_at|server_revision|sync_log|эквивалентност' docs/superpowers/plans/2026-06-16-phase1-veha0-foundation.md
grep -c '^### Task' docs/superpowers/plans/2026-06-16-phase1-veha0-foundation.md   # ожидаемо: 12
grep -n 'TBD\|TODO' docs/superpowers/plans/2026-06-16-phase1-veha0-foundation.md    # пусто
```
Первый grep: допустимо только упоминание в строке «Ревизия…» шапки, если она поясняет, что вырезано; лучше — ноль совпадений.

- [ ] **Step 8: Commit**

```bash
git add docs/superpowers/plans/2026-06-16-phase1-veha0-foundation.md
git commit -m "docs(plans): подрезка Вехи 0 под v3.1 — минус PowerSync/sync, плюс retry-буфер и entity_origins"
```
