# Отложенное: живые проверки до появления кредитов и решения владельца

Снято 2026-09-13 по леджерам срезов А (`2026-08-26-properties-reform-a`), Б-1 (`2026-09-02-properties-reform-b1`), V1, D42, Ш1 и
спеке реформы. **Одно место** для всего, что ждёт (а) кредитов у LLM-провайдеров и (б) слова владельца. При закрытии пункта —
вычёркивать здесь же, с датой и ссылкой на носитель.

Состояние на 2026-09-13: у **обоих** провайдеров кредитов нет с 2026-09-02 (OpenAI — HTTP 429 «You have no credits remaining»,
Anthropic — HTTP 400 «Your credit balance is too low»; логи в леджере Б-1 `logs/llm-smoke-*.log`). Следствие: **чат в проде отвечает
отказом провайдера**, рутины (садовник словаря) живьём не работают, все живые приёмки ниже не выполнены.

## 1. Когда появятся кредиты — порядок и носители

Ключи: прод — Render Environment (`sync: false`, runbook §2), локально — `apps/server/.env` (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`,
`ORBIS_LLM_PROVIDER`). Пополнение кредитов ключей не меняет — передеплой не нужен; ротация ключа — Render env + рестарт.
Все скрипты — из корня репозитория (`.env` подхватывается bun'ом), локальная БД поднята (`bun run db:prepare`).

| # | Проверка | Команда / сценарий | Ожидание | Откуда долг |
|---|---|---|---|---|
| 1 | **Живой смоук провайдера** — гейт «в прод только после живого смоука» | `ORBIS_SMOKE_OWNER_ID=<uuid локального владельца> bun scripts/llm-smoke.ts` (OpenAI по умолчанию); затем `ORBIS_LLM_PROVIDER=anthropic …` | EXIT 0: провайдер ПРИНЯЛ схемы 44 тулов (attach_*: 12) и ответил живым вызовом; `--dry-run` уже зелёный (09.09) | А §С8 (остаток №1), Б-1 задача 21 шаг 10 (В-П-5) |
| 2 | Предел имени тула у второго провайдера | в том же смоуке OpenAI: ключ аспекта `.max(64)` → имя тула ≤ 72 при пределе провайдера 128 | оба провайдера принимают самые длинные имена `attach_<аспект>`; отказ → сужать `.max` | Б-1 реестр остатков №75 (Ф-Б1-53в) |
| 3 | **П4 — блокирующая приёмка §С8-12 среза А** | `bun scripts/probe-p4.ts` — корпус П1 (20 сценариев, `docs/superpowers/specs/assets/2026-08-26-properties-reform/p1-tasks.json`) через `ai.sendMessage` тем же промптом и тулами, что чат владельца, плюс прогон садовника | EXIT 0 — замер состоялся, порог не превышен; 3 — порог первой цифры (`proposed`/день) превышен → Р14 сужается (решение названо в спеке заранее); 2 — замер не состоялся (нет провайдера / корпус не пройден); 1 — сломался. Вторая цифра (дубли) печатается списком пар, кода не выставляет (Р-17-5). Замер идёт на ТЕКУЩИХ промптах — v6 (Б-1, задача 17), а не v5, на которые рассчитывал срез А | А `acceptance-a.md` §12; `progress.md` А «Открытые вопросы владельцу» 1–2 |
| 4 | **§С8-18 живьём — гейт части Б «через AI»** | в чате прода (или локально с ключом): попросить AI завести пользовательский аспект только декларацией — финансовый (`implements` → `orbis/money-movement`, `orbis/completable`, `orbis/when`) и нефинансовый (`completable`, `when.moment`) — тулами `aspect_create`/`aspect_implements_set`; затем увидеть аспект в четырёх потребителях: ведомость `spent`, Повестка, строка списка, `excludeBlocked` | как фикстурный гейт `apps/server/test/gate-c8-18.test.ts` (4/4), но руками модели и без строки кода; по завершении — `aspect_implements_remove`/удалить пробный аспект | Б-1 рамка §«Кредитов нет» и Р2; `acceptance-b1.md` §18 |
| 5 | Прод-смоук через чат | сценарии задачи 21 шага 18 (Повестка, Бюджет, `class`-запрос) — те же вопросы AI в чате прода: «что по бюджету», «что просрочено», «открытые дела» | ответы через `budget_status`/`agenda`/`entity_query` с `class`; карточки чата с полями по слотам | Б-1 задача 21 (выполнено только MCP + экраны) |
| 6 | §С8-4 среза А — живая проба (запись 9a) | повторить сценарий записи через чат (создание сущности с аспектом по промпту) на текущем промпте | сущность создана через `entity_create` с валидными свойствами реестра | А рамка Б-1 «§С8-4/§С8-12 так и не закрыты живьём» |
| 7 | П3 — бюджеты слоёв промпта | пересъём токенов по слоям на промпте v6 (замер 25.08 снят на v4; на v5/v6 не переснимался) — носитель: `01-architecture.md:1077` «перезамер бюджетов промпта» | таблица бюджетов обновлена; вход для генератора промпта Б-3 | А остатки владельцу №9; рамка Б-1 (Б-3) |
| 8 | §С8-30 — П3-повтор на промпт-индексе | **только после среза Б-3** (генератор промпта + `property_catalog`-индекс §Б7-2): 12 сценариев × 2 модели | паритет с полным каталогом (34/36); регрессия → откат к «каталог аспектов + relevance» (решение названо заранее) | спека §С8-30; рамка Б-1 — состав Б-3 |
| 9 | Рутина «Садовник словаря» живьём | дождаться понедельничного прогона (09:00, `act`, `property_merge`) или запустить руками | отчёт в ленте прогона; карточки слияний в пачке решений | V1/А: прогон упирался в кредиты; долг «отчёт садовника не в треде рутины» — А задача 17 |

Порядок: 1 → 2 (в том же прогоне) → 3 → 4 → 5/6 → 9; 7 — когда удобно; 8 — после Б-3. Итоги пунктов 3 и 4 дописывать в
`acceptance-a.md` §12 и `acceptance-b1.md` §18 леджеров (и сюда).

## 2. Решения владельца (открытые)

### 2.1 Срез Б-1 — требуют вашего слова (леджер Б-1: `owner-questions.md`, `ledger/remainders-b1.md` §6, `progress.md` «Итог для владельца»)

| # | Вопрос | Что сделано без вас | Что решить |
|---|---|---|---|
| ОВ-Б1-1 | `test.failing` в Bun 1.2.7 работает только с синхронным телом (async внутри: красный красит сьют, зелёный молча проходит) | правило «новый `test.failing` — только синхронное тело» действует в коде | закрепить правило (CLAUDE.md / нормативы) или снять апгрейдом Bun |
| ОВ-Б1-2 | `daily_pace` вне активной фазы охраняется `remaining ≥ 0`; спека §Б5-4 пишет формулу без охраны | код — с охраной (Ф-Б1-34/40) | эррата таблицы §Б5-4 спеки |
| ОВ-Б1-3 | «порядок ключей = порядок вычисления» §Б5-3 невыполним: `jsonb` переупорядочивает ключи | движок считает по ссылкам `{agg}` топологически, фазы — взаимоисключение (Ф-Б1-37) | эррата §Б5-3; схема `phases` списком — в Б-2 |
| ОВ-Б1-4 | явное поле `alerts.of/against`: ассет спеки несёт `alerts.when`, схема подписки — нет | порог `alerts` в форме задачи 5 (Ф-Б1-38) | форма порога `alerts` — в Б-2 |
| В-П-2 | снести ли оракул `computeOverview` раньше Б-2 | оставлен рядом с движком (ИО-1) | до сноса нужен новый эталон перф-сверки §С8-15 |
| В-П-4 | индекс 0019 при провале порога 500 мс | не заводился: движок 271–330 мс против 500 | по числам не нужен; довод докблока про `relations_source_role` неверен на всех долях корпуса (остаток 87) — перевести пин в инвариант в Б-2 |
| 66 | единица пачки решений неодобряема после снятия слота-статуса пересевом | — | поведение пачки при смене реестра под ней |
| 67 | будущий механизм относится к гейту §С8-18 явно — правило в докблоке, не проверяется | — | ось гейта: «нет строки кода под аспект» или «под механизм» |
| хвост смоука | сущность «Смоук Б-1 (задача 21) — удалить» (`01a085f0-…`) в `cancelled`; карточка её архивации `pending_confirmation` ждёт в чате прода | — | подтвердить/отклонить карточку; при желании удалить сущность |
| PRD | окно Повестки — по слоту `start`, срок — только «Просрочено» (по спеке; задача «на сегодня» в окне не показывается) | смоук это подтвердил | сказать это явно в PRD, иначе читается как дефект |

Рулинги за вас (принять или перерешить): Ф-Б1-59…66 (`facts.md` Б-1) — в т.ч. Ф-Б1-61/65 (булевы узлы и `if` в SQL-бэкенде; плечо
`{const:null}` — отказ `EXPR_NOT_TOTAL` на записи во всех позициях E), Ф-Б1-62 (слот в `where` законен), Ф-Б1-63 (пустая дельта — не
ошибка), Ф-Б1-64 (хрупкий пин `volume.test.ts`), Ф-Б1-66 (дрейф `progress_source` через вложенную схему Q-AST). Реестр остатков —
87 строк: Б-2 — 30, Б-3 — 6, владельцу — 9.

### 2.2 Срез А — остались после 2026-09-02 (`progress.md` А «Остатки владельцу (полный список)»)

- П4 (§С8-12) — п. 3 таблицы выше; порядок замера — на текущих промптах.
- Разметка корпуса П1 (эталон дублей): без неё вторая цифра П4 остаётся списком без вердикта.
- `format: url` у `orbis/repo_url` — scp-адреса `git@host:owner/repo.git` больше не принимаются (ужесточение §А8) — оставить или расширить.
- В-4: `orbis/last_step_at` без `run_` (как в спеке §А8) — принять имя.
- Ветка `{text}` у `orbis/progress_source` — «дата смерти» наступила, решение вынесено владельцу.
- `undo_last` от модели отменяет и мутации реестра (восьмой путь) — Б-2 по рамке; подтвердить.
- `MERGE_DEPRECATED_TARGET` — код отказа сверх спеки (Р-24-9) — внести в §13/PRD при следующей ревизии.
- Сев мира безусловный (Р-24-6/11): сущность мира, снесённую физически, заход заведёт заново — оставить?
- Шесть долгов задачи 17 А (структурное поле исхода прогона; отчёт садовника не в треде рутины и др.) — кандидаты в Б-2/V2.

### 2.3 Старше (память сессий)

- **D42 «Пачка решений»**: Р4-1 (пре-чек строже executor'а по `orbis/assignment` — откат одна строка), Ф-8b (гашение обходит все прогоны рутины — секунды на старте полугодовой рутины), Ф-7c (битая pending-запись морозит подметание владельца), Ф-13a («Продолжить сейчас» скрыта при паузе, `runNow` работает).
- **Ш1**: решение по `LEAKPROOF`/миграции; разделение большого файла. (Доступ к Render у ассистента теперь есть — снято.)
- **V1 рутины**: мелкие долги по приоритетам North Star — «undo фразой» и правдивость карточек после отката (П2, кандидат на срез до V2).

### 2.4 Концепция «страницы и приложения» (спека `2026-09-11-pages-and-apps-concept.md`, ревизия 2 принята 13.09)

- В9 — состав стандартной библиотеки примитивов (список предложить и утвердить) — первый шаг при планировании срезов.
- Блоки в чате и уведомлениях — направление принято, детали отдельно.
- Дизайн-бриф: навигация на телефоне, форма присутствия хоста. Тулбар (§11) — отложен, не принят.
- В PRD не внесено — адреса правок в спеке.

### 2.5 Что дальше по реформе — выбор среза

- **Б-2** (по рамке): 21 отказ с мутационной проверкой (§С8-24), каталог правил, действия, `assign_level`/§С8-26, `undo_last` против реестра; вход — реестр остатков Б-1 §1/§4/§6, ОВ-Б1-2/3/4, остаток 87.
- **Б-3**: генератор промпта и индекс `property_catalog` (§Б7-2, затем §С8-30), экраны реестра §С4, экспорт §С5, web-половина выключения модуля Финансы (§С8-22), чат-карточки по слотам, перезамер бюджетов промпта.
