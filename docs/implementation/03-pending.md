# Отложенное: живые проверки до появления кредитов и вопросы владельцу

**Единственный реестр** всего, что ждёт (а) кредитов у LLM-провайдеров и (б) слова владельца. Заведён 2026-09-13 по леджерам
срезов А, Б-1, V1, D42, Ш1 и спекам. Правило: новый вопрос владельцу — только сюда, ссылкой на источник (леджер, спека, ревью);
леджеры и спеки — источники, не реестр. Закрытый пункт — вычёркивать здесь же, с датой и ссылкой на носитель.

Состояние на 2026-09-13: у **обоих** провайдеров кредитов нет с 2026-09-02 (OpenAI — HTTP 429 «You have no credits remaining»,
Anthropic — HTTP 400 «Your credit balance is too low»; логи в леджере Б-1 `logs/llm-smoke-*.log`). Следствие: **чат в проде отвечает
отказом провайдера**, рутины (садовник словаря) живьём не работают, все живые приёмки §1 не выполнены.

## 1. Когда появятся кредиты — порядок и носители

Ключи: прод — Render Environment (`sync: false`, runbook §2), локально — `apps/server/.env` (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`,
`ORBIS_LLM_PROVIDER`). Пополнение кредитов ключей не меняет — передеплой не нужен; ротация ключа — Render env + рестарт.
Все скрипты — из корня репозитория (`.env` подхватывается bun'ом), локальная БД поднята (`bun run db:prepare`).

| # | Проверка | Команда / сценарий | Ожидание | Откуда долг |
|---|---|---|---|---|
| 1 | **Живой смоук провайдера** — гейт «в прод только после живого смоука» | `ORBIS_SMOKE_OWNER_ID=<uuid локального владельца> bun scripts/llm-smoke.ts` (OpenAI по умолчанию); затем `ORBIS_LLM_PROVIDER=anthropic …` | EXIT 0: провайдер ПРИНЯЛ схемы тулов (число печатает скрипт; эталон реестра после Б-2 и среза страниц 1а — 51 тул, `attach_*`: 12 — `orbis/page` тула не получает, D45) и ответил живым вызовом; `--dry-run` уже зелёный (09.09) | А §С8 (остаток №1), Б-1 задача 21 шаг 10 (В-П-5) |
| 2 | Предел имени тула у второго провайдера | в том же смоуке OpenAI: ключ аспекта `.max(64)` → имя тула ≤ 72 при пределе провайдера 128 | оба провайдера принимают самые длинные имена `attach_<аспект>`; отказ → сужать `.max` | Б-1 реестр остатков №75 (Ф-Б1-53в) |
| 3 | **П4 — блокирующая приёмка §С8-12 среза А** | `bun scripts/probe-p4.ts` — корпус П1 (20 сценариев, `docs/superpowers/specs/assets/2026-08-26-properties-reform/p1-tasks.json`) через `ai.sendMessage` тем же промптом и тулами, что чат владельца, плюс прогон садовника | EXIT 0 — замер состоялся, порог не превышен; 3 — порог первой цифры (`proposed`/день) превышен → Р14 сужается (решение названо в спеке заранее); 2 — замер не состоялся (нет провайдера / корпус не пройден); 1 — сломался. Вторая цифра (дубли) печатается списком пар, кода не выставляет (Р-17-5). Замер идёт на ТЕКУЩИХ промптах — v7 после среза страниц 1а (индекс аспектов; до него — v6, Б-1 задача 17), а не v5, на которые рассчитывал срез А | А `acceptance-a.md` §12; `progress.md` А «Открытые вопросы владельцу» 1–2 |
| 4 | **§С8-18 живьём — гейт части Б «через AI»** | в чате прода (или локально с ключом): попросить AI завести пользовательский аспект только декларацией — финансовый (`implements` → `orbis/money-movement`, `orbis/completable`, `orbis/when`) и нефинансовый (`completable`, `when.moment`) — тулами `aspect_create`/`aspect_implements_set`; затем увидеть аспект в четырёх потребителях: ведомость `spent`, Повестка, строка списка, `excludeBlocked` | как фикстурный гейт `apps/server/test/gate-c8-18.test.ts` (4/4), но руками модели и без строки кода; по завершении — `aspect_implements_remove`/удалить пробный аспект | Б-1 рамка §«Кредитов нет» и Р2; `acceptance-b1.md` §18 |
| 5 | Прод-смоук через чат | сценарии задачи 21 шага 18 (Повестка, Бюджет, `class`-запрос) — те же вопросы AI в чате прода: «что по бюджету», «что просрочено», «открытые дела» | ответы через `budget_status`/`agenda`/`entity_query` с `class`; карточки чата с полями по слотам | Б-1 задача 21 (выполнено только MCP + экраны) |
| 6 | §С8-4 среза А — живая проба (запись 9a) | повторить сценарий записи через чат (создание сущности с аспектом по промпту) на текущем промпте | сущность создана через `entity_create` с валидными свойствами реестра | А рамка Б-1 «§С8-4/§С8-12 так и не закрыты живьём» |
| 7 | П3 — бюджеты слоёв промпта: **байты сняты срезом 1а, токены ждут кредитов** | байты — `bun scripts/prompt-size.ts` (сделано 24–25.09, задачи 3 и 17 среза 1а: слой 1 = 15 119 Б, из них индекс аспектов 2 005; слой 5 = 110 952 Б — 44 тула чата из 51); **токены** — `bun scripts/prompt-size.ts --tokens` (живой вызов провайдера, разность `inputTokens`; 24.09 — «no credits remaining») | токены слоёв 1 и 5 записаны в таблицу бюджетов `01-architecture.md` §7.1 рядом с байтами (прежние 3 055 / 5 521 токенов — v4, 25.08) | А остатки владельцу №9; рамка Б-1 (Б-3); спека 1а §10 п. 3, эррата Э-5 плана 1а |
| 8 | §С8-30 — П3-повтор на промпт-индексе: **стенд готов к прогону** (срез 1а, задача 3) | `bun scripts/probe-p3.ts --variant=index\|catalog --model=<id> --rep=N --out=<каталог> [id …]` по обеим моделям и обоим вариантам, затем `bun scripts/probe-p3.ts --report --out=<каталог>`; `--dry-run` зелёный. 12 сценариев П3 каналом чата × 2 модели; отдельная строка «В-6 (диагностика)» — канал `propose`-рутины без `ai_instructions` (вопрос В-6 плана 1а), в паритет не входит. Фильтр `contract` у `property_catalog` — срез 2 | паритет индекса с полным каталогом (34/36); регрессия → откат к «каталог аспектов + relevance» (решение названо заранее) | спека §С8-30; спека 1а §10 п. 4 |
| 9 | Рутина «Садовник словаря» живьём | дождаться понедельничного прогона (09:00, `act`, `property_merge`) или запустить руками | отчёт в ленте прогона; карточки слияний в пачке решений | V1/А: прогон упирался в кредиты; долг «отчёт садовника не в треде рутины» — А задача 17 |
| 10 | Б-2 живьём: действие от рутины и уровень по шагам в чате — **НЕ ВЫПОЛНЕН по кредитам** | (а) рутина «Перенос остатков» (включить, запустить руками первого числа): вызов `budget_rollover` из прогона — отложенная единица с карточкой строк; (б) в чате попросить «отложи просроченные задачи на пятницу» (`run_action` / `action_planner_postpone_overdue`) и «переведи план в факт» | (а) единица в пачке решений, конверты заводит только «Принять»; (б) уровень подтверждения — по резолвленным шагам (≤ 10 целей — `preview`, иначе подтверждение по масштабу), откат одним «отмени последнее» | Б-2 задача 20 (В-П-5 — смоук `--dry-run`) |
| 11 | Срез 1а живьём: чат на промпте v7 с индексом аспектов — **НЕ ВЫПОЛНЕН по кредитам** (прод-процедура 1а, задача 19 шаг 12) | в чате прода: «заведи цель…», «назначь агенту…» — модель находит аспект по строке индекса и зовёт `attach_*`; «покажи прогоны» — `entity_query` с `aspect=orbis/agent-run` (строка-граница служебных); «сделай страницу» — модель страниц не пишет (аспекта `orbis/page` нет ни в тулах, ни в индексе) | индекс достаточен без текстов инструкций; служебные не навешиваются; страница не создаётся моделью | план 1а задача 19; спека 1а §10 |

Порядок: 1 → 2 (в том же прогоне) → 3 → 4 → 5/6 → 9 → 10; 7 (токены) и 8 — одним заходом после выкатки 1а; 11 — после 8. Итоги пунктов 3 и 4 дописывать в
`acceptance-a.md` §12 и `acceptance-b1.md` §18 леджеров (и сюда).

**Приёмка среза Б-2 (§С8 пп. 24, 25, 26, 27, 29 и §С2-2) ключа НЕ требует** и закрыта без него: корпус отказов (21/21 двумя жанрами),
фикстуры каталога, одиннадцать правил делегирования на синтетическом сиде и golden действий — тесты. Единственное место среза,
упирающееся в кредиты, — живой смоук провайдера в прод-процедуре (задача 20): он идёт `--dry-run`, как в Б-1 (В-П-5); живые
сценарии среза — п. 10.

## 2. Вопросы владельцу

### 2.1 Сейчас (минуты)

| # | Вопрос | Действие |
|---|---|---|
| 1 | В чате прода висит карточка архивации сущности «Смоук Б-1 (задача 21) — удалить» (`01a085f0-…`, задача уже в `cancelled`) | подтвердить или отклонить карточку; при желании удалить сущность |
| 2 | Кредиты провайдеров — не решение, а действие; без них AI в проде мёртв | пополнить, когда AI понадобится; затем §1 по порядку |

### 2.2 При планировании срезов реформы — Б-2 спланирован рамкой 14–16.09 (решения В-1…В-11 — `specs/2026-09-14-properties-reform-b2-frame.md` §8; ревизия 4 спеки внесена 16.09); остальное — при планировании остатка бывшего Б-3

Код уже согласован с рулингами; спека/PRD правятся эрратой в момент планирования.

| # | Вопрос | Что сделано без вас | Что решить | Источник |
|---|---|---|---|---|
| Б-1 ОВ-1 | `test.failing` в Bun 1.2.7 работает только с синхронным телом | правило «новый `test.failing` — только синхронное тело» действует | закрепить в нормативах или снять апгрейдом Bun | леджер Б-1 `owner-questions.md`, Ф-Б1-15 |
| ~~Б-1 ОВ-2~~ | закрыто 16.09 ревизией 4: §Б5-4 несёт охрану `remaining ≥ 0` | — | — | спека §Б5-4 |
| Б-1 ОВ-3 | эррата §Б5-3 внесена ревизией 4 (топология `{agg}`, взаимоисключающие фазы); **`phases` списком** — не Б-2 (В-10 рамки), при ревизии схемы подписок. Срез Б-2 исполнен 24.09 — часть «схема `phases` списком» им не взята и остаётся у владельца | движок считает по ссылкам `{agg}` (Ф-Б1-37) | схема `phases` списком | спека §Б5-3 |
| ~~Б-1 ОВ-4~~ | закрыто 16.09 ревизией 4: форма порога `{warn_at, on_raw}` без `of/against` записана в §Б5-4 | — | — | спека §Б5-4 |
| ~~Б-1 В-П-2~~ | решено 16.09 (В-10 рамки Б-2): оракул сносится в Б-2 в порядке «golden-снимки движка → `rolloverPreview` на декларацию → снос» | — | — | рамка Б-2 Р-32 |
| ~~Б-1 В-П-4~~ | закрыто 23.09 задачей 2 среза Б-2 (Ф-Б2-16, рулинг 2-1): индекс не нужен; пин `relations_source_role` переведён в инвариант близнецов («под ролью обслуживает хотя бы один из двух, Seq Scan по `relations` нет») — вердикт решает карта видимости, а не доля корпуса (остаток 87) | — | — | леджер Б-2 `facts.md` Ф-Б2-16 |
| Б-1 66 | единица пачки неодобряема после снятия слота-статуса пересевом | — | поведение пачки при смене реестра под ней | там же |
| Б-1 67 | правило «механизм относится к гейту §С8-18» — в докблоке, не проверяется | — | ось гейта: «под аспект» или «под механизм» | там же |
| Б-1 PRD | окно Повестки — по слоту `start`, срок — только «Просрочено» (по спеке) | смоук подтвердил | сказать явно в PRD, иначе читается как дефект | `acceptance-b1.md` смоук |
| Б-1 рулинги | Ф-Б1-59…66 (булевы узлы/`if` в SQL, `{const:null}` → `EXPR_NOT_TOTAL`, слот в `where`, пустая дельта, хрупкий пин volume, дрейф `progress_source`) | приняты координатором | подтвердить или перерешить при ревизии спеки | леджер Б-1 `facts.md` |
| А | разметка корпуса П1 (эталон дублей для второй цифры П4) | вторая цифра печатается списком без вердикта | размечать или нет | леджер А «Остатки владельцу» |
| А | `format: url` у `orbis/repo_url` — scp-адреса больше не принимаются | ужесточение §А8 | оставить или расширить | там же |
| А | `orbis/last_step_at` без `run_` (В-4); ветка `{text}` у `progress_source` («дата смерти» наступила); `MERGE_DEPRECATED_TARGET` сверх спеки → §13/PRD; сев мира безусловный (Р-24-6/11); ~~`undo_last` от модели против реестра~~ (закрыто 23.09 задачей 8 среза Б-2: `undo_last` идёт через классификатор, Р-31); шесть долгов задачи 17 А (поле исхода прогона, отчёт садовника не в треде) | — | принять имена/поведение при ревизии | там же |
| ~~выбор среза~~ | решено 13.09 (§13 концепции страниц): **Б-2 ядро** → «страницы, срез 1» → срез 2; рамка Б-2 написана 14.09, решения В-1…В-11 приняты 14–16.09, ревизия 4 спеки внесена 16.09; дальше — план и исполнение; **24.09 — срез 1 разрезан на 1а «Страница и шаблон» и 1б «Оболочка и приложения»**, спека 1а принята (`specs/2026-09-23-pages-slice-1a-design.md`) | — | — | рамка Б-2 |
| Б-2 план | судьба режима рутины `propose` после D42: `orbis_propose` принимает только правки графа (`PROPOSAL_ALLOWED_TOOLS`), тулы модулей (`budget_rollover`) и `run_action` в предложение не заворачиваются; `act` + отложенная единица закрывает «предложить, не делать» шире | рутина «Перенос остатков» сеется в `act` с одним тулом, фон всегда откладывает (Р-К-38/39, В-П-7 принято 20.09) | оставить `propose` как есть, расширить на тулы модулей, или считать `act` с отложенной единицей его заменой — при работе над логикой агентов | леджер Б-2 `facts.md` Р-К-38, В-П-7 |
| ~~Б-2 В-П-1…8~~ | вопросы плана Б-2: ревизия 5 спеки, `$now`, `default(currency)`, правило 10, смоук, онбординг рутины, режим рутины, `waiting_for` | решены владельцем 16–20.09 (протокол — план `plans/2026-09-14-properties-reform-b2.md`, раздел «Решения владельца»); **ревизия 5 спеки внесена 20.09** (поле `undo`, событие `on_enter_class` двумя формами, `actor: 'routine'`, `progress_source` — данные, `batch_cap` без дефолта, §С8-15 без оракула, контракт `orbis/delegable` с `exclusive_classes` и запись классом, правило 10 на `orbis/routine` до V2); исполненные по умолчаниям — см. четыре строки ниже | — | спека реформы, ревизия 5 (шапка и §0.2-24…30) |
| ~~Б-2 В-П-2~~ | `$now` против `{prop:'orbis/updated_at'}` для штампа завершения | исполнено `updated_at` (Р-К-2): строка `task_completed_at` копирует момент записи однажды, при входе в класс; `$now` в язык не заведён — кандидат по принципу §Б3-2а (заявки: ведомость «идёт сейчас», правило «метка в будущем») | — | леджер Б-2 `facts.md` В-П-2 |
| ~~Б-2 В-П-3~~ | `default(orbis/currency)` строкой сида | переведён задачей 14: строка `envelope_currency_default` на `orbis/budget`; `dropStaleCarryover` остался кодом — именованный остаток | — | там же, В-П-3 |
| ~~Б-2 В-П-4~~ | правило 10 §Б4-5 на аспекте `orbis/routine` | исполнено в приёмке §С8-26 как упрощение (Р-К-20); честная форма «вызов тула запуска рутины актором-рутиной» — V2 (область `contract`), правится одна фикстура корпуса | — | там же, В-П-4 |
| ~~Б-2 В-П-6~~ | рутина «Перенос остатков» — онбордингом, не прод-командой | исполнено задачей 10: досев по PK в `seedOwner` при первом заходе владельца, рутина ложится на паузе (рулинг 10-1) | — | там же, В-П-6 |
| ~~остаток Б-3~~ | §С8-35 (прод-дефекты П3: дата в системном канале, «продолжения последними», дубль `ai_instructions`) | **закрыто:** пп. 1–2 — ещё `c04b17e` (26.08); п. 3 (дубль) — **срезом 1а, задача 1** (индекс аспектов вместо секции инструкций, промпт v7; в проде — с выкаткой 1а) | — | спека §С9 п. 5; спека 1а §10; D45 |
| остаток Б-3 (уточнён по §15 спеки 1а) | что осталось от бывшего Б-3 после 1а | из семи гардов В10 две проверки собранного промпта («инструкций аспектов — ноль», «ни слова о выключенных приложениях») исполнены срезом 1а (задача 1) | **пять гардов В10 из семи**, экспорт §С5, экраны реестра §С4, генерация перечней и чисел промпта (Б7-1, Б7-3) — когда планировать | спека 1а §10, §15 |

### 2.2б Срез Б-2 «ядро» — вопросы исполнения (задачи 0a–18, 22–24.09; код согласован с умолчаниями координатора, спека не тронута)

Каждая строка — рулинг, принятый без владельца, с ценой «если неверно». Перерешать — при планировании страниц (срезы 1а/1б) или
отдельным словом; полный перечень отложенного среза — реестр остатков `ledger/remainders-b2.md` леджера Б-2.

| # | Вопрос | Что сделано без вас | Что решить | Источник |
|---|---|---|---|---|
| Б-2 10-1 | Месячное расписание рутины «Перенос остатков»: у рутины нет «дня месяца» (`orbis/routine_days` — дни НЕДЕЛИ) | рутина сеется **на паузе**; ежедневный ход с проверкой «первое ли число» стоил бы ≈29 вызовов модели и прогонов в месяц — включает владелец. Цена: перенос не идёт сам, пока рутина выключена | завести месячное расписание (день месяца) или принять ручное включение | леджер Б-2 рулинг 10-1 (`progress.md`) |
| Б-2 10-1б | Read-тул предпросмотра переноса для модели | нет: рутина опирается на `budget_status` (остаток родителя уже включает детей — тело велит не переносить дважды), суммы проверяет владелец по карточке единицы; `budget_status` отдаёт и конверты произвольного периода и чужой валюты — `rolloverCreate` завёл бы преемника в валюте по умолчанию (300 USD → 300 ₽; две валюты одной категории → `duplicate_rollover_category`), страхуют пауза и карточка (реестр остатков Б-2, строка 42) | заводить ли тул предпросмотра | там же |
| Б-2 3-2 | Можно ли очистить свойство с умолчанием (`default`)? | умолчание подставляется на ЛЮБОЙ правке, где свойства нет, включая явный `unset` (паритет со снятым кодом валюты конверта). Цена: свойство с `default` очистить нельзя — вернётся той же операцией; у конверта без валюты посторонняя правка ещё и снимает перенос (`dropStaleCarryover` читает «валюты не было → стала» как смену четвёрки; практически недостижимо) | оставить «на трёх путях» или «только при создании» | рулинг 3-2; ревью 14 m4 |
| Б-2 7-1 | Действие из рутины: владелец сам вписывает `run_action`/`action_*` в `allowed_tools` | гейт двухлинейный и fail-closed: имя `run_action` (или `action_<key>`) — первая линия, тулы шагов — вторая (§Б6-2 «только если все шаги ∈ `allowed_tools`» — необходимое условие). Цена: рутине нужно перечислять и тул действия, и тулы его шагов | подтвердить или сузить до одной линии | рулинг 7-1; вторая линза задачи 7 |
| Б-2 M-1/M-2 | Карточка предпросмотра действия | уровень считается по резолвленным шагам, но отдельной карточки предпросмотра у действия нет: пакетное действие ≤ 10 целей от AI исполняется уровнем `preview` сразу, без карточки: в ленте остаётся только строка журнала «Действие «…»» — §7.10 «показать» для него не исполняется; мелкий перенос остатков из чата на том же уровне тоже исполняется сразу, но приносит информационную карточку `preview` со сводкой (`tools/dispatch.ts` ≈:559-562, фикс-раунд задачи 10, I-3). Цена: владелец видит сделанное, а не предлагаемое; для действия — даже без карточки, строкой журнала | нужна ли карточка предпросмотра у действия всегда (или с порога) | гейт 7 M-1, Fable 7 M-2; сомнение задачи 10 |
| Б-2 M-1б | Что считать «массовостью» действия — цели или операции (ряд «пачка» таблицы уровней §С2-1) | считаются ЦЕЛИ: одиночное действие из N шагов — `execute` при любом N; пакетное 10 целей × 10 шагов = 100 операций — `preview` без карточки, тогда как те же 100 операций через `batch_execute` — `explicit-confirmation`; кап 100 операций стоит только на `explicit` (и на отложке рутины, `actions/run.ts` ≈:144), и 10 целей × 11 шагов = 110 операций исполнятся одной транзакцией (голая пачка > 100 отвергается). Цена: многошаговое действие проходит мягче, чем голая пачка тех же операций | мерка — цели или операции; кап операций на любом уровне | Fable 7 M-1; Ф-Б2-18 |
| Б-2 M-5 | Гонка предусловия действия | предусловие проверяется на резолве и повторно на «Принять» (по сохранённым целям); между проверкой и записью — окно CAS: проигравший получает `CONFLICT precondition_failed` | достаточно ли, или держать замок на целях | Fable 7 M-5; задача 8 M3 |
| Б-2 8-2 | `undo_last`, чей откат архивирует (например, отмена создания) (Р-31) | отступление от буквы Р-31: `runUndoLast` считает `archives: false` — откат правки графа, в том числе отмена создания, остаётся молчаливым (бриф, В-8). Цена: откат, архивирующий созданные записи, не эскалируется | принять отступление или поднимать уровень отката по факту `archives` | рулинг задачи 8 (8-2) |
| Б-2 12-3 | Условная уникальность: `when` у `unique_among` | отвергается валидатором (`RULE_WHEN_UNSUPPORTED`): условие сделало бы вердикт зависимым от порядка записей; подмножество — областью-аспектом. Цена: владелец не может объявить «уникален среди удовлетворяющих условию» | симметричная семантика (частичный индекс) — расширение спеки | рулинг 12-3 |
| Б-2 14a-М3 | Встроенный тикет: обещанный выигрыш В-П-8 «правка данных без выкатки» не наступил | системная строка неизменяема; дельта не переименовывает и не снимает вариант статуса (только добавляет варианты и правит `classMap`), а все пять классов делегирования заняты встроенной картой — новый вариант владельца отнести в классы нельзя (`CLASS_NOT_EXCLUSIVE`), он законно остаётся вне состояний. Выигрыш есть для своих аспектов (срез страниц) и для пересева: значения живут в строках сида, а не в трёх файлах кода. Цена: «добавлю статус тикета — агенты его поймут» без релиза не работает | нужна ли правка встроенной карты делегирования дельтой (снятие/перенос варианта) | Fable 14а M-3 |
| Б-2 15-1 | Кто считается актором `'ai'` в правилах делегирования 1/4/5/8a и 10b | `'ai'` включает чат и рутины, но НЕ MCP-агента (`actorKind: 'agent'`); фикстуры приёмки §С8-26 написаны актором `'ai'` (понижение без актора валидатор отвергает, Р-27); живой конвейер правил не читает — V2. Цена, если неверно: при включении конвейера правила 1/4/5/8a обойдут MCP-агента, а 10b «`never` для не-владельца» его не запретит — он получит общий `explicit-confirmation` вместо `forbidden` | разделять ли актор `'agent'` в правилах делегирования (или `'ai'` ⊇ агент) — решить до включения живого конвейера | рулинг 15-1; сомнение задачи 15 |
| Б-2 15-ост | Членство в наборах Budget при двух аспектах, связывающих один слот разными свойствами | членство (`facts`/`plans`, окно конвертов) — OR по привязкам, класс — по рангу; при такой паре член и значение расходятся (будущий факт может тратить, R2). Встроенный сид так не связывает | правило разрешения двух привязок одного слота | остаток задачи 15 |
| Б-2 16-гр | Правило СВОЕЙ строки против нового системного при пересеве | сид строк владельца не пишет: побеждает первый по (носитель, id), своё молча мертво до жеста владельца; сид печатает заметку `ownRowConflicts`, но `ops check` её **не показывает** | показывать ли конфликт в `ops check` / владельцу в треде | Ф-Б2-28; задача 16 (граница, m-A) |
| Б-2 16-5 | Откат правила после `property_merge` | точечный откат снятия правила, записанного до слияния, возвращает декларацию с ПРЕЖНИМ адресом (общий закон отката Orbis «последний пишущий побеждает», докблок `undoMerge`) — и с финала Б-2 (E-3, Ф-Б2-32) валидатор записи такой адрес ОТВЕРГАЕТ: откат громко отказывает `RULE_UNKNOWN_PROPERTY cause:'merged'` (запинено `ops.test.ts`), выход — `rule_set` с адресом цели. Слияние, пока на источнике стоят правила, само отказывает `MERGE_SOURCE_RULES`. Цена: откат такого действия не исполняется, владелец заводит правило заново | переписывать адрес по `merged_into` при откате или принять громкий отказ | рулинг 16-5; ре-ревью 16 раунд 4, проба Q3; финал Б-2 E-3 |
| Б-2 16-М3 | `rule_set` не считает существующих нарушителей | новое `requires_when`/`unique_among` над старыми данными: запись-нарушитель отказывает на ЛЮБОЙ правке свойств — включая рутины и планировщик; выход есть (`rule_remove` идёт мимо движка), но карточка `rule_set` числа нарушителей не показывает | нужна ли проба «N записей нарушат» в карточке | Fable 16 M-3 (рулинг 16-1) |
| Б-2 14-М6 | Текст отказа правила памяти потерял выход «либо запишите фактом (`orbis/memory_kind: fact`)» | подсказку держат `aiInstructions` аспекта `orbis/memory`; текст отказа — шаблонный (строка `requires_when`), не тронут: он для владельца | вернуть ли подсказку в текст отказа | Fable 14 M-6 (рулинг 14-1/14-2) |
| Б-2 Ф-23 | Смена поведения без владельца: ребро `ticket` на цель без контракта `orbis/completable` — теперь отказ `INVARIANT relation_contract` | `target_contract` роли `ticket` заработал (спека §А4-3); существующие рёбра остаются, новые — отказ | принять | Ф-Б2-23 |
| Б-2 Ф-24 | Строки-носители параметров движков (`nearest_ancestor`, `materialize`, `rollover`, `mirror_relation`) неотключаемы | выключение отказывает: без включённой строки движок бросает сборку. Цена: «кнопки» §Б4-4 у них нет — только замена параметров релизом. Строка уникальности конверта `duplicate_envelope` носителем движка НЕ является и выключается законно: идентичность конверта (снятие переноса) от выключения и отключения не зависит — читается системная строка до дельт владельца (Ф-Б2-21; финал Б-2 E-1, Ф-Б2-31) | принять | Ф-Б2-24, `rules/carriers.ts` |
| Б-2 Ф-34 | «Принять» отложенной единицы действия при выключенном модуле | исполняется: §Б6-4 закрывает список условий «Устарело» (строки нет / `deprecated` / хеш разошёлся), маска §Б8-3 стоит на поверхностях вызова (`run_action`, тулы действий); шаги модульных сидов — `entity_update` существующих записей («правка — да»), появление аспекта модуля отвергнет исполнитель. Прецеденты: повторный `attach_*` в единице и ручка `budget.confirmPurchase` исполняются так же. Цена: единица, отложенная до выключения модуля, после «Принять» пишет журнал `type:'action', module:<выключенный>` | строгая семантика «выключил модуль — его отложенное не исполняется» — правка спеки (четвёртое условие §Б6-4 или фраза §Б8-3). Эскиз: в `approvePending` после `actionStateOf` — отказ `MODULE_DISABLED` без гашения единицы (после включения «Принять» работает); гасить на `module_set` нельзя («данные не трогаются», inverse модуля); то же решение нужно для `confirmPurchase` и повторного `attach_*` | финал Б-2 E-2 (опровергнута как дефект), Ф-Б2-34, `policy/pending.ts` `actionStateOf` |
| Б-2 T-запись | T-правило владельца (`default`, `on_enter_class.set`, `on_leave.unset`) пишет свойство-цель мимо `assertPropsWritable`: и вычисляемые (`orbis/parent_project`, `orbis/root_project`), и `system_writable` | по букве §А2-5 законно (механизм `rule` в обоих списках); проверок записываемости цели в `registry/rules.ts`/`rules/engine.ts` нет. Цена: `default` на `orbis/parent_project` спорит с пересчётом предков (`nearest_ancestor`) без отказа — `RULE_CONFLICT` писателя-движка не видит | запретить T-правилам владельца цели движков-носителей (и/или `system_writable`) либо принять | финал Б-2, наблюдение B2; реестр остатков Б-2 строка 119 |

### 2.2в Срез страниц 1а — вопросы исполнения (задачи 1–18, 24–25.09; код согласован с умолчаниями плана и рулингами координатора, спека не тронута)

Вопросы плана В-1…В-11 (`plans/2026-09-24-pages-slice-1a.md`, раздел «Вопросы владельцу») владелец до исполнения не
перерешал — исполнено по умолчаниям; ниже каждое с ценой. Строки «новое» — рулинги исполнения без владельца. Полный
перечень отложенного среза — реестр остатков `remainders-1a.md` леджера `.superpowers/sdd/2026-09-23-pages-slice-1/`.

| # | Вопрос | Что сделано без вас | Что решить | Источник |
|---|---|---|---|---|
| 1а В-1 | Как не предлагать аспект «страница» модели, если флаг `service` прячет записи из всех выдач | временный список кода `AUTHORING_DEFERRED_ASPECTS = ['orbis/page']`: нет `attach_orbis_page`, нет строки индекса. Цена: `ai_instructions` страницы мёртвое до среза 2 — модель аспекта не видит в тулах и индексе, но **видит** в выдачах записей и в `property_catalog`, а core-тулы (`entity_create`/`entity_update`) запись `aspects:['orbis/page']` и `orbis/template_for` технически примут; защищает только незнание аспекта | принять список до среза 2; либо `service: true` (страницы пропадут из выдач), либо дать модели `attach_orbis_page` | РП-1, Э-1; гейт задачи 4 |
| 1а В-2 | Окно простоя между пересевом и выкаткой | принято, как в Б-2; с рулингом Ф-1а-23 (мерж один раз, автодеплой не выключался) окно = время сборки Render. Цена: минуты 500 на всём, что читает реестр | вариант сократить окно (пуш до пересева) — решить в начале прод-процедуры | РП-2, Ф-1а-23; ранбук «Деплой среза 1а» |
| 1а В-3 | Блок данных читает `display` везде: преднастроенные списки сида (прежнее имя «смарт-листы», устар.) и тела проектов с `display=list` станут строками `EntityRow` | читается везде (один механизм). Цена: вид блоков в заметках меняется; числа — `ops.ts census-v3` владельцу до пересева (СТОП до слова) | посмотреть числа и принять, либо «в заметках всегда `compact`» | РП-5, Э-3 |
| ~~1а В-4~~ | выкатить индекс аспектов отдельно сразу после задачи 1 | нет — вместе со срезом; снято рулингом владельца 24.09 «нет смысла пушить в `main`, пока срез не готов» (Ф-1а-23) | — | Ф-1а-23 |
| 1а В-5 | Остатки Б-2 с адресатом «страницы-1» (14 строк `remainders-b2.md`) | 1а закрыл №39 (правдивые комментарии экрана назначения, задачи 12, 14) и по коду №79 (индекс, перезамер байтов, стенд §С8-30 — задачи 1, 3, 17; живые прогоны — §1 пп. 7–8); **12 строк → 1б** (№76 — 1б/срез 2, №77 — по Р-13 спеки 1а срез 2) — §2.3.1 ниже | подтвердить перенос | РП-19 |
| 1а В-6 | Рутина в режиме `propose` после индекса не видит `ai_instructions` нигде (её тулов `attach_*` нет) | принято: канал получает индекс (id, подпись, описание). Риск меряется: стенд §С8-30 несёт отдельную строку «В-6 (диагностика)» — законное предложение рутины каналом `propose` (вне паритета) | по итогу живого прогона §1 п. 8: оставить, или вернуть инструкции в канал `propose`-рутины | Ф-1а-13, Э-6; рулинг задачи 3 (б′) |
| 1а В-7 | Токены промпта требуют живых вызовов провайдера | байты — сделаны; токены — §1 п. 7 | — | РП-17, Э-5 |
| 1а В-8 | Инструкция служебного `orbis/agent-run` («вручную не создавай и не правь») после индекса не доходит до модели | строка-граница индекса «Служебные — не навешивай и не правь сам: orbis/agent-run (в выдачах их нет — запрашивай явно aspect=<id>)»; суффикс вернул подсказку, ушедшую с инструкцией (гейт задачи 1, I2). Цена: ≈60 байт промпта | принять | РП-26; рулинг задачи 1 |
| 1а В-9 | Спека §6.4 «нет `{{backlinks}}` — обратные ссылки не грузятся» | один `entity.get` экрана с сегодняшними включениями для любой записи и страницы: шаблон известен только после чтения записи, сужение стоило бы второго запроса на каждое открытие. Цена: лишние обратные ссылки и тред у страниц, которым они не нужны (≈20 мс сервера) | принять до выбора шаблона на сервере | РП-13, Э-14 |
| 1а В-10 | Формы проекции отличаются от Р-10 (`columns=a\|b`, `aggregate=sum:x`, голый `hide_empty`) | исполнено формами РП-4: скобки в грамматике зарезервированы за печатью невыразимого дерева | принять (эррата Э-2 спеки) | РП-4, Э-2 |
| 1а В-11 | Эрраты, меняющие смысл приёмки: С1а-6 — четыре сторожа вместо «существующего перф-гейта web» (его нет); С1а-8 случай 1 — копия шаблона без `{{body}}` | исполнено; итог по весу: чанк экрана записи 27,5 кБ gzip против базы 30 338 Б (меню «⋯» — ленивый чанк по нажатию) | принять эрраты Э-7, Э-13 ревизией спеки | Э-7, Э-13; задача 14 |
| 1а новое-1 | Тела «с документом и маркером в body» (перепись `census-v3`, группа `markerInBodyWithDoc`) | ничего: решение — на прод-процедуре (ранбук, шаг (3)). Внимание: `audit-bodies` после выкатки пометит v2-строки с абзацем вида `{{title}}` как «пара разошлась» (печать v3 экранирует такой абзац) | перепечатать `body` из `body_doc` (абзац остаётся абзацем), перепарсить (станет блоком) или оставить | рулинг F2 задачи 8; гейт задачи 8 |
| 1а новое-2 | Слой 5 промпта — 110 952 байт на 44 тулах чата (25.08 было 5 521 токен на 31 туле) | записано в 01-architecture §7.1; главный вклад — `$defs` Q-AST в `attach_orbis_goal` (≈12 КБ), `entity_query`, `property_create`/`property_update`, `rule_set`; проекция блока данных (1а, задача 6) добавила 6,2 % | принять, или сужать схемы (подгрузка тулов по релевантности, общий `$defs`) — после живого замера токенов | задачи 3, 6 среза 1а |
| 1а новое-3 | Потерянная правка свойств между устройствами | CAS (`expectedUpdatedAt`) сверяется только у правки тела; выбор в споре шаблонов («Главнее, чем») и прочие `props` с двух устройств — «последний пишущий побеждает». Цена: выбор в споре может быть молча затёрт параллельной правкой | нужна ли версия и у правки свойств | Fable задачи 14 |
| 1а новое-4 | Подпись вкладки при настройке не правится (и markdown-правки у страниц нет) | «Вкладка 1» после вставки меню «/» не переименовать; сломанный шаблон при настройке — неразобранный кусок только удаляется. Цена: переименование — пересозданием вкладки | правка подписи в настройке — 1а-хвост или 1б | гейт задачи 16, m-3 |
| 1а новое-5 | Предпросмотр шаблона на чужой записи | тело выбранной записи — только чтение (§9.3), но заголовок, теги, подзадачи и тред той записи в предпросмотре правятся; финальное ревью дополнило: из предпросмотра доступны «Прогнать сейчас» и пауза рутины, ответ исполнителю, откат прогона, новый тред у чужой записи. Цена: владелец может случайно поправить чужую запись, глядя на шаблон | запретить правку обвязки в предпросмотре целиком или принять | гейт задачи 16, m-5 |
| 1а новое-6 | `in` над отсутствующим списком бросает `EXPR_VALUE`, а `empty` считает отсутствие пустым | правила страницы обходят охраной `and(not(empty(…)), in(…))`. Цена: несоответствие тотальности §Б3-4 — ловушка для правил владельца | исправить семантику `in` (не расширение языка) или записать в §Б3-4 | гейт задачи 4 (eval.ts) |
| 1а новое-7 | Флаки полного прогона | таймауты `routers/routine.test.ts` и (финал 25.09) `registry/ops.test.ts` (и изредка `TransactionsScreen`, `import.test`) под нагрузкой — задачи 1, 4, 5 и далее, по отдельности зелёные; `test:perf:graph` `descendants_of:subtree` p95 > 100 мс дважды (107, 119; сервер не тронут) | наблюдение; поднимать ли пороги/таймауты — решить при повторе в CI | задачи 1, 4, 5, 10, 14 |
| 1а новое-8 | `registry.effective` уходит дважды на холодном старте web | дефект до среза, не тронут: первый запрос реестра идёт под ключом `['registry', null]`, второй — под версией из `entity.get`. Цена: лишний запрос реестра на каждом холодном открытии; у владельца со своими шаблонами экран записи до прихода реестра стоит скелетом (последовательный круг сети, гейт задачи 14 m8) | чинить ли в 1б (один ключ кеша реестра) | гейт и Fable задачи 14 |
| 1а новое-9 | Узлы страницы (колонки, вкладки, блоки записи, карточки) в схеме документа — `group: 'block'`: цитата, пункт списка, ячейка их пускают | финальное ревью (A-I1/C2-I1): одно правило места `layoutMisplaced` (`shared/doc/placement.ts`) + страж транзакций редактора — жест, дающий неверное место или глубину, отменяется; меню «/» на том же правиле. Схема НЕ сужена. Цена: недопустимая вставка молча отменяется, а не переставляется; документ с неверным местом, присланный мимо редактора, сохраняется целиком `rawBlock` | сузить схему группой `pageBlock` (разрешена только в doc/column/tab) — в 1б, пока v3 не разошёлся по данным? | финальное ревью 1а |
| 1а новое-10 | Агент с грантом `worker` через tRPC читает весь граф (`entity.query`, теперь и `entity.blocks`) | не тронуто: сужение `scope: 'worker'` действует только в диспатче MCP; дыра была до среза, срез добавил вторую дверь с тем же поведением. Цена: исполнитель по PAT видит записи вне своей задачи | сузить на tRPC (обе процедуры) — когда? | финальное ревью 1а B-M4 |
| 1а новое-11 | Два `{{cards}}` или два одинаковых `{{card: X}}` в шаблоне/на странице рисуют карточки дважды (у каждой копии своё состояние: два поля ответа исполнителю, две «Прогнать сейчас») | не тронуто: спека ограничивает только второй `{{body}}` (§5.3) | плашка «второй», как у тела, или «так задумано»? | финальное ревью 1а C2-M2 |
| 1а новое-12 | Плитка `aggregate=latest:` денежного свойства печатается без валюты (у `sum` валюта есть, §7.2) | не сделано: ответ `entity.blocks` для `latest` не несёт валюты — нужно расширять провод | расширить провод в 1б? | финальное ревью 1а C1-M4 |

### 2.2а Единица владения — граф: спека `specs/2026-09-19-graph-ownership-unit-design.md`, ревизия 2.1 от 20.09 (направление принято 19.09; ревью и ре-ревью Fable 20.09 — реестры находок в §10 спеки; вердикт ре-ревью «после точечных правок», правки внесены). **Срез Г исполнен 21.09.2026 (миграции `0019`–`0021`, D44 записан); в проде с 21.09.2026**

| # | Вопрос | Статус / умолчание | Где перерешать |
|---|---|---|---|
| ~~В-Г-1~~ | имя колонки ключа владения | **решено 20.09: `owner_id` → `graph_id`** («`owner_id` сбивает с толку»); D44 заменяет п. 1 чек-листа D11 | спека §3.4 |
| ~~В-Г-2~~ | членство людей — своя таблица `graph_members`, не семья `agent_grants` | решено 20.09 (умолчание) | спека §3.2 |
| ~~В-Г-3~~ | один личный граф на аккаунт — инвариант | решено 20.09 (умолчание); держит `CHECK ((owner_kind = 'person' AND id = owner_ref) OR owner_kind = 'organization')` | спека §3.1 |
| ~~В-Г-4~~ | субъект entitlements и расхода | решено 20.09 (умолчание): резолвер субъекта уже принимает (D11 п. 5), расход на граф | спека §3.4 |
| ~~В-Г-5~~ | термины | решено 20.09, включая два слова ревизии 2 — «текущий граф» и «держатель гранта» | спека §2 |
| ~~В-Г-6~~ | место работы | **решено 20.09 (после ревью): отдельный мини-срез «Г — единица владения» ДО Б-2**; прежнее умолчание «задача 0f» снято — задач пять, и в 0-серии Б-2 они упирались в Р-18 и Р-23 плана | спека §8 |
| ~~В-Г-7~~, ~~В-Г-8~~ | исключения из Р-18 и Р-23 плана Б-2 | сняты решением В-Г-6 | — |
| ~~В-Г-9~~ | миссия: анти-цель 4 и чтение анти-цели 1 в графе компании | **решено 20.09: слова §7 спеки приняты владельцем**; вносятся в `05-mission` задачей доков среза Г одним заходом с D44 | спека §7 |
| ~~В-Г-10~~ | окно простоя прода при выкатке среза Г | **решено 20.09: принято владельцем** (минуты; планировщик рутин на это время выключен, версия клиента поднимается, до прода — репетиция на бэкапе) | спека §8.1 Г-5 |
| ступень 2 | общий глобальный тред графа компании при ассистенте «на аккаунт» смешал бы истории держателей грантов | не предрешается сейчас | спека §3.4 |
| ступень 2 | `user_settings` смешивает настройки аккаунта (`timezone`, `plan`) и графа (`disabled_modules`, `registry_version`): таймзона рутины в графе компании окажется таймзоной графа, а не человека | долг записан, разводится на ступени 2 | спека §3.4, PRD 01 §4.4 |
| владельцу, при планировании (правка 21.09, финальное ревью ветки) | формулы `uuidv5` (глобальный тред, треды сущностей, сид-сущности мира, садовник, audit-id пачек) стоят на ключе графа: после перепривязки владельца на новый аккаунт id остаются посчитанными от СТАРОГО ключа. Это **не** «допустимое следствие», как говорилось раньше: глобальный чат и ранее открытые треды сущностей дают **500** (`ensureThread` вставляет без цели конфликта и читает по новому id → пусто → `throw`; проверено на живой базе), а первый заход нового аккаунта **дублирует** 18 сущностей мира и садовника | для тредов рабочий путь есть и прогнан (перекей `uuid_generate_v5` в той же транзакции — ранбук §4.3); для мира и садовника пути **нет**: перекей сущности задевает пять ссылающихся таблиц, пины `user_settings` и id внутри `chat_messages.metadata` — отдельная операция со своей репетицией | `02-ops-runbook.md` §4.3 |
| владельцу, при планировании (правка 21.09, финальное ревью ветки) | каталог `scripts/` (16 файлов `.ts`, среди них прод-обёртка `ops.ts`, `issue-pat.ts`, `setup-db.ts`, `test-rls.ts`) **не входит ни в один tsconfig** и не покрыт `bun run typecheck`: корневой скрипт это `bun run --filter '*' typecheck`, то есть только workspaces (`packages/*`, `apps/*`), а `scripts/` не workspace. Измерено: голый литерал `const forged: Identity = {…}` в `scripts/` проходит и компилятор (EXIT=0), и греп-гейт, и исполняется. То есть замок типа пары, брендовые приведения и вообще любые типовые гарантии в `scripts/` НЕ ДЕЙСТВУЮТ — а `ops.ts issue-pat` пишет строки `agent_grants` на проде | дыра шире среза и была до него: срез Г закрыл её ПОВЕДЕНЧЕСКИ (рантайм-барьер `isIdentity` в `withIdentity` отбивает такую пару — проверено мутацией прямо в `scripts/`), но типовой проверки там по-прежнему нет. Починка — свой `tsconfig.json` для `scripts/` плюс шаг `typecheck` в корне и CI; число всплывающих ошибок неизвестно, поэтому это отдельная работа | `scripts/`, корневой `package.json` |
| ступень 2 (правка 21.09, Ф-Г-28) | запрос в граф, где у актора нет гранта, отдаёт ноль строк, но платит сканом по строкам ЧУЖОГО графа: security-qual RLS в гейтовый `One-Time Filter` Postgres не поднимает — тайминговый канал размера чужого графа для того, кто умеет назвать чужой `graph_id` | в v1 путь недостижим: текущий граф выставляют резолверы сервера, клиент его не называет. Формой политики не лечится — нужен отказ ДО запроса | спека §3.6 |
| ступень 2 | `observer` читает `agent_grants` текущего графа наравне с `owner` (`access_hash` — хеш, сырых токенов схема не хранит) | по плану среза; устраивает ли это на ступени 2 — вопрос владельцу | PRD 01 §4.10 |
| мелкое (гейт-ревью Г-4) | у `orbis_app` остались DML-гранты на `entity_versions` (`0011:41`) и `envelope_spent_cache` (`0018:41`), которыми никто не пользуется: политик `TO orbis_app` у этих таблиц нет, значит права мёртвые | снимать ли отдельной миграцией | `02-ops-runbook.md` §4.3, блок «Про права» |
| мелкое (Ф-Г-62/Ф-Г-76) | вердикт `relations_source_role` качается между двумя взаимозаменяемыми индексами рекурсивного обхода (ряд из 7 точек): сьют зелёный, в seqscan обход не уходит, но детерминизма у выбора нет | пин оставлен как есть | `02-ops-runbook.md` §8.1 |
| мелкое (Ф-Г-68) | выигрыш `plpgsql` у четырёх функций политик держится на кеше планов **в сессии**: соединения живут в пуле. Переход сервера на соединение-на-запрос вернёт цену перепланирования | пересмотреть, если модель соединений сменится | `0021_graph_rls.sql`, шапка |
| владельцу, при планировании страниц (правка 21.09, Р-ИГ-7) | экран согласия OAuth рендерится ВНЕ `OnboardingGate` (`apps/web/src/main.tsx:29-44`), поэтому аккаунт может дойти до согласия раньше, чем у него появится личный граф; срез отвечает типизированным отказом (`NotGraphOwnerError` → `FORBIDDEN`, «граф не заведён») | правильный ли это ответ продуктово — «сначала онбординг» или «завести граф прямо на согласии» — решает владелец; `apps/web` срез не трогает | концепция страниц §13 |
| владельцу, при планировании среза Г-next | составные имена «на граф» без `Id` (`ownerVersion`, `bumpOwnerRegistryVersion`, `ownerTimeZone`, `ownerSets`, `lockOwnerRegistry`, `lockOwnerBudget`, `ownerCategories`, `seedOwnerWorld`…) и голое `owner` в тестах — переименованы **не** были (полный список — `rename-ledger.md`) | переименовывать ли их отдельным проходом | леджер среза Г, `rename-ledger.md` |
| владельцу, ревизией 5 реформы свойств | §Б3-5: `$owner` языка E — это id **ТЕКУЩЕГО ГРАФА**, а не аккаунта (в коде исполнено Г-3: `CompileCtx.graphId`). Имя узла языка E не меняется — язык правит только владелец | внести смысл в §Б3-5 при ревизии 5 | спека реформы §Б3-5 |

### 2.3 При планировании концепции «страницы и приложения» (спека `2026-09-11-pages-and-apps-concept.md`, ревизия 2 принята 13.09; срез 1 → 1а/1б, спека 1а принята 24.09 — `specs/2026-09-23-pages-slice-1a-design.md`)

| # | Вопрос | Источник |
|---|---|---|
| ~~В9~~ | состав стандартной библиотеки примитивов первого состава | **решено 23.09** — спека 1а §7 (Р-13) | спека §12; спека 1а §7 |
| В8+ | блоки в чате и уведомлениях: как карточки чата становятся блоками, что с ними без сети — **срез 2** (спека 1а §15) | §12 |
| бриф | навигация на телефоне (шапка / меню / домашняя); форма присутствия хоста; экран установки по манифесту; запоминать ли последнее значение параметра — **к спеке 1б** (сегодняшний нижний `TabBar` нарушает «навигация никогда не внизу») | §12; спека 1а §11.2 |
| уровень приложения | «проект» и «задача» — один аспект с иерархией или два | §12 |
| ~~PRD~~ | правки §14 спеки (глоссарий, §10, §4.16/§11.4, §2.3/§3.4, решение, §2 миссии) — **минимум внесён 25.09 задачей 17 среза 1а**: 02-core-os §3.4, §3.5, §3.10; глоссарий 00-product («Страница», «Блок», «Шаблон», «Смарт-лист» — устар.); **D45**; 01-architecture §3.18, §6.1, §7.1, §7.6, §9.1. Остальные адреса §14 концепции — 1б | §14; спека 1а §16 |
| тулбар | §11 — грубые наброски, не принято | §11 |
| ~~спека 22.08~~ | «Разделы-сущности и трансклюзия» — **решено 23.09 (Р-4 спеки 1а):** не в 1а; концепцией НЕ поглощена (§6.4 — страница со `this`, глубина ≤ 2; черновик — тело любой записи, глубина 1, «вынести в запись»); остаётся входом: позже один механизм `{{embed}}` для страниц и записей + «вынести в запись», переписанный под словарь концепции | спека 1а §15 |
| ~~ревизия концепции~~ | §6.2 — правило выбора шаблона (больший набор → выбор владельца, запомненный для спора; шаблон хоста только если подходящих нет — перерешает «несколько — шаблон хоста»); §13 — срез 1 разрезан на 1а/1б; §14 — номер решения | **внесено 24.09** — ревизия 3 концепции (§1, §3.4, §5.3, §6.2, §9, §10, §12, §13, §14) | спека 1а §4.2, §16 |
| ~~ревизия спеки реформы~~ | §С9 п. 5 — состав среза 1а вместо «среза 1» | **внесено 24.09** — ревизия 6 спеки реформы (§С9 п. 5, сноска §Б7-2, суперсид §0.2-31) | спека 1а §16 |
| **не забыть** (спека 1а §15) | **1б:** обратные ссылки как запрос («кто ссылается на `this`»), группировка списка, параметр «период», прогресс/пороги, правка шаблона хоста (эталон), записи со списками по запросу (прежнее имя «смарт-листы», устар.) → страницы, модульные гейты web — «12 точек»: 3 закрыты (`installedViews`), 7 открыты — 1б; **срез 2:** чекбокс в строках списка на странице (действием), перевод существующей правки экрана записи на действия, фильтр `contract` у `property_catalog`, аспект «страница» — обычный; **позже:** встраивание, доска/календарь/графики, колонки в редакторе заметок | полный перечень — спека 1а §15 |

#### 2.3.1 Перенесено из 1а (спека 1а §15)

| Куда | Что |
|---|---|
| **1б** | адреса с приложением; оболочка и навигация из размещений, **не внизу на телефоне**; манифест данными; Бюджет и Повестка как приложения; выключение приложений и 7 открытых гейтов web; чипы Browser по контрактам; правка шаблона хоста (копия с эталоном, «вернуть как было»); записи со списками по запросу (прежнее имя «смарт-листы», устар.) → страницы; счётчики закреплённых; параметр «период» и токены периодов; **обратные ссылки как запрос** («кто ссылается на `this`» — первый потребитель: транзакции категории; сначала проверить существующее `via=`); **группировка списка** (первый потребитель: Повестка по дням); прогресс и пороги на блоке; слова кода вне словаря; правило §4.2 для адресов без приложения |
| **1б — остатки Б-2 «страницы-1»** | 12 строк `remainders-b2.md` леджера Б-2: №70 (web-копии `unset waiting_for`), №71 (клиентский штамп `completed_at`), №72 (комментарии с адресами снесённого кода в `TransactionsScreen`), №73 (старый текст отказа в фикстуре `EnvelopeCard.test.tsx`), №74 (`ACTION_STALE` как «уже отклонено»), №75 (`asLegacyRefusal` у `budget.confirmPurchase`), №76 (`preview`-карточка многошагового действия — 1б/срез 2), №77 (переключение статуса строкой списка — по Р-13 спеки 1а срез 2), №78 (экранные остатки Б-1), №98 (возврат из закрытия литералом `'inbox'`), №100 (`orbis/all_day` сырым чтением), №117 (провод Budget на классы контракта). №39 и №79 закрыты срезом 1а |
| **1б — из исполнения 1а** | свои карточки аспектов — код хоста (`OWN_ASPECT_CARDS`, пять аспектов; финансовая — кандидат в Бюджет); нижний `TabBar`; правка шаблона хоста; счётчики закреплённых — второй путь данных; «тикет + рутина» — история прогонов на экране дважды (функция намеренных отличий `INTENDED_1A` относит второй показ к карточке рутины) |
| **срез 2** | формы и действия на страницах, **включая чекбокс в строках списка**; перевод существующей правки экрана записи на действия; авторство страниц агентом и промпт авторства; фильтр `contract` в `property_catalog`; аспект «страница» становится обычным (`attach_*`, снятие `AUTHORING_DEFERRED_ASPECTS`); карточки чата как блоки (`keyFields`); параметры в интерфейсе |
| **позже** | встраивание страниц и записей одним механизмом `{{embed}}` + «вынести в запись» (вход — черновик `specs/2026-08-22-section-entities-transclusion-design.md`, переписать под словарь, Р-4); доска, календарь, графики; ярус 2 и песочница; настоящие колонки в редакторе заметок; «Вернуть вид по шаблону» |
| **остаток Б-3** | пять гардов В10 из семи, экспорт §С5, экраны реестра §С4, генерация перечней и чисел промпта (Б7-1, Б7-3) |

### 2.4 Мелкое из прежних срезов (решения, не работа; можно не трогать)

- **D42 «Пачка решений»**: Р4-1 (пре-чек строже executor'а по `orbis/assignment`, откат — одна строка), Ф-8b (гашение обходит все прогоны рутины), Ф-7c (битая pending-запись морозит подметание владельца), Ф-13a («Продолжить сейчас» скрыта при паузе, `runNow` работает).
- **Ш1**: решение по `LEAKPROOF`/миграции; разделение большого файла.
- **V1 рутины**: минорные долги по приоритетам North Star (`final-minors.md` леджера V1) — «undo фразой» и правдивость карточек после отката (П2, кандидат на срез до V2).

### 2.5 Где вопросы уже закрыты (не искать там)

Рамка Б-2 `specs/2026-09-14-properties-reform-b2-frame.md` §8 — В-1…В-11 решены владельцем 14–16.09.2026 (язык E: принцип расширения, `empty`, Е-5 `$touched`; понижение с полом; Е-6 остатком; `rollover` два дома + `postpone_overdue`; дефолт даты на клиенте; тулы записи + дельты правил; «модуль» до среза 1; `undo_last` через классификатор; кап 100; оракул сносится, таймзона в Б-2). Разделы «Вопросы владельцу» в спеках и планах реформы (`2026-08-26-properties-reform-design.md` §«Вопросы владельцу» — «вопросов не осталось»; рамка Б-1 §8 — В-1…В-9 решены 02.09; планы А/Б-1/V1/Ш1/D42 — «по умолчанию»), заметки 23.08 (`properties-model-notes` В1 «аспект/роль», `property-ref-type-notes` В1) — закрыты D43; бэклоги слайсов 3c/3e — закрыты закрытием роадмапа 06.08; `01-phase0-findings.md` — веха 0.
