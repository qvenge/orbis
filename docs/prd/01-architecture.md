# Orbis PRD v3 — 01: Архитектура

| Поле | Значение |
|---|---|
| Версия | 3.0 |
| Дата | 2026-06-13 |
| Статус | Draft |
| Родитель | 00-product.md |

Этот документ — единственный источник истины по модели данных, схемам аспектов, таблицам БД, sync-протоколу, query-движку, AI-слою, entitlements и публичному контракту тулов. Документы 00 (продукт), 02 (Core OS) и 03 (Budget) ссылаются на него и не дублируют его содержание.

---

## §1. Обзор и принципы

> v2.2 — предыдущая редакция PRD (сохранена в истории git); её разбор — в дизайн-спеке `docs/superpowers/specs/2026-06-12-orbis-prd-v3-design.md`.

### §1.1 Технологический стек

| Слой | Технология |
|---|---|
| Runtime | Bun + TypeScript |
| API | tRPC |
| ORM | Drizzle |
| БД | PostgreSQL (Supabase) + RLS |
| Auth | Supabase Auth |
| Frontend | Vite + React PWA |
| State | Zustand |
| Local DB | IndexedDB |
| AI | LLM-agnostic слой, Claude по умолчанию |
| Внешние агенты | MCP |

### §1.2 Граница документа

PRD фиксирует **контракты и поведение** — схемы данных, протоколы, грамматику запросов, реестр тулов, инварианты, — но **не реализацию**. Структура кода, выбор библиотек, сигнатуры процедур, организация модулей живут в implementation-планах фаз и в самом коде. Если деталь не влияет на наблюдаемое поведение системы или на совместимость данных — её здесь нет намеренно.

### §1.3 Сквозные принципы

**Принцип 1: данные существуют до views.** Сущности и их связи — первичны; views (Budget, Agenda, Browser, будущие Fitness и Habits) — линзы поверх одного графа. Удаление или замена view не теряет ни одной записи: расход, заведённый через Budget, останется сущностью с `orbis/financial` и будет виден в Browser и в любых будущих views. Следствие для проектирования: ни один view не имеет собственного хранилища данных — только настройки отображения (`user_settings.viewPreferences`).

**Принцип 2: единый query-движок.** Одна грамматика запросов (§6), один движок с двумя бэкендами (SQL на сервере, JS поверх IndexedDB на клиенте) и шесть потребителей — от `{{query:...}}`-блоков в body до AI-тула `entity_query`; любая новая фильтрующая возможность добавляется в грамматику один раз и автоматически доступна всем потребителям. Полный список потребителей — §6.3.

**Принцип 3: правила слоёв сущности.**

- **meta — только AI-извлечённое** до структурирования в аспекты. Никакого application state: координаты досок, настройки отображения, кэши — не сюда. Исключение — узкий набор служебных ключей с зарезервированными именами (`raw_input` — исходная фраза ввода, `import_id` — дедуп-ключ CSV-импорта, §4.1, 03-budget §3.4.1): они не являются ни структурой аспекта, ни application state и под соглашение §3.9 не подпадают.
- **body — текст и живые запросы.** Markdown, ссылки на сущности, `{{query:...}}`-блоки. Smart lists — это сущности с query-блоками в body, а не отдельный механизм.
- **aspects — всё типизированное**, включая данные views: всё, что имеет схему в реестре, живёт в aspects и валидируется по этой схеме.

---

## §2. Модель данных: сущность

### §2.1 Четыре слоя

Сущность — универсальная единица графа. Все домены (задачи, финансы, заметки, бюджеты, память AI) выражаются одной структурой из четырёх слоёв:

```
┌──────────────────────────────────────────────────────────────────┐
│  СУЩНОСТЬ                                                        │
│                                                                  │
│  ┌ 1. Core ────────────────────────────────────────────────────┐ │
│  │  id (UUIDv7, генерируется клиентом), title, emoji,          │ │
│  │  created_at, updated_at, archived                           │ │
│  └─────────────────────────────────────────────────────────────┘ │
│  ┌ 2. Body ────────────────────────────────────────────────────┐ │
│  │  markdown + [[entity:uuid|text]] + {{query:...}}            │ │
│  │  body_refs[] — UUID, извлечённые из body при сохранении     │ │
│  └─────────────────────────────────────────────────────────────┘ │
│  ┌ 3. Tags + Meta ─────────────────────────────────────────────┐ │
│  │  tags[] — нормализованные ярлыки (от AI и пользователя)     │ │
│  │  meta{} — AI-извлечённые ключ-значения до структурирования  │ │
│  └─────────────────────────────────────────────────────────────┘ │
│  ┌ 4. Aspects ─────────────────────────────────────────────────┐ │
│  │  { "orbis/task": {...}, "orbis/financial": {...} }          │ │
│  │  типизированные данные по схемам из реестра аспектов        │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

- **Core** — минимум, который есть у всего: `id` (UUIDv7, генерируется на клиенте — время в префиксе даёт естественную хронологическую сортировку и работу офлайн), `title`, опциональный `emoji`, таймстемпы, флаг `archived` (мягкое удаление; жёсткого удаления сущностей в MVP нет).
- **Body** — markdown с двумя расширениями: inline-ссылка `[[entity:uuid|текст]]` и живой query-блок `{{query:...}}` (§6). Из body при каждом сохранении извлекается `body_refs[]` — массив UUID упомянутых сущностей, для быстрых backlink-запросов без парсинга markdown.
- **Tags + Meta** — прото-аспектный слой. Пишется всегда, даже когда данные уже структурированы в аспекты: теги дают сквозную фильтрацию, meta — сырьё для ретроактивной миграции (§3.10).
- **Aspects** — JSONB-карта `aspect_id → данные`, валидируемая по JSON-схемам из реестра (§3). Одна сущность может иметь несколько аспектов: «купить кроссовки 8000₽ к субботе» = одна сущность с `orbis/task` + `orbis/financial` + `orbis/schedule`.

### §2.2 Что является сущностью, а что нет

| Сущности (живут в `entities`) | Не сущности (отдельные таблицы) |
|---|---|
| Задачи | `user_settings` |
| Расходы и доходы (транзакции) | `aspect_definitions` (реестр аспектов) |
| Заметки | История чатов (`chat_threads`, `chat_messages`) |
| Проекты | Метеринг (`ai_usage`) |
| Конверты бюджета | Журнал синхронизации (`sync_log`) |
| Категории | |
| Memory-факты и правила AI | |
| Smart lists (query-блоки в body) | |
| Recurring-шаблоны и их инстансы | |
| Чекины привычек (future) | |

### §2.3 Правило идентичности

**Сущность — то, на что пользователь ссылается как на вещь с собственной судьбой.** У неё есть имя, её можно открыть, связать с другими, заархивировать, обсудить в её треде. Категория «Еда» — сущность: пользователь переименовывает её, пишет в её body правила категоризации, смотрит её транзакции. Настройка `weekStartDay` — не сущность: это параметр поведения системы, у него нет собственной жизни в графе. Этим правилом проверяется каждый новый тип данных: если объект нужно показывать в Browser, связывать relations или обсуждать — это сущность; если это конфигурация или журнал — это таблица.

### §2.4 Сквозной пример

Сущность из ввода «купить кроссовки 8000₽ к субботе» — все четыре слоя в одном JSON (служебные столбцы `user_id` и client-only `synced_at` опущены; поля аспектов — по схемам §3.2 и §3.3):

```json
{
  "id": "019ea8b1-4778-7f3d-9a5c-6a521fa1cc24",
  "title": "Купить кроссовки",
  "emoji": "👟",
  "body": "К субботней пробежке. Модель выбрана в [[entity:019e4466-1000-7e07-b5d4-64be9721da51|Wishlist: бег]].",
  "body_refs": ["019e4466-1000-7e07-b5d4-64be9721da51"],
  "tags": ["task", "expense", "running"],
  "meta": {
    "raw_input": "купить кроссовки 8000₽ к субботе"
  },
  "aspects": {
    "orbis/task": {
      "status": "planned",
      "priority": "medium",
      "due_date": "2026-06-13"
    },
    "orbis/financial": {
      "amount": 8000,
      "currency": "RUB",
      "direction": "expense",
      "category_ref": "019d48ea-2e00-7a52-876a-c301529b0456",
      "planned": true
    }
  },
  "created_at": "2026-06-08T19:24:11Z",
  "updated_at": "2026-06-08T19:24:11Z",
  "archived": false
}
```

Здесь: `body_refs` извлечён из `[[entity:...]]`-ссылки в body; `category_ref` указывает на категорию-сущность «Одежда»; `planned: true` — покупка ещё не совершена, расход виден в прогнозах, но не списывается из конверта (§3.3, §3.5). Ключ `meta.raw_input` — служебный (исходная фраза пользователя); соглашение §3.9 касается извлечённых структурных ключей.

---

## §3. Аспекты MVP

Семь встроенных аспектов. Схема каждого хранится в реестре `aspect_definitions` (§4.3) как JSON Schema; ниже — нормативное содержание этих схем. Все семь активны для каждого пользователя с первого дня: passive-состояния в v3 нет.

Условные обозначения: ✓ в колонке Req — поле обязательно при наличии аспекта на сущности.

### §3.1 `orbis/schedule` — время

| Поле | Тип | Req | Описание |
|---|---|---|---|
| `start_at` | timestamp (ISO 8601) | ✓ | Начало события / момент привязки ко времени |
| `end_at` | timestamp (ISO 8601) | | Конец события; если задан — должен быть ≥ `start_at` |
| `duration_min` | integer | | Длительность в минутах (альтернатива `end_at`; если заданы оба — приоритет у `end_at`) |
| `all_day` | boolean | | Событие на весь день (default `false`) |
| `recurrence` | object | | Правило повторения: `{freq: daily\|weekly\|monthly, interval: int, byweekday?: string[], until?: date}`. Наличие = сущность является recurring-шаблоном |
| `location` | string | | Место |
| `timezone` | string (IANA) | | Таймзона события; по умолчанию — таймзона пользователя |

`tag_mappings`: `["schedule", "event", "meeting", "appointment"]`

Recurrence живёт только здесь — единый источник истины «когда повторяется» для любых доменов (платежи, тренировки, привычки). Инстансы порождаются от шаблона связью `derived_from` с детерминированными ID (§5.5).

### §3.2 `orbis/task` — задача

| Поле | Тип | Req | Описание |
|---|---|---|---|
| `status` | enum: `inbox` \| `planned` \| `in_progress` \| `waiting` \| `done` \| `cancelled` | ✓ | Состояние задачи; default при создании — `inbox` |
| `priority` | enum: `low` \| `medium` \| `high` | | Приоритет |
| `due_date` | date (ISO 8601) | | Срок |
| `completed_at` | timestamp | | Момент завершения; проставляется при переходе в `done`, при откате задачи из `done` — очищается |
| `effort_min` | integer | | Оценка трудозатрат в минутах |
| `waiting_for` | string | | Кого/чего ждём (для `status=waiting`) |

`tag_mappings`: `["task", "todo", "action", "deadline"]`

### §3.3 `orbis/financial` — финансовая операция

| Поле | Тип | Req | Описание |
|---|---|---|---|
| `amount` | number > 0 | ✓ | Сумма (всегда положительная; знак задаёт `direction`) |
| `currency` | string (ISO 4217) | | Валюта; default — `user_settings.defaultCurrency` |
| `direction` | enum: `income` \| `expense` | ✓ | Направление операции |
| `category_ref` | uuid | ✓ | Ссылка на **категорию-сущность** (с аспектом `orbis/category`) |
| `planned` | boolean | | `true` = запланированная операция (ещё не факт): видна в прогнозах и секции planned, не списывается из конверта как фактический расход. Default `false` |
| `recurring` | boolean | | `true` = сущность является шаблоном регулярной операции (период — в `orbis/schedule.recurrence` той же сущности); инстансы связываются `derived_from` |
| `payment_method` | string | | Способ оплаты (карта, наличные, счёт) |
| `counterparty` | string | | Контрагент (магазин, работодатель, человек) |

`tag_mappings`: `["expense", "income", "payment", "cost"]`

Категория — ссылка на сущность (`category_ref`), а не строка: переименование категории не требует массового обновления транзакций, категория несёт иерархию (через `parent`), body-правила и собственный тред.

**Инвариант recurring.** Канонический источник повторяемости — `orbis/schedule.recurrence` на сущности-шаблоне (§3.1). `orbis/financial.recurring=true` — информационный маркер для прогнозов (Coming up, прогноз баланса), валидный только на шаблоне с `recurrence` или на его инстансах (связь `derived_from`). Несогласованность — `recurring=true` без `recurrence` и без `derived_from` — предмет валидации при записи.

### §3.4 `orbis/note` — заметка (маркер)

| Поле | Тип | Req | Описание |
|---|---|---|---|
| `content_type` | enum: `markdown` \| `plain` \| `checklist` | | Тип содержимого; default `markdown` |
| `pinned` | boolean | | Закреплена в списках заметок; default `false` |

`tag_mappings`: `["note", "thought", "idea", "journal"]`

Содержимое заметки живёт в `body` (как у любой сущности); аспект — маркер «главное назначение этой сущности — текст», делающий её фильтруемой как заметку. Задача с богатым body не обязана иметь `orbis/note`.

### §3.5 `orbis/budget` — конверт бюджета

| Поле | Тип | Req | Описание |
|---|---|---|---|
| `category_ref` | uuid | ✓ | Категория-сущность, к которой относится конверт |
| `limit` | number ≥ 0 | ✓ | Лимит конверта на период |
| `currency` | string (ISO 4217) | | Валюта; default — `user_settings.defaultCurrency` |
| `period_start` | date (ISO 8601) | ✓ | Начало периода (включительно) |
| `period_end` | date (ISO 8601) | ✓ | Конец периода (включительно) |
| `carryover` | number | | Остаток, перенесённый из предыдущего периода (может быть отрицательным — перерасход). Default `0`. Доступно в периоде = `limit + carryover − spent` |

`tag_mappings`: `["budget", "envelope", "limit"]`

**`spent` не хранится** — потраченное вычисляется из транзакций (сущностей с `orbis/financial`, `planned=false`), привязанных к конверту связью `parent` (конверт — родитель транзакции). Это единственный источник истины: правка, перенос или архивация транзакции немедленно и без рассинхрона меняет состояние конверта. Любое отображаемое значение `spent` — вычисление на лету или кэш по правилу 3 (§10).

Пример пары «конверт + транзакция» (показаны значимые слои; остальные поля — как в §2.4):

```json
{
  "id": "019e81fb-7180-7306-ad8f-842ad8efb58d",
  "title": "Еда — июнь 2026",
  "tags": ["budget"],
  "aspects": {
    "orbis/budget": {
      "category_ref": "019d48ea-4188-765d-8e96-93a0ad9c262a",
      "limit": 30000,
      "currency": "RUB",
      "period_start": "2026-06-01",
      "period_end": "2026-06-30",
      "carryover": -1200
    }
  }
}
```

```json
{
  "id": "019eac7a-5980-7fa8-8425-8b14dfcbba25",
  "title": "Обед",
  "tags": ["expense", "food"],
  "meta": { "raw_input": "обед 340" },
  "aspects": {
    "orbis/financial": {
      "amount": 340,
      "currency": "RUB",
      "direction": "expense",
      "category_ref": "019d48ea-4188-765d-8e96-93a0ad9c262a",
      "planned": false
    }
  }
}
```

Их связывает relation `parent` (§4.2): `source_id` = конверт `019e81fb-…`, `target_id` = транзакция `019eac7a-…` — конверт является родителем транзакции. Оба `category_ref` указывают на одну категорию-сущность «Еда» (стартовая категория №1, 02-core-os §7.1; «продукты» — её alias, не отдельная категория). `spent` конверта на лету = сумма `amount` всех его транзакций-детей с `planned=false`; доступно в периоде = `30000 + (−1200) − spent`.

### §3.6 `orbis/category` — категория

| Поле | Тип | Req | Описание |
|---|---|---|---|
| `icon` | string | | Иконка (имя иконки или emoji) |
| `color` | string (hex) | | Цвет категории — используется конвертами и графиками |
| `aliases` | string[] | | Синонимы для резолва ввода, включая русские. Пример: `["еда", "продукты", "обед", "food", "groceries", "lunch"]` |

`tag_mappings`: `["category"]`

Иерархия категорий — через relations `parent` (категория-родитель → подкатегория). Правила категоризации («бар → Развлечения») пользователь пишет в body категории или они появляются как memory-правила (§7.4).

### §3.7 `orbis/memory` — память AI

| Поле | Тип | Req | Описание |
|---|---|---|---|
| `kind` | enum: `fact` \| `rule` | ✓ | `fact` — знание о пользователе («работаю из дома по пятницам»); `rule` — правило обработки ввода («бар → категория Развлечения») |
| `scope` | string (aspect-id) | | Опциональная привязка к аспекту (например, `orbis/financial`): правило инжектится/применяется только в контексте этого домена. Пусто = глобально |

`tag_mappings`: `["memory", "preference", "rule"]`

Memory-факты — обычные сущности: видимы и редактируемы в Browser, пользователь курирует свою память напрямую (§7.4).

### §3.8 Реестр: встроенные аспекты

| Aspect ID | Название | Default-статус |
|---|---|---|
| `orbis/schedule` | Schedule | active |
| `orbis/task` | Task | active |
| `orbis/financial` | Financial | active |
| `orbis/note` | Note | active |
| `orbis/budget` | Budget | active |
| `orbis/category` | Category | active |
| `orbis/memory` | Memory | active |

Будущие аспекты (`orbis/fitness`, `orbis/habit`, `orbis/goal` и пользовательские) — новые записи в реестре + views, без изменений ядра (§11).

### §3.9 Соглашение об именах meta-ключей

Правило для AI-инструкций (фиксируется в system prompt и в `ai_instructions` аспектов): **имена ключей в `meta` обязаны совпадать с именами полей аспектов**. AI, извлекая структуру из «потратил 500 на такси» до/помимо аспекта, пишет `meta: {amount: 500, direction: "expense"}` — те же имена, что в схеме `orbis/financial`. Это делает ретроактивную миграцию (§3.10) механической операцией, а не угадыванием.

### §3.10 Ретроактивная миграция

Механизм подключения будущих аспектов к уже накопленным данным. Когда в реестре появляется новый аспект (например, `orbis/fitness`), исторические сущности, описывавшие этот домен тегами и meta, подключаются к нему так:

1. **Поиск кандидатов** — запрос по `tag_mappings` нового аспекта: все сущности, у которых есть хотя бы один из маппинг-тегов и нет самого аспекта.
2. **Маппинг meta → поля аспекта** — благодаря соглашению §3.9 перенос прямой: `meta.amount → aspect.amount`. Ключи meta, не входящие в схему аспекта, остаются в meta.
3. **Валидация по схеме** — собранный объект аспекта проверяется JSON-схемой из реестра (обязательные поля, типы, enum-значения).
4. **Attach** — валидные сущности получают аспект; перенесённые ключи удаляются из meta.
5. **Невалидные** — сущности, не прошедшие валидацию (нет обязательного поля, неверный тип), получают флаг ручного разбора (тег `needs-review`). Пользователь видит их в **Browser через фильтр «требуют разбора»** и дозаполняет вручную или через чат («у этих пяти тренировок не хватает даты — расставь по описанию»).

Миграция идемпотентна: повторный запуск пропускает сущности, у которых аспект уже есть.

---

## §4. Таблицы БД

Восемь таблиц. Всё, что является сущностью (§2.2), живёт в `entities`; остальные таблицы — связи, реестр, конфигурация, чаты и журналы.

### §4.1 `entities`

| Столбец | Тип | Описание |
|---|---|---|
| `id` | uuid PK | UUIDv7, генерируется клиентом |
| `user_id` | uuid NOT NULL | Владелец (FK → `auth.users`) |
| `title` | text NOT NULL | Отображаемое имя |
| `emoji` | text | Визуальный идентификатор |
| `body` | text NOT NULL default `''` | Markdown с расширениями `[[entity:...]]` и `{{query:...}}` |
| `body_refs` | text[] NOT NULL default `{}` | UUID сущностей, извлечённые из body; автовычисляется при каждом сохранении |
| `tags` | text[] NOT NULL default `{}` | Нормализованные английские теги (нижний регистр) |
| `meta` | jsonb NOT NULL default `{}` | AI-извлечённые ключ-значения (прото-аспектный слой) |
| `aspects` | jsonb NOT NULL default `{}` | Карта `aspect_id → данные` по схемам реестра |
| `created_at` | timestamptz NOT NULL | |
| `updated_at` | timestamptz NOT NULL | Основа LWW-разрешения конфликтов (§5) |
| `synced_at` | timestamptz | **Client-only** (только IndexedDB, на сервере столбца нет — §4.12): NULL = локальное изменение ещё не отправлено |
| `archived` | boolean NOT NULL default `false` | Мягкое удаление |

### §4.2 `relations`

| Столбец | Тип | Описание |
|---|---|---|
| `id` | uuid PK | Генерируется клиентом |
| `source_id` | uuid NOT NULL | FK → `entities.id`, ON DELETE CASCADE |
| `target_id` | uuid NOT NULL | FK → `entities.id`, ON DELETE CASCADE |
| `relation_type` | text NOT NULL | `parent` \| `blocks` \| `related_to` \| `derived_from` |
| `meta` | jsonb NOT NULL default `{}` | Например `{source: "body_ref", auto: true}` для неявных связей из body |
| `created_at` | timestamptz NOT NULL | |
| `updated_at` | timestamptz NOT NULL | Курсор sync для relations |
| `deleted_at` | timestamptz | Tombstone: NULL = связь жива; заполнен = удалена (§5.4) |

Семантика типов: `parent` — source является родителем target (проект → задача, конверт → транзакция, категория → подкатегория); `blocks` — source блокирует target; `related_to` — мягкая связь (явная или автоматическая из body_refs); `derived_from` — target порождён от source (recurring-шаблон → инстанс).

Constraints: `CHECK (source_id != target_id)` (запрет связи с самим собой); partial unique index `UNIQUE (source_id, target_id, relation_type) WHERE deleted_at IS NULL` — уникальность только среди живых связей; tombstone-строки (§5.4) под индекс не подпадают, поэтому пересоздание удалённой связи — это новая строка с новым `id`.

**Инвариант ацикличности `blocks`.** Граф `blocks`-связей обязан оставаться ацикличным: сервер отклоняет `relation_create` (§9.2), замыкающую цикл, со структурированной ошибкой, содержащей путь цикла («A → B → C → A»). Проверка — обход графа живых `blocks`-связей при создании; алгоритм обхода — деталь реализации и здесь не специфицируется. Поведение UI при отказе — 02-core-os.md §6.

### §4.3 `aspect_definitions`

| Столбец | Тип | Описание |
|---|---|---|
| `id` | text NOT NULL | Namespaced: `orbis/task`, `user/sleep` (уникальность — см. ниже) |
| `user_id` | uuid | NULL = встроенный аспект; иначе — владелец кастомного |
| `name` | text NOT NULL | Отображаемое имя |
| `namespace` | text NOT NULL | `orbis`, `user`, имя автора |
| `description` | text | Человекочитаемое описание назначения аспекта |
| `icon` | text | Иконка аспекта для UI (карточки, реестр, «+аспект») |
| `schema` | jsonb NOT NULL | JSON Schema полей аспекта |
| `ai_instructions` | text | Фрагмент промпта: когда и как AI применяет аспект |
| `tag_mappings` | text[] NOT NULL default `{}` | Теги, указывающие на аспект (для ретроактивной миграции и подсказок) |
| `aggregations` | jsonb default `{}` | Декларация агрегаций по полям аспекта (клапан для аналитики, §12) |
| `view_config` | jsonb default `{}` | Подсказки рендеринга для generic-views |
| `created_at` | timestamptz NOT NULL | |

Уникальность — два partial unique index, без surrogate PK: встроенные аспекты (`user_id IS NULL`) — глобально уникальный `id` (`UNIQUE (id) WHERE user_id IS NULL`); кастомные — `UNIQUE (user_id, id)`. Два пользователя могут завести каждый свой `user/sleep`, не конфликтуя; в видимости одного пользователя (встроенные + свои) `id` однозначен, поэтому ключи в `entities.aspects` резолвятся без коллизий.

### §4.4 `user_settings`

Одна строка на пользователя. Это конфигурация, не сущность. Имена столбцов настроек — в camelCase (историческое соответствие коду); в остальных таблицах — snake_case.

| Столбец | Тип | Описание |
|---|---|---|
| `user_id` | uuid PK | FK → `auth.users` |
| `plan` | text NOT NULL default `'dev'` | Тарифный план; маппинг план → entitlements — в серверном конфиге (§8) |
| `timezone` | text NOT NULL | IANA-таймзона (default `Europe/Moscow`) |
| `defaultCurrency` | text NOT NULL | ISO 4217 (default `RUB`) |
| `weekStartDay` | text NOT NULL | `monday` \| `sunday` |
| `tagColors` | jsonb NOT NULL default `{}` | Тег → цвет |
| `installedViews` | text[] NOT NULL default `{}` | Установленные views: `["orbis-budget"]` |
| `pinnedEntities` | jsonb NOT NULL default `[]` | Упорядоченный список `{id, order}` для сайдбара Browser |
| `viewPreferences` | jsonb NOT NULL default `{}` | Настройки доменов/views, namespaced: `{"orbis-budget": {...}}`. Новый view — новый ключ, без миграции БД |
| `updated_at` | timestamptz NOT NULL | |

Поля `statusStripMetrics` в v3 **нет** — status strip отложен (Future Directions, 00-product.md); его настройки появятся вместе с ним. Поля статусов аспектов тоже нет: passive-состояние убрано, все аспекты MVP активны.

### §4.5 `chat_threads`

| Столбец | Тип | Описание |
|---|---|---|
| `id` | uuid PK | Генерируется клиентом |
| `user_id` | uuid NOT NULL | Владелец |
| `entity_id` | uuid | FK → `entities.id`. **NULL = глобальный тред** (мессенджер-модель: один бесконечный диалог); иначе — персистентный тред сущности |
| `title` | text | Заголовок (для глобального не отображается) |
| `archived` | boolean NOT NULL default `false` | |
| `created_at` | timestamptz NOT NULL | |
| `updated_at` | timestamptz NOT NULL | |

Инвариант приложения: не более одного треда на сущность и один глобальный тред на пользователя. Схема намеренно не запрещает множественные глобальные треды — это путь расширения без миграции.

Глобальный тред создаётся при онбординг-сидировании (02-core-os.md §7.3) с детерминированным ID `uuidv5(ORBIS_NAMESPACE, "<user_id>:global-thread")` — приём §5.5: повторное сидирование и инициализация с нескольких устройств дают один и тот же UUID, дубль невозможен по построению.

### §4.6 `chat_messages`

| Столбец | Тип | Описание |
|---|---|---|
| `id` | uuid PK | Генерируется клиентом |
| `thread_id` | uuid NOT NULL | FK → `chat_threads.id` |
| `role` | text NOT NULL | `user` \| `assistant` \| `system` |
| `content` | text NOT NULL | Текст сообщения |
| `metadata` | jsonb NOT NULL default `{}` | Состав: `{actions: [...], cards: [...], suggestions: [...]}` — `actions` — журнал действий (формат — §7.8), `cards` — карточки сообщения, `suggestions` — suggestion-чипы |
| `created_at` | timestamptz NOT NULL | |

Таблица **append-only**: сообщения не редактируются и не удаляются, поэтому `updated_at` отсутствует, а sync-merge тривиален (§5.6). `metadata` неизменяема после записи: отмена действия — не правка журнала, а **новое** системное сообщение `{type: "undo", undoes: <action_id>}` (механизм — §7.8).

### §4.7 `ai_usage`

Метеринг LLM-запросов с первого дня — per user, per day, per model.

| Столбец | Тип | Описание |
|---|---|---|
| `user_id` | uuid NOT NULL | |
| `date` | date NOT NULL | Календарный день (в таймзоне UTC) |
| `model` | text NOT NULL | Идентификатор модели |
| `input_tokens` | bigint NOT NULL default 0 | Накопительно за день |
| `output_tokens` | bigint NOT NULL default 0 | Накопительно за день |
| `request_count` | integer NOT NULL default 0 | Накопительно за день |

PK: `(user_id, date, model)`. Каждый LLM-вызов инкрементирует строку. Читается entitlements-резолвером (§8) для дневных лимитов.

### §4.8 `sync_log`

| Столбец | Тип | Описание |
|---|---|---|
| `id` | uuid PK | |
| `user_id` | uuid NOT NULL | |
| `device_id` | text NOT NULL | Идентификатор устройства-клиента |
| `last_sync_at` | timestamptz NOT NULL | Курсор устройства (§5.1) |
| `entity_count` | integer | Записей в последнем батче |
| `conflicts` | jsonb default `[]` | Журнал конфликтов для отладки |
| `created_at` | timestamptz NOT NULL | |

Одна строка на устройство — upsert по `(user_id, device_id)`: каждый sync-цикл перезаписывает `last_sync_at`, `entity_count` и `conflicts` своей строки, история циклов не накапливается.

### §4.9 Индексы

| Таблица | Индекс | Назначение |
|---|---|---|
| `entities` | GIN (`tags`) | Фильтры по тегам (containment) |
| `entities` | GIN (`aspects`) | Наличие аспекта, значения полей аспектов |
| `entities` | GIN (`meta`) | Поиск по meta (AI-контекст, миграция) |
| `entities` | GIN (`body_refs`) | Backlinks: «кто ссылается на X» |
| `entities` | GIN (`to_tsvector('simple', title)`), GIN (`to_tsvector('simple', body)`) | Full-text `search=` по title + body |
| `entities` | b-tree (`user_id`, `updated_at` DESC) WHERE NOT `archived` | Основной листинг и pull-sync |
| `entities` | IndexedDB-индекс `synced_at` (**клиентский**, §4.12) | Push-sync: очередь несинхронизированных изменений |
| `relations` | b-tree (`source_id`, `relation_type`) | Дети сущности (`children_of`) |
| `relations` | b-tree (`target_id`, `relation_type`) | Родители/блокеры (`parents_of`, `excludeBlocked`) |
| `relations` | b-tree (`updated_at`) | Sync relations, включая tombstones |

### §4.10 Row Level Security

Принцип один: **каждая строка принадлежит ровно одному пользователю, и видит/меняет её только владелец**. Политика-шаблон для всех таблиц с `user_id`:

```sql
CREATE POLICY user_owns_row ON <table>
  FOR ALL USING (user_id = auth.uid());
```

Особые случаи: `relations` (нет своего `user_id`) — доступ через владельца source-сущности; `aspect_definitions` — встроенные записи (`user_id IS NULL`) читаемы всеми, изменяемы никем, кастомные — по шаблону.

### §4.11 Реестр тегов — вычислимый

Отдельной таблицы тегов нет. Реестр тегов (для автокомплита, фильтров, цветов) — агрегация `SELECT unnest(tags), count(*) FROM entities GROUP BY 1` плюс клиентский кэш, обновляемый при записи сущностей. Цвета тегов — `user_settings.tagColors`. Это устраняет целый класс проблем синхронизации справочника с фактическими данными.

### §4.12 Клиент vs сервер

Схемы §4.1–§4.8 — серверные (PostgreSQL); клиентская база (IndexedDB) — их зеркало с тремя отличиями:

- **Client-only столбец `entities.synced_at`** — состояние доставки изменений на *этом* устройстве (NULL = ещё не запушено). На сервере поля нет: доставка на устройства отслеживается курсорами `sync_log` (§4.8), а не флагом в строке.
- **Что зеркалируется**: `entities`, `relations`, `aspect_definitions` — полностью; `chat_threads`/`chat_messages` — частично (открытые и недавние треды, полная история — на сервере); `user_settings` — кэш одной строки. Дополнительно клиент хранит base-копии изменённых сущностей для diff-sync (§5.2). `ai_usage` и `sync_log` на клиенте не живут.
- **Индексы**: GIN (tags/aspects/meta/body_refs) и full-text — серверные (§4.9); клиентские — IndexedDB-индексы по `updated_at` (pull-merge), `synced_at` (очередь push) и полям, нужным JS-бэкенду query-движка (§6.2).

---

## §5. Sync-протокол

### §5.1 Модель

- **IndexedDB — источник истины на клиенте.** Все чтения и записи UI идут в локальную базу; приложение полностью работает офлайн (кроме LLM-пути, §7.9).
- **PostgreSQL — копия в облаке** и точка обмена между устройствами.
- **Push/pull по курсорам устройств.** Каждое устройство хранит курсор `last_sync_at` (отражён в `sync_log`). Цикл: push локальных изменений (diff'ы записей с `synced_at IS NULL` или `synced_at < updated_at` против base, §5.2) → сервер применяет merge-правила §5.2 → pull серверных изменений с `updated_at` позже курсора → курсор сдвигается.

Конфликт возникает, когда одна и та же запись изменена на двух устройствах после последней общей синхронизации. Базовый механизм разрешения — LWW (last-write-wins) по `updated_at`, но **гранулярность сравнения различается по полям** — её фиксирует матрица §5.2.

### §5.2 Матрица конфликтов

| Объект / поле | Гранулярность | Стратегия |
|---|---|---|
| `entities.title` | поле | LWW: значение из версии с поздним `updated_at` |
| `entities.emoji` | поле | LWW |
| `entities.archived` | поле | LWW |
| `entities.tags` | **массив целиком** | LWW всего массива |
| `entities.meta` | **ключ** | Key-level LWW: ключи, изменённые/добавленные только в одной версии, сохраняются из неё; ключ, изменённый в обеих, — из поздней; ключ, удалённый в поздней версии, удаляется |
| `entities.aspects` | **аспект** | Aspect-level LWW: каждый `aspect_id` сравнивается независимо по тем же правилам, что meta-ключи. Конфликт правок внутри одного аспекта — атомарно из поздней версии |
| `entities.body` | поле | LWW + **конфликтная копия** (§5.3) |
| `relations` | строка | Tombstone-модель (§5.4); поле `meta` — LWW |
| `chat_threads` | строка | LWW (`title`, `archived`) |
| `chat_messages` | строка | Append-only merge по `id`: объединение множеств, конфликтов нет по построению |

**Почему tags — LWW целиком, а не union-merge.** Union-merge (объединение массивов с двух устройств) выглядит дружелюбнее, но воскрешает удалённые теги: пользователь снял тег `work` на телефоне, ноутбук при merge возвращает его обратно — удаление неотличимо от «на этом устройстве тега ещё не было». Чтобы различать, пришлось бы хранить tombstone на каждый тег. Для одного пользователя с 2–3 устройствами честный LWW всего массива проще и предсказуемее; редкая потеря — тег, добавленный на проигравшем устройстве, — легко восстановима.

**Механизм применения: state-based diff против base.** Чтобы key- и aspect-level merge были точными, а не эвристикой, клиент хранит в IndexedDB **теневую копию (base)** — последнюю синхронизированную версию каждой локально изменённой сущности. Push отправляет не полную сущность, а diff локальной версии против base, послойно:

- изменённые и **удалённые** ключи `meta`;
- изменённые и **удалённые** аспекты (по `aspect_id`);
- изменения скалярных полей (`title`, `emoji`, `archived`; `tags` — массив как одно значение, в соответствии с матрицей);
- новый `body` — целиком, только если менялся.

Удаление ключа или аспекта — **явная операция в diff'е**, а не «отсутствие ключа»: это закрывает случай «ключ удалён в ранней версии» — сервер видит само намерение удаления и применяет к нему key-level LWW, не путая удаление с «на этом устройстве ключа не было». Сервер применяет diff поверх своей текущей версии по матрице выше: непересекающиеся изменения сливаются без потерь, пересекающиеся разрешаются LWW на зафиксированной гранулярности. После успешного push клиент обновляет base до подтверждённой серверной версии.

Это state-based diff sync: по проводу едут дельты состояний, но семантика разрешения конфликтов остаётся ровно той, что задана матрицей §5.2, — матрица нормативна и не меняется.

### §5.3 Body: конфликтная копия

Body — единственное поле, где тихий LWW означал бы потерю набранного текста. Поэтому при body-конфликте (обе версии изменили `body` после общей точки):

1. Применяется LWW — побеждает версия с поздним `updated_at`.
2. Проигравшая версия **сохраняется archived-снапшотом**: создаётся новая сущность с `archived=true`, title `«<оригинал> — конфликтная копия <дата>»`, телом проигравшей версии, тегом `conflict-copy` и relation `related_to` к оригиналу.

Пользователь видит копию в панели Related оригинала и находит все копии в Browser по тегу `conflict-copy`. Текст не теряется никогда; цена — изредка лишняя архивная сущность.

### §5.4 Relations: tombstones

Удаление связи — не `DELETE`, а проставление `deleted_at` (+ обновление `updated_at`). Sync relations идёт по `updated_at`, поэтому удаление доезжает до других устройств как обычное изменение; клиенты фильтруют `deleted_at IS NULL` при чтении. Без tombstone удалённая на одном устройстве связь возвращалась бы с другого (классическая дыра v2.2). Пересоздание удалённой связи — **новая строка с новым `id`**: уникальность живых связей обеспечивает partial unique index `WHERE deleted_at IS NULL` (§4.2), под который tombstone-строки не подпадают. Физическая очистка старых tombstones — фоновая задача сервера, допустима после того, как все устройства пользователя синхронизировались позже `deleted_at`.

### §5.5 Детерминированные ID порождаемых сущностей

Все сущности, которые система порождает по расписанию от шаблона, получают ID не случайно, а детерминированно. Формула byte-точная:

```
id = uuidv5(ORBIS_NAMESPACE, name)

ORBIS_NAMESPACE = "cb339e97-82d7-4d16-91c6-942d42df7054"   // фиксированная константа проекта
name            = "<template_id>:<YYYY-MM-DD>"
```

где `template_id` — UUID шаблона **в lowercase**, `<YYYY-MM-DD>` — дата инстанса, вычисленная в таймзоне `orbis/schedule.timezone` шаблона, а при её отсутствии — в таймзоне пользователя из `user_settings.timezone` (**не** в таймзоне устройства: устройства в разных таймзонах дали бы разные даты — и разные ID).

Пример вычисления: шаблон `019ded47-d100-717a-8307-a5b7a5be722f`, инстанс на 1 июля 2026 →

```
name = "019ded47-d100-717a-8307-a5b7a5be722f:2026-07-01"
id   = uuidv5("cb339e97-82d7-4d16-91c6-942d42df7054", name)
     = "e7d0bfa4-f62a-59c1-b560-1c17cb32e89f"
```

Два устройства, офлайн сгенерировавшие инстанс одного шаблона на одну дату, получают **один и тот же UUID** — при sync вставка идемпотентна по PK, дубль невозможен по построению.

**Материализация инстансов.** Инстансы recurring-шаблонов создаются лениво: любой запрос диапазона дат потребителем query-движка (Agenda, query-блоки, Budget) материализует инстансы видимого диапазона, но не дальше горизонта **14 дней вперёд** от сегодня. Идемпотентность гарантирована детерминированными ID: создание с одного или нескольких устройств даёт одну и ту же сущность; уже созданные инстансы не пересоздаются и не перезаписываются.

Применения: инстансы recurring-операций и событий (`orbis/financial.recurring` + `orbis/schedule.recurrence`), чекины привычек (future, §11). Это правило 2 проектирования аспектов (§10).

### §5.6 Sync чатов

- `chat_threads` — LWW по `updated_at` (конфликтовать могут только `title` и `archived`).
- `chat_messages` — append-only merge: объединение по `id` (UUID генерируется клиентом), сообщения не редактируются и не удаляются, поэтому конфликтов нет по построению. Порядок — по `created_at`.

---

## §6. Query-движок

Единая грамматика фильтрации сущностей. Запрос — строка из конструкций, разделённых запятыми; все условия соединяются логическим AND (внутри значения `|` даёт OR).

### §6.1 Грамматика

| Возможность | Синтаксис | Пример / семантика |
|---|---|---|
| Теги (OR внутри) | `tags=a\|b` | Сущности с тегом `work` ИЛИ `personal` |
| Исключение тегов | `excludeTags=x` | Исключить сущности с тегом |
| Наличие аспекта | `aspect=orbis/task` | Аспект должен присутствовать |
| Поле аспекта (OR) | `<поле>=v1\|v2` | `status=planned\|in_progress` |
| Отрицание | `<поле>=!v1&!v2` | `status=!done&!cancelled` — НЕ эти значения |
| Относительные даты | `<date-поле>=today\|overdue\|next_7d\|after_7d` | Токены применимы к **любому** полю типа date/timestamp: `due_date=today`, `start_at=next_7d`. `today` — сегодня; `overdue` — строго «значение поля < сегодня»; `next_7d` — диапазон [сегодня; сегодня + 7 дней], **включительно с обеих сторон**; `after_7d` — строго после этого диапазона: значение поля > сегодня + 7 дней |
| Числовое сравнение | `amount>1000` | Поле аспекта больше значения (аналогично `<`) |
| Числовой диапазон | `amount=500..2000` | Между значениями включительно |
| Дети сущности | `children_of=<uuid\|this>` | Вернуть детей X — сущности, у которых X — родитель (по `parent`). Так конверт показывает свои транзакции, проект — задачи |
| Родители сущности | `parents_of=<uuid\|this>` | Вернуть родителей X (по `parent`) |
| Исключение заблокированных | `excludeBlocked=true` | Исключить сущности, у которых есть входящая `blocks`-relation от сущности со статусом НЕ в `done\|cancelled` |
| Архивные | `archived=true\|any` | Токен отсутствует — только неархивные; `archived=true` — только архивные; `archived=any` — все |
| Сортировка multi-field | `sortBy=priority:desc\|due_date:asc` | Упорядоченный список полей с направлением |
| Полнотекстовый поиск | `search=API` | По `title` + `body` |
| Лимит | `limit=20` | Максимум результатов |
| Режим отображения | `display=compact\|list\|table` | Подсказка рендереру query-блока |
| Заголовок | `title=My Tasks` | Заголовок над результатами query-блока |

Лексика и экранирование:

- Значения, содержащие `,`, `|`, `&` или пробелы по краям, заключаются в двойные кавычки: `title="My Tasks, важное"`. Внутри кавычек `"` экранируется как `\"`. Переводы строк внутри `{{query:...}}`-блока эквивалентны пробелам.
- Смешивание `|` и `&` в одном значении (`status=a|b&!c`) — ошибка парсинга (§6.4).
- `due` — документированный алиас для `orbis/task.due_date`.
- `overdue` — чистое сравнение дат, **без доменной логики задач** («и не закрыто» в семантику токена не входит): для «просроченных и незакрытых» задач условия комбинируются явно — `due_date=overdue, status=!done&!cancelled`.
- Относительные date-токены комбинируются через `|` как обычные значения: `due_date=today|overdue` — срок сегодня ИЛИ просрочен.
- Сортировка enum-полей — по **порядку объявления значений в схеме аспекта** (`aspect_definitions.schema`), не по алфавиту. NULL-значения — всегда в конце, независимо от направления.

Правила резолва имени поля: сначала зарезервированные ключи грамматики из таблицы выше (`tags`, `aspect`, `sortBy`, `title`…), затем **core-поля сущности** `created_at`, `updated_at`, `title` (см. §4.1), затем имена полей аспектов (`status`, `amount`, `due_date`…) по схемам реестра. Core-поля доступны для фильтрации и сортировки наравне с полями аспектов: `created_at=today`, `sortBy=updated_at:desc`. Исключение — `title`: в позиции фильтра ключ `title=` занят параметром заголовка, поэтому core-поле `title` участвует только в `sortBy`, а отбор по заголовку выполняется через `search=`. При неоднозначности имени поля аспекта (одно имя в двух аспектах) запрос обязан содержать `aspect=`; имя, не разрешившееся ни одним шагом резолва, — неизвестное поле (ошибка парсинга, §6.4). `display` и `title` — параметры представления, на отбор не влияют.

Пример полного запроса в body проекта:

```
{{query: aspect=orbis/task, status=!done&!cancelled, excludeBlocked=true,
         children_of=this, sortBy=priority:desc|due_date:asc, limit=20,
         display=compact, title=Активные задачи проекта}}
```

Иллюстрация семантики (псевдо-SQL серверного бэкенда):

```sql
SELECT * FROM entities
WHERE user_id = :uid AND NOT archived
  AND aspects ? 'orbis/task'
  AND aspects->'orbis/task'->>'status' NOT IN ('done', 'cancelled')
  AND id IN (SELECT target_id FROM relations
             WHERE source_id = :this AND relation_type = 'parent'
               AND deleted_at IS NULL)
  -- excludeBlocked: NOT EXISTS входящей blocks-связи (deleted_at IS NULL)
  --                 от сущности со status NOT IN ('done', 'cancelled')
ORDER BY CASE aspects->'orbis/task'->>'priority'   -- порядок объявления enum в схеме
           WHEN 'low' THEN 0 WHEN 'medium' THEN 1 WHEN 'high' THEN 2
         END DESC NULLS LAST,
         (aspects->'orbis/task'->>'due_date') ASC NULLS LAST
LIMIT 20;
```

### §6.2 Два бэкенда и требование эквивалентности

Движок имеет два бэкенда: **SQL** (PostgreSQL, сервер) и **JS поверх IndexedDB** (клиент, работает офлайн). Спецификация — одна (эта глава); парсер грамматики — общий код.

**Приёмочный критерий эквивалентности:** существует один общий набор фикстур (сущности + relations) и набор эталонных запросов; оба бэкенда обязаны возвращать **одинаковые результаты** (тот же состав и порядок ID) на каждом запросе набора. Любая новая конструкция грамматики добавляется в набор фикстур одновременно с реализацией в обоих бэкендах. Расхождение бэкендов — баг релизного уровня: пользователь не должен видеть разные списки офлайн и онлайн.

### §6.3 Шесть потребителей

| Потребитель | Где живёт запрос | Бэкенд |
|---|---|---|
| Body-блоки `{{query:...}}` | body любой сущности | клиентский (рендер), серверный (AI-чтение) |
| Smart lists | body pinned-сущностей (Daily Planning, Upcoming, All Tasks) | клиентский |
| Фильтры views | состояние UI Browser/Budget/Agenda | клиентский |
| Прогресс целей (future) | `orbis/goal.progress_source.query` | оба (§11) |
| Авто-чекины привычек (future) | `orbis/habit.auto_source.query` | оба (§11) |
| AI-тул `entity_query` | вызовы LLM и MCP-агентов | серверный |

### §6.4 Поведение при ошибках

- **Невалидный запрос** (синтаксическая ошибка, неизвестное поле, неоднозначное поле без `aspect=`) — это **ошибка парсинга, отображаемая пользователю**: query-блок рендерится с сообщением об ошибке и указанием места; AI-тул получает структурированную ошибку. Запрос с ошибкой никогда не выполняется частично.
- **Пустой результат ≠ ошибка**: валидный запрос без совпадений рендерится как пустой список («ничего не найдено»). Различие принципиально: тихая пустота вместо сообщения об ошибке — главный способ потерять доверие к живым документам.

---

## §7. AI-слой

### §7.1 Пятислойная модель контекста

Каждый LLM-вызов собирает контекст из пяти слоёв:

```
┌ 1. System prompt ── статика: правила, соглашения, аспектные      ┐
│                     инструкции из реестра                        │ глобальный
├ 2. Память ───────── активные orbis/memory-сущности, кап ~50      ┤ глобальная
├ 3. Рабочий контекст─ активный view, открытая сущность            ┤ ситуативный
├ 4. История диалога ─ ТОЛЬКО текущий тред (rolling + summary)     ┤ скоупнутая
└ 5. Доступ к графу ── через тулы (§9.2), по необходимости         ┘ весь граф
```

| Слой | Содержимое | Токен-бюджет (ориентир) |
|---|---|---|
| 1. System prompt | Правила поведения, соглашение meta-ключей (§3.9), инструкции активных аспектов | ~800 |
| 2. Память | До 50 активных memory-фактов и правил (с учётом `scope`) | ~1–2K |
| 3. Рабочий контекст | Текущий view, открытая сущность (title, tags, аспекты, превью body) | ~500 |
| 4. История треда | Последние сообщения текущего треда + summary более старых | ~4K |
| 5. Доступ к графу | Через тулы (§9.2): в контекст входят определения тулов (tiered loading, §7.6), сами данные графа приходят ответами тулов. Токен-бюджет строки — стоимость определений тулов | ~1.5K |

Бюджеты — целевые значения для контроля стоимости, не жёсткие константы; точные числа живут в конфиге и метрятся через `ai_usage`.

### §7.2 Диалоги эфемерны, знания живут в графе

Принцип: **история тредов — рабочая поверхность, а не хранилище знаний**. То, что должно пережить разговор, переносится в граф — тремя путями:

1. **Memory-факт** — AI предлагает сохранить факт/правило, пользователь подтверждает → создаётся `orbis/memory`-сущность (§7.4).
2. **Дозапись в body якорной сущности** — вывод разговора в треде сущности дописывается в её body («добавь это резюме в проект»).
3. **Note-сущность** — отдельная заметка с relation `related_to` к обсуждавшейся сущности.

По умолчанию — изоляция: содержимое треда никуда не утекает без явного действия.

### §7.3 Треды

- **Глобальный тред** (`chat_threads.entity_id = NULL`) — мессенджер-модель: один бесконечный диалог, rolling window + summary. Это главная вкладка Chat.
- **Тред сущности** — персистентный диалог, привязанный к сущности; живёт вкладкой на её детальном экране. Контекст слоя 3 автоматически содержит эту сущность.
- **Тулы не скоупятся.** Из любого треда доступен весь набор тулов и весь граф: **скоупится разговор, а не способности**. В треде категории «Еда» можно спросить про задачи — AI дотянется через `entity_query`.

### §7.4 Память

Память AI — это `orbis/memory`-сущности (§3.7), а не скрытое состояние:

- **Видимы и редактируемы в Browser**: пользователь курирует память напрямую — правит формулировки, архивирует устаревшее.
- **Инжекция**: активные (не archived) memory-сущности попадают в слой 2; правила с `scope` — приоритетно в контексте своего домена. Кап ~50 фактов; при превышении — приоритет правилам (`kind=rule`) и недавно использованным.
- **Пополнение**: пользователь просит запомнить; AI предлагает сохранить факт из разговора (по подтверждению); эскалация исправлений (§7.8) порождает правила.

### §7.5 Fast-path: детерминированный парсер

Клиентский парсер на грамматике (без LLM) перехватывает частотные паттерны ввода до отправки в LLM. Мгновенно, бесплатно, **работает офлайн**. Пользователь не выбирает «режим» — fast-path прозрачен: если парсер уверен, карточка появляется немедленно; нет — ввод молча уходит LLM-путём.

Паттерны MVP:

| Паттерн | Пример | Результат |
|---|---|---|
| `<текст> <число>` | «обед 340» | Сущность с `orbis/financial`: `direction=expense`, `amount=340`, валюта по умолчанию; `category_ref` — резолв «обед» по `aliases` категорий-сущностей |
| `+<число> <текст>` | «+150000 зарплата» | `direction=income`, `amount=150000`; категория — по aliases |
| `<текст> <число> <валюта>` | «кофе 4 usd» | Как expense, но с явной `currency` |

Числа — целые и десятичные с точкой или запятой («340», «127.50», «99,90»). Валютные токены MVP: `₽`, `руб`, `р`, `$`, `€`, `USD`, `EUR`, `RUB` (без учёта регистра); без явного токена — валюта пользователя (`user_settings.defaultCurrency`).

Правила передачи в LLM (парсер обязан уступить при любой неуверенности):

- **Неизвестная категория** — текст не разрезолвился ни в один alias ни одной категории;
- **Множественные суммы** — в строке больше одного числа («обед 340 и такси 200»);
- **Вопросительная форма** — `?`, вопросительные слова («сколько я потратил на еду?») — это запрос, а не ввод.

Fast-path **применяет correction-правила из памяти** (`orbis/memory`, `kind=rule`, `scope=orbis/financial`): правило «бар → Развлечения» работает и в детерминированном пути, без LLM. Карточка результата fast-path идентична AI-карточке: inline-правка и Undo работают одинаково (§7.8).

### §7.6 Динамические тулы из реестра

Набор тулов LLM генерируется из реестра аспектов: **core-набор** (§9.2) + по одному тулу `attach_<aspect>` на каждый активный аспект (схема input = JSON Schema аспекта, описание = `ai_instructions`). Новый аспект в реестре автоматически даёт AI способность работать с ним — без изменения кода AI-слоя.

**Tiered loading** — контроль токен-бюджета слоя 5: core-тулы загружаются всегда; `attach_<aspect>`-тулы — полными схемами для аспектов, релевантных контексту разговора, и однострочными упоминаниями для остальных (LLM может запросить полную схему, начав использовать аспект).

### §7.7 Роутинг моделей

Два класса моделей, выбор — по типу операции:

| Класс | Операции |
|---|---|
| Дешёвая | Нормализация тегов, категоризация транзакций (в т.ч. CSV-импорт), summary тредов |
| Основная | Создание сущностей из свободного текста, cross-aspect запросы и рассуждения, диалог |

Имена конкретных моделей в PRD не фиксируются — это конфиг (слой LLM-agnostic, Claude по умолчанию). Каждый вызов метрится в `ai_usage` с указанием модели.

### §7.8 Corrections & Undo

- **Карточка действия.** Каждое AI- или fast-path-действие порождает карточку в чате с inline-редактированием полей (поменять категорию, сумму, дату — прямо в карточке) и кнопкой Undo.
- **Журнал действий.** Каждый исполненный tool call записывает обратную операцию в `chat_messages.metadata.actions`. Формат элемента:

```json
{
  "id": "0190a3c2-8e51-7b04-b21f-3d9e7a44c615",
  "type": "entity_created",
  "entity_id": "018f...",
  "inverse": { "op": "entity_archive", "payload": { "id": "018f..." } }
}
```

  Обратные операции: создание → архивация; обновление → восстановление старых значений (`payload` = прежние поля); создание relation → её удаление. Журнал общий для внутреннего чата, fast-path и MCP-агентов (§9.3).
- **Состояние Undo при append-only журнале.** `chat_messages.metadata` неизменяема (§4.6), поэтому отмена не правит записанное действие, а добавляет **новое системное сообщение** `{type: "undo", undoes: "<action_id>"}` (где `action_id` — `id` элемента журнала). Действие считается отменённым, если в журнале существует undo-запись с его `id`; повторная отмена отклоняется по этой же проверке. «Отмени последнее» сканирует журнал с конца, пропуская уже отменённые действия, и применяет `inverse` первого неотменённого.
- **Эскалация в правило.** После **двух одинаковых исправлений** (например, пользователь дважды переложил «бар» из «Еды» в «Развлечения») AI предлагает создать memory-правило; по подтверждению появляется `orbis/memory` (`kind=rule`, `scope=orbis/financial`), которое применяется и LLM-путём, и fast-path-парсером. Счётчик одинаковых исправлений не хранится отдельным состоянием — он вычисляется сканом журнала действий за последние 30 дней.

### §7.9 Деградация

| Состояние | Работает | Не работает |
|---|---|---|
| Офлайн | Fast-path-ввод, Browser, Agenda, Budget, любые правки сущностей (в IndexedDB, sync позже), чтение тредов | LLM-путь; LLM-запросы встают в **видимую очередь** и исполняются при появлении сети |
| Сеть есть, AI недоступен (сбой провайдера, исчерпан лимит) | Всё то же + полноценный sync данных | LLM-путь; та же видимая очередь + явное сообщение о причине |

Инвариант: без AI Orbis остаётся полнофункциональным локальным инструментом; AI — ускоритель, а не точка отказа.

---

## §8. Entitlements & metering

Шов для будущей монетизации, закладываемый сейчас, чтобы потом не переписывать проверки по всему коду.

- **Entitlements-резолвер** — один серверный модуль, отвечающий на вопрос «что можно этому пользователю и с какими лимитами» по строковым ключам фич. **Все** проверки лимитов в системе идут только через него.

| Ключ | Что ограничивает | Значение для plan `'dev'` |
|---|---|---|
| `ai.requests_per_day` | LLM-запросов в день | не ограничено |
| `ai.tokens_per_day` | Токенов (input+output) в день | не ограничено |
| `aspects.custom_max` | Кастомных аспектов в реестре | не ограничено |
| `import.csv` | Доступность CSV-импорта | разрешено |
| `history.depth_days` | Глубина истории в запросах | не ограничено |
| `agents.requests_per_day` | Запросов внешних агентов (MCP) в день | не ограничено |

- **`user_settings.plan`** — строка (сейчас у всех `'dev'`); маппинг план → значения ключей живёт в серверном конфиге, не в БД.
- **`ai_usage`** (§4.7) — метеринг с первого дня: резолвер сравнивает дневные счётчики с лимитами; владельцу метеринг полезен и лично (сколько стоит AI-слой).
- **Rate limits** (частотные ограничения AI- и agent-запросов) — тоже через резолвер, по тем же ключам.
- **НЕ делаем** в этой итерации: платёжную интеграцию, состояния подписки, тарифную сетку, paywall-экраны. Когда понадобятся — добавится конфиг планов и UI, но ни одна проверка в коде не изменится.

---

## §9. API и внешние агенты

### §9.1 Внутренний API (tRPC)

Транспорт клиент ↔ сервер — tRPC-роутеры:

| Роутер | Назначение |
|---|---|
| `entity` | CRUD сущностей, выполнение запросов движка (§6) на серверном бэкенде |
| `relation` | Создание/tombstone-удаление связей, чтение графа связей |
| `aspect` | Чтение реестра аспектов, CRUD кастомных аспектов, ретроактивная миграция (§3.10) |
| `user` | `user_settings`, онбординг-сидирование |
| `sync` | Push/pull-протокол (§5) |
| `ai` | LLM-диалог: сборка контекста (§7.1), исполнение тулов, журнал действий |
| `chat` | Треды и сообщения (`chat_threads`, `chat_messages`) |

Отдельного роутера категорий **нет** — категории суть сущности и ходят через `entity`.

Принципы: входы/выходы валидируются Zod-схемами из shared-пакета (одни типы у клиента и сервера, end-to-end типизация); все мутации — `protectedProcedure` (аутентифицированный пользователь); мутации клиента — оптимистичные (запись в IndexedDB сразу, sync следом); создание сущностей **идемпотентно по client-generated `id`** — повторный запрос с тем же UUID не создаёт дубль.

Точные сигнатуры процедур в PRD не фиксируются — этот контракт живёт в коде и проверяется типами. Единственное место, где API специфицируется как внешний контракт, — реестр тулов (§9.2).

### §9.2 Реестр тулов — единый публичный контракт

Один реестр тулов с одним tool-executor'ом и **двумя потребителями**: внутренний чат-LLM (§7) и внешние агенты через MCP (§9.3). Любое действие любого потребителя проходит через executor → попадает в журнал действий (§7.8) → подлежит Undo.

Core-тулы (нотация input-схем: `*` после типа — обязательное поле, `?` после имени — опциональное):

| Тул | Назначение | Input-схема |
|---|---|---|
| `entity_query` | Поиск/фильтрация сущностей грамматикой §6 | `{ query: string* }` — строка в грамматике §6 (включая `sortBy`, `limit`). Возвращает список сущностей (core + tags + aspects) |
| `entity_get` | Полное чтение одной сущности | `{ id: uuid*, include?: ("body" \| "relations" \| "backlinks" \| "thread")[] }` — по умолчанию `body` + `relations`; `backlinks` — кто ссылается через `body_refs`; `thread` — сообщения треда сущности |
| `entity_create` | Создание сущности | `{ id?: uuid, title: string*, emoji?: string, body?: string, tags: string[]*, meta?: object, aspects?: object }` — `id` опционален (идемпотентность повторов); `aspects` валидируются схемами реестра |
| `entity_update` | Частичное обновление | `{ id: uuid*, title?, emoji?, body?, tags?, meta?, aspects?: { [aspect_id]: object \| null }, archived? }` — передаются только изменяемые поля; `aspects` мержится по aspect-id, а внутри аспекта — по полям (shallow merge: переданы только `{status, completed_at}` → остальные поля аспекта сохраняются; поле со значением `null` удаляется); значение `null` вместо объекта аспекта **снимает аспект** целиком (detach), остальные данные сущности не затрагиваются |
| `relation_create` | Создание связи | `{ source_id: uuid*, target_id: uuid*, relation_type: ("parent" \| "blocks" \| "related_to" \| "derived_from")* }` |
| `relation_delete` | Удаление связи (tombstone) | `{ source_id: uuid*, target_id: uuid*, relation_type: ("parent" \| "blocks" \| "related_to" \| "derived_from")* }` — тот же enum, что в `relation_create` |

Внутренний хелпер `user_query` (вопрос-агрегация «сколько потрачено на еду за месяц») в публичный реестр **не входит** — это внутренний инструмент чат-LLM; MCP-агенты используют `entity_query` и агрегируют результаты сами.

Динамические тулы: **`attach_<aspect>`** — по одному на каждый активный аспект реестра, генерируются автоматически (§7.6):

| Тул | Input-схема |
|---|---|
| `attach_orbis_task`, `attach_orbis_financial`, … | `{ entity_id: uuid*, data: object* }` — `data` валидируется JSON-схемой соответствующего аспекта (§3) |

Контрактные гарантии executor'а: валидация input до исполнения; ошибки — структурированные (код + сообщение), без частичного применения; каждое мутирующее действие пишет обратную операцию в журнал (§7.8); лимиты — через entitlements-резолвер (§8).

### §9.3 MCP-сервер

MCP-сервер — **тонкий адаптер** над тем же tool-executor'ом, что у внутреннего чата: транслирует MCP-вызовы в тулы §9.2 один в один, без собственной бизнес-логики. Внешний агент (Claude Code и др.) получает ровно те же способности и ограничения, что внутренний AI.

- **Аутентификация: PAT-токен**, отдельный от Supabase JWT. MVP: один токен полного доступа на пользователя, генерация через CLI/env — без UI управления. Скоупы, несколько токенов, отзыв через UI — Future Directions (00-product.md).
- **Аудит**: действия агентов пишутся в тот же журнал действий (§7.8) — пользователь видит карточки агентских действий, inline-правка и **Undo работают и для агентов**.
- **Лимиты**: `agents.requests_per_day` и rate limits через entitlements-резолвер (§8).

Сквозной сценарий — «найди среди моих задач X и выполни»:

1. `entity_query({ query: "aspect=orbis/task, status=!done&!cancelled, search=X" })` → агент получает кандидатов, выбирает нужную задачу;
2. `entity_get({ id, include: ["body", "relations", "thread"] })` → агент читает постановку из body, контекст из связей (блокеры, родительский проект) и обсуждение из треда;
3. агент выполняет работу своими средствами (вне Orbis);
4. `entity_update({ id, aspects: { "orbis/task": { status: "done", completed_at: "<now>" } } })` → задача закрыта; действие — в журнале, пользователь видит карточку и может отменить.

---

## §10. Правила проектирования аспектов

Три правила, обязательные для всех аспектов — встроенных и будущих. Каждое выведено из конкретной ошибки v2.2.

**Правило 1: ограниченные массивы.** Массив внутри аспекта допустим, только если он ограничен по размеру и пишется в рамках одной сессии (сеты одной тренировки: ~5–30 элементов, write-once). Неограниченно растущие во времени ряды — это child-сущности через relation `parent`, не массив.

*Обоснование:* JSONB-аспект пишется атомарно — каждое добавление элемента переписывает весь объект (write-amplification), а aspect-level LWW при sync (§5.2) превращает конкурентные дописывания с двух устройств в потерю данных: побеждает один массив целиком. *Нарушение в v2.2:* `check_ins[]` внутри аспекта привычки — годовой ряд чекинов в одном JSONB: каждый чекин переписывал растущий объект, а чекины, поставленные офлайн на втором устройстве, стирались merge'ем.

**Правило 2: детерминированные ID порождаемых сущностей.** Всё, что система порождает от шаблона по расписанию, получает `id = uuidv5(ORBIS_NAMESPACE, "<template_id>:<дата>")` (точная формула — §5.5).

*Обоснование:* порождение происходит на клиенте и офлайн; при случайных UUID два устройства создают два разных объекта «один и тот же инстанс на одну дату», и sync честно сохраняет оба. Детерминированный ID делает вставку идемпотентной — дубль невозможен по построению, без дедупликации и эвристик. *Нарушение в v2.2:* recurring-инстансы со случайными UUID — дубли платежей и событий после офлайн-работы на двух устройствах.

**Правило 3: производные значения — кэш, не истина.** Стрики привычек, прогресс целей, `spent` конвертов вычисляются из графа (транзакции, чекины, query-агрегации). Хранить их в аспекте можно только как кэш, пересчитываемый при записи затрагивающих сущностей и после sync-merge.

*Обоснование:* производное значение, хранимое как истина, неизбежно расходится с источником — особенно после merge, который меняет состав исходных данных в обход кода доменного пересчёта. Кэш с обязательным пересчётом по двум триггерам (запись + post-merge) даёт скорость чтения без рассинхрона. *Нарушение в v2.2:* `streak` хранился в аспекте привычки как авторитетное значение — после sync-merge чекинов стрик молча показывал неверное число. В v3 тот же принцип закрывает `spent` конвертов (§3.5): хранили бы — разъезжался бы с транзакциями при каждой правке задним числом.

---

## §11. Приложение: проверка масштабируемости

Три будущих домена прогнаны через модель данных v3 — ядро (слои сущности, relations, query-движок, sync-правила) не потребовало изменений ни в одном случае.

### §11.1 Фитнес

```
Программа «База 3×/нед»            (entity + orbis/fitness-program)
 ├─ parent ──→ День A: грудь/трицепс   (orbis/workout-plan:
 │              упражнения — ограниченный массив, правило 1)
 │              └─ derived_from ──→ Тренировка 2026-06-09
 │                                  (orbis/fitness: фактические сеты
 │                                   + orbis/schedule: когда была)
 ├─ parent ──→ День B: спина/бицепс
 │              └─ derived_from ──→ Тренировка 2026-06-11 ...
 └─ parent ──→ День C: ноги
```

Сеты тренировки — массив внутри `orbis/fitness`: ограничен (десятки элементов) и пишется в одной сессии — правило 1 соблюдено. Каталог упражнений — клиентский JSON + кастомные упражнения как сущности. Ядру нужны только записи в реестре (`orbis/fitness-program`, `orbis/workout-plan`, `orbis/fitness`) и view.

### §11.2 Привычки

```
Привычка «Зал 3×/нед»   (orbis/habit: тип, frequency,
 │                       streak — КЭШ (правило 3),
 │                       auto_source: {query})
 ├─ parent ──→ Чекин 2026-06-09   id = uuidv5(NS, habit_id + ":2026-06-09")
 ├─ parent ──→ Чекин 2026-06-11   id = uuidv5(NS, habit_id + ":2026-06-11")
 └─ parent ──→ Чекин 2026-06-13   ...
```

Чекины — child-сущности (неограниченный ряд — правило 1), с детерминированными ID (правило 2), стрик — кэш, пересчитываемый из чекинов (правило 3). **Авто-чекин из графа**: `auto_source: {query}` — запрос движка §6 (например `aspect=orbis/fitness, start_at=today`); непустой результат автоматически зачитывает привычку за день — тренировка закрывает «зал 3×/нед» без ручного чекина. Это потребитель №5 единого query-движка.

### §11.3 Цели

```
Цель «Накопить 300 000 ₽»   (orbis/goal:
 │                           progress_source: {
 │                             query: "aspect=orbis/financial, direction=income,
 │                                     tags=savings",
 │                             aggregate: "sum",
 │                             field: "amount"
 │                           },
 │                           current_value — КЭШ (правило 3),
 │                           target_value: 300000)
 └─ related_to ──→ сущности-вехи, заметки
```

Прогресс цели = запрос движка §6 + агрегация: `aggregate: "sum" | "count" | "latest"`, агрегируемое поле — отдельным ключом `field` (для `sum` и `latest`; `count` поля не требует). «Прочитать 24 книги» — `aggregate: "count"`; «вес 80 кг» — `aggregate: "latest", field: "weight"`. `current_value` — кэш по правилу 3. Честное ограничение: агрегация `max` **внутрь** JSONB-массивов (цель «жим 100 кг» по сетам внутри тренировок) — за скоупом этого механизма (§12); такой цели потребуется либо вынос рекордов на уровень полей аспекта, либо расширение движка.

### §11.4 Вывод

**Ядро не меняется: новый домен = записи в реестре аспектов + view.** Слои сущности, типы relations, sync-матрица, грамматика запросов и контракт тулов покрывают все три домена в текущем виде; три правила §10 предотвращают известные по v2.2 ошибки на этапе проектирования аспекта.

---

## §12. Известные ограничения

Фиксируются честно — это осознанные компромиссы, а не упущения.

1. **Тяжёлая аналитика поверх JSONB** — не сильная сторона модели: сложные агрегации по полям аспектов (по месяцам × категориям × годам) упираются в JSONB-извлечение. Клапан: декларация `aggregations` в реестре аспектов (§4.3) как точка будущей материализации, без изменения модели данных.
2. **LWW-sync рассчитан на одного пользователя с 2–3 устройствами.** Конфликты редки и дёшевы (худший случай — конфликтная копия body). Family-/team-сценарии с конкурентной правкой одних сущностей потребуют пересмотра протокола (CRDT или серверная сериализация правок) — осознанно за скоупом.
3. **Семантический поиск отсутствует** — только full-text (`search=`, GIN по title + body). «Найди заметку, где я думал про смену работы» работает по словам, не по смыслу — до добавления embeddings (Future Directions, 00-product.md).
4. **`max`-агрегации внутрь JSONB-массивов** (личные рекорды из сетов внутри тренировок) — вне MVP-механизма целей (§11.3): `progress_source` агрегирует поля сущностей, но не элементы массивов внутри аспектов.
