/**
 * Идентификаторы контрактов v1 — состав §Б1-2 спеки «Реформа свойств».
 *
 * Это ШИМ интервала А→Б-1, и он снимается ПЕРВЫМ актом среза Б-1 (сид контрактов): тогда
 * же тест пиннит равенство этого списка множеству строк таблицы `contract_definitions`
 * после сида, а сам список исчезает.
 *
 * Зачем он нужен здесь. В срезе А таблица контрактов создаётся ПУСТОЙ (контракты — часть Б),
 * но свойство `orbis/rule_scope` уже имеет тип `registry_ref {target: contract}` и обязано
 * принимать `orbis/money-movement` в переведённых записях памяти (§А8, В7). Валидировать
 * ссылку по пустой таблице нечем — значит, до сида контрактов её проверяет этот список.
 * Альтернатива «сеять контракты в срезе А» отвергнута планом: контракт без слотов и привязок
 * — запись без смысла, а со слотами это уже срез Б.
 *
 * Порядок — как в §Б1-2: сначала четыре контракта ядра, затем четыре модульных.
 */
export const CONTRACT_IDS_V1 = [
  // Ядро (всегда включены — грамматика и общие движки стоят на них, №14):
  'orbis/completable',
  'orbis/when',
  'orbis/recurrence',
  'orbis/sensitivity',
  // Модули:
  'orbis/money-movement', // Финансы, 9 слотов
  'orbis/envelope', // Финансы, 6 слотов
  'orbis/progress', // Цели
  'orbis/categorizable', // Финансы
] as const;
export type ContractIdV1 = (typeof CONTRACT_IDS_V1)[number];
