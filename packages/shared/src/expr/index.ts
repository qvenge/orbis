/**
 * Вход языка E — `@orbis/shared/expr` (§Б3).
 *
 * Отдельный сабпат, а не корневой баррель, по той же ЗАЯВЛЕННОЙ границе, что у
 * `@orbis/shared/query` (докблок `query/index.ts`): потребителю языка нужны канон, чекер и
 * печать, но не схемы сущностей и не реестр целиком. В корневой баррель едут ТОЛЬКО коды
 * отказа (`expr/codes`, задача 1) — их импортирует `errors.ts` сервера ради одного имени на
 * два пакета.
 *
 * Фикстуры (`fixtures.ts`) сюда ВХОДЯТ, в отличие от корпуса Q: там они тянут встроенные
 * словари и разбирают их схемой на загрузке модуля, здесь — это данные из литералов, и
 * читает их не только тест пакета, но и валидатор подписок сервера (§С8-28).
 */
export * from './ast';
export * from './check';
export * from './codes';
export * from './fixtures';
export * from './json-schema';
export * from './normalize';
export * from './print';
