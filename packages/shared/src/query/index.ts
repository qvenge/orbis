/**
 * Вход канона Q-AST — `@orbis/shared/query` (§А5-7).
 *
 * Почему ОТДЕЛЬНЫЙ вход, а не корневой баррель пакета: имена `QueryAst`, `QueryDateToken`,
 * `QuerySortField` и `QueryDisplayMode` там уже заняты СТАРОЙ грамматикой (`query/grammar`),
 * которую по РП-11 нельзя трогать до Задачи 21 — её типы прямо сейчас держат серверный
 * компилятор, материализацию и конструктор запросов в web. Два `export *` с общими именами —
 * не «последний побеждает», а ошибка типизации (TS2308) и молчаливо исчезнувший экспорт.
 * Когда Задача 9b переключит сервер, а Задача 21 снесёт старую грамматику, содержимое этого
 * файла переезжает в корневой баррель под теми же именами — переименовывать будет нечего.
 *
 * Фикстуры (`ast-fixtures.ts`) сюда НЕ входят намеренно: они тянут встроенные словари и
 * разбирают их схемой на загрузке модуля — в браузерный чанк такому ехать незачем (тот же
 * порядок, что у старого `query/fixtures.ts`, которого в баррелях тоже нет).
 */
export * from './ast';
export * from './ast-json-schema';
export { buildCatalogFromRegistry } from './catalog';
// Переходный мост старой грамматики (Задача 9b, умирает вместе с ней в Задаче 21): он
// здесь, а не в корневом барреле, потому что отдаёт КАНОН — и потребитель у него один,
// серверный разбор текста.
export * from './legacy-bridge';
export * from './parse-ast';
export * from './print';
export * from './static';
